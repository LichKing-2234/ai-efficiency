// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/attributionclaimgroup"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// AttributionClaimGroupQuery is the builder for querying AttributionClaimGroup entities.
type AttributionClaimGroupQuery struct {
	config
	ctx        *QueryContext
	order      []attributionclaimgroup.OrderOption
	inters     []Interceptor
	predicates []predicate.AttributionClaimGroup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributionClaimGroupQuery builder.
func (acgq *AttributionClaimGroupQuery) Where(ps ...predicate.AttributionClaimGroup) *AttributionClaimGroupQuery {
	acgq.predicates = append(acgq.predicates, ps...)
	return acgq
}

// Limit the number of records to be returned by this query.
func (acgq *AttributionClaimGroupQuery) Limit(limit int) *AttributionClaimGroupQuery {
	acgq.ctx.Limit = &limit
	return acgq
}

// Offset to start from.
func (acgq *AttributionClaimGroupQuery) Offset(offset int) *AttributionClaimGroupQuery {
	acgq.ctx.Offset = &offset
	return acgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (acgq *AttributionClaimGroupQuery) Unique(unique bool) *AttributionClaimGroupQuery {
	acgq.ctx.Unique = &unique
	return acgq
}

// Order specifies how the records should be ordered.
func (acgq *AttributionClaimGroupQuery) Order(o ...attributionclaimgroup.OrderOption) *AttributionClaimGroupQuery {
	acgq.order = append(acgq.order, o...)
	return acgq
}

// First returns the first AttributionClaimGroup entity from the query.
// Returns a *NotFoundError when no AttributionClaimGroup was found.
func (acgq *AttributionClaimGroupQuery) First(ctx context.Context) (*AttributionClaimGroup, error) {
	nodes, err := acgq.Limit(1).All(setContextOp(ctx, acgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributionclaimgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) FirstX(ctx context.Context) *AttributionClaimGroup {
	node, err := acgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributionClaimGroup ID from the query.
// Returns a *NotFoundError when no AttributionClaimGroup ID was found.
func (acgq *AttributionClaimGroupQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = acgq.Limit(1).IDs(setContextOp(ctx, acgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributionclaimgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) FirstIDX(ctx context.Context) int {
	id, err := acgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributionClaimGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributionClaimGroup entity is found.
// Returns a *NotFoundError when no AttributionClaimGroup entities are found.
func (acgq *AttributionClaimGroupQuery) Only(ctx context.Context) (*AttributionClaimGroup, error) {
	nodes, err := acgq.Limit(2).All(setContextOp(ctx, acgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributionclaimgroup.Label}
	default:
		return nil, &NotSingularError{attributionclaimgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) OnlyX(ctx context.Context) *AttributionClaimGroup {
	node, err := acgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributionClaimGroup ID in the query.
// Returns a *NotSingularError when more than one AttributionClaimGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (acgq *AttributionClaimGroupQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = acgq.Limit(2).IDs(setContextOp(ctx, acgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributionclaimgroup.Label}
	default:
		err = &NotSingularError{attributionclaimgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) OnlyIDX(ctx context.Context) int {
	id, err := acgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributionClaimGroups.
func (acgq *AttributionClaimGroupQuery) All(ctx context.Context) ([]*AttributionClaimGroup, error) {
	ctx = setContextOp(ctx, acgq.ctx, ent.OpQueryAll)
	if err := acgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributionClaimGroup, *AttributionClaimGroupQuery]()
	return withInterceptors[[]*AttributionClaimGroup](ctx, acgq, qr, acgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) AllX(ctx context.Context) []*AttributionClaimGroup {
	nodes, err := acgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributionClaimGroup IDs.
func (acgq *AttributionClaimGroupQuery) IDs(ctx context.Context) (ids []int, err error) {
	if acgq.ctx.Unique == nil && acgq.path != nil {
		acgq.Unique(true)
	}
	ctx = setContextOp(ctx, acgq.ctx, ent.OpQueryIDs)
	if err = acgq.Select(attributionclaimgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) IDsX(ctx context.Context) []int {
	ids, err := acgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (acgq *AttributionClaimGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, acgq.ctx, ent.OpQueryCount)
	if err := acgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, acgq, querierCount[*AttributionClaimGroupQuery](), acgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) CountX(ctx context.Context) int {
	count, err := acgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (acgq *AttributionClaimGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, acgq.ctx, ent.OpQueryExist)
	switch _, err := acgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (acgq *AttributionClaimGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := acgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributionClaimGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (acgq *AttributionClaimGroupQuery) Clone() *AttributionClaimGroupQuery {
	if acgq == nil {
		return nil
	}
	return &AttributionClaimGroupQuery{
		config:     acgq.config,
		ctx:        acgq.ctx.Clone(),
		order:      append([]attributionclaimgroup.OrderOption{}, acgq.order...),
		inters:     append([]Interceptor{}, acgq.inters...),
		predicates: append([]predicate.AttributionClaimGroup{}, acgq.predicates...),
		// clone intermediate query.
		sql:  acgq.sql.Clone(),
		path: acgq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GroupID string `json:"group_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributionClaimGroup.Query().
//		GroupBy(attributionclaimgroup.FieldGroupID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (acgq *AttributionClaimGroupQuery) GroupBy(field string, fields ...string) *AttributionClaimGroupGroupBy {
	acgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributionClaimGroupGroupBy{build: acgq}
	grbuild.flds = &acgq.ctx.Fields
	grbuild.label = attributionclaimgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GroupID string `json:"group_id,omitempty"`
//	}
//
//	client.AttributionClaimGroup.Query().
//		Select(attributionclaimgroup.FieldGroupID).
//		Scan(ctx, &v)
func (acgq *AttributionClaimGroupQuery) Select(fields ...string) *AttributionClaimGroupSelect {
	acgq.ctx.Fields = append(acgq.ctx.Fields, fields...)
	sbuild := &AttributionClaimGroupSelect{AttributionClaimGroupQuery: acgq}
	sbuild.label = attributionclaimgroup.Label
	sbuild.flds, sbuild.scan = &acgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributionClaimGroupSelect configured with the given aggregations.
func (acgq *AttributionClaimGroupQuery) Aggregate(fns ...AggregateFunc) *AttributionClaimGroupSelect {
	return acgq.Select().Aggregate(fns...)
}

func (acgq *AttributionClaimGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range acgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, acgq); err != nil {
				return err
			}
		}
	}
	for _, f := range acgq.ctx.Fields {
		if !attributionclaimgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if acgq.path != nil {
		prev, err := acgq.path(ctx)
		if err != nil {
			return err
		}
		acgq.sql = prev
	}
	return nil
}

func (acgq *AttributionClaimGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributionClaimGroup, error) {
	var (
		nodes = []*AttributionClaimGroup{}
		_spec = acgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributionClaimGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributionClaimGroup{config: acgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, acgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (acgq *AttributionClaimGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := acgq.querySpec()
	_spec.Node.Columns = acgq.ctx.Fields
	if len(acgq.ctx.Fields) > 0 {
		_spec.Unique = acgq.ctx.Unique != nil && *acgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, acgq.driver, _spec)
}

func (acgq *AttributionClaimGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributionclaimgroup.Table, attributionclaimgroup.Columns, sqlgraph.NewFieldSpec(attributionclaimgroup.FieldID, field.TypeInt))
	_spec.From = acgq.sql
	if unique := acgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if acgq.path != nil {
		_spec.Unique = true
	}
	if fields := acgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributionclaimgroup.FieldID)
		for i := range fields {
			if fields[i] != attributionclaimgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := acgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := acgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := acgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := acgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (acgq *AttributionClaimGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(acgq.driver.Dialect())
	t1 := builder.Table(attributionclaimgroup.Table)
	columns := acgq.ctx.Fields
	if len(columns) == 0 {
		columns = attributionclaimgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if acgq.sql != nil {
		selector = acgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if acgq.ctx.Unique != nil && *acgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range acgq.predicates {
		p(selector)
	}
	for _, p := range acgq.order {
		p(selector)
	}
	if offset := acgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := acgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttributionClaimGroupGroupBy is the group-by builder for AttributionClaimGroup entities.
type AttributionClaimGroupGroupBy struct {
	selector
	build *AttributionClaimGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (acggb *AttributionClaimGroupGroupBy) Aggregate(fns ...AggregateFunc) *AttributionClaimGroupGroupBy {
	acggb.fns = append(acggb.fns, fns...)
	return acggb
}

// Scan applies the selector query and scans the result into the given value.
func (acggb *AttributionClaimGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acggb.build.ctx, ent.OpQueryGroupBy)
	if err := acggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionClaimGroupQuery, *AttributionClaimGroupGroupBy](ctx, acggb.build, acggb, acggb.build.inters, v)
}

func (acggb *AttributionClaimGroupGroupBy) sqlScan(ctx context.Context, root *AttributionClaimGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(acggb.fns))
	for _, fn := range acggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*acggb.flds)+len(acggb.fns))
		for _, f := range *acggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*acggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributionClaimGroupSelect is the builder for selecting fields of AttributionClaimGroup entities.
type AttributionClaimGroupSelect struct {
	*AttributionClaimGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (acgs *AttributionClaimGroupSelect) Aggregate(fns ...AggregateFunc) *AttributionClaimGroupSelect {
	acgs.fns = append(acgs.fns, fns...)
	return acgs
}

// Scan applies the selector query and scans the result into the given value.
func (acgs *AttributionClaimGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acgs.ctx, ent.OpQuerySelect)
	if err := acgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionClaimGroupQuery, *AttributionClaimGroupSelect](ctx, acgs.AttributionClaimGroupQuery, acgs, acgs.inters, v)
}

func (acgs *AttributionClaimGroupSelect) sqlScan(ctx context.Context, root *AttributionClaimGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(acgs.fns))
	for _, fn := range acgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*acgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
