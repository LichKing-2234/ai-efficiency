# Relay Planning #309-312

**Status:** The Relay Planning follow-up is published to production as `v0.1.0-preview.91` at application commit `3c5d271bc86c6f2ca4ff000ab43a719d3497bc7b`. Production revision 91 is healthy, and the saved staging mapping was synchronized to production after provider, local-user identity, directory-department, and live Relay relationship validation. Authenticated production Mapping, Replan, and Rebind-dialog checks passed without submitting a Relay mutation.

**Follow-up Status (2026-08-21, Issues #334 and #335):** Department-based new Target naming and opt-in existing Target rename are implemented, locally verified, pushed on `feat/relay-planning-department-names`, and opened as PR #337. Staging deployment and authenticated Chinese browser verification are complete without submitting a Relay mutation. This follow-up has not been deployed to production, and the published status above applies only to the earlier #309-312 delivery.

**Current Follow-up Status (2026-08-24, Issue #339):** Implementation and local verification are complete, pushed on `fix/relay-planning-departments-339`, and opened as PR #340. No staging or production deployment is part of the current authorization.

**Current UI Bugfix Status (2026-08-24):** The bounded department picker pagination and long-path overflow repair is complete, locally verified, pushed on `fix/relay-department-picker-ui`, and opened as PR #349. No release or deployment is authorized.

- [x] Add the optional Relay Group rename capability and sub2api admin HTTP adapter coverage.
- [x] Add department/Platform name suggestions, reviewed-name validation, relationship-stale protection, and pending-creation retry coverage.
- [x] Add per-Target rename selection, explicit Apply All, rename-only confirmation/results, and bilingual Vue interaction coverage.
- [x] Update the current Relay mapping contract and project architecture overview.
- [x] Run backend, frontend, and ae-cli full tests plus frontend production build and bundle measurement.
- [x] Push the feature branch and open a PR after operator approval.
- [x] Perform authenticated browser and staging verification after a reviewed build is deployed.

## Scope

- [x] Preview department x Platform candidates from the current Directory Sync snapshot and 30-day relay usage.
- [x] Allocate eligible source-group members by descending cost into the lowest-cost Group with stable tie breaks.
- [x] Show global Token rank, multi-department conflicts, over-target warnings, and missing migratable API-key warnings.
- [x] Execute explicit Group duplication/activation, subscription assignment, API-key binding, and source removal through `relay.Provider` optional capabilities.
- [x] Persist ID-based `relay_group_mappings` and expose list/rebind/replan/resize APIs.
- [x] Add admin UI using Element Plus and TailwindCSS with second confirmation before execution.
- [x] Publish the exact application commit to staging and complete live health/API/browser self-test.

## Verification

- Frontend: `npm test` (58 files / 782 tests) and `npm run build:measure` passed locally; measured Initial shell gzip was 72,763 bytes and default English `/usage` gzip was 159,265 bytes.
- Backend focused: `GOCACHE=/tmp/ai-efficiency-gocache go test ./internal/relayplanning` passed.
- Backend full: `GOCACHE=/tmp/ai-efficiency-gocache go test ./... -count=1` passed with local PostgreSQL/Redis access.
- Staging image: `ghcr.io/lichking-2234/ai-efficiency:staging-ae23738e8d69c09fcadb79c8929f9bbe2e459400`, multi-arch manifest `sha256:5183076aaebc77d275380d3c405d2095c66ffda1e915602fd827ccf1bc4200dc`.
- Staging Helm: context `luxuhui-agora-hci-losangeles3s`, release `ai-efficiency-staging`, namespace `la3-ai-efficiency-prod`, revision `135`; Phase A/B/C completed, Backend and prewarmer rolled out.
- Staging health: `/api/v1/health/live` and `/api/v1/health/ready` passed; ready reported database, Redis, and relay `up`, with commit `ae23738e8d69c09fcadb79c8929f9bbe2e459400`.
- Browser self-test: authenticated `/admin/relay-planning` loaded; preview returned a plan with one recommended Group, four eligible candidates, global token ranks, and proposed assignments. No execute action was submitted; `relay_group_mappings` remained empty.

## White-Screen Regression Follow-Up

- Reproduction: selecting `Department Alpha`, `openai`, `Group Alpha (#15)`, cost target `1`, and Group count `2500` returned empty assignment `user_ids` as JSON `null`; the UI then threw `Cannot read properties of null (reading 'length')` while rendering.
- Fix: initialize empty assignment member lists as `[]` in the backend and render assignment counts null-safely in the frontend. Regression coverage asserts the serialized empty-array contract.
- Verification: commit `455124750a88214614ef9f6f0a3dc00655fd9770` was deployed to staging as Helm revision `138`, image manifest `sha256:866ad8db13cad972bb0e01252194df5219ff8e1b5e076c74b6c1180610bcf26a`. The exact reproduction rendered all 2500 assignments with zero application console errors; no execute action was submitted.
- Follow-up fix: commit `5cbf2088` restores missing 30-day token values from the existing Relay `users-trend` batch source when the batch users-usage response contains cost-only fields, and bounds per-candidate Relay/Directory reads to eight workers while preserving candidate ordering. Focused relay-planning tests and the full backend suite passed.
- Current staging verification: local HEAD `551760256050bbd768cc2eaf231151003e16bf8a` was published as `ghcr.io/lichking-2234/ai-efficiency:staging-551760256050bbd768cc2eaf231151003e16bf8a`, multi-arch manifest `sha256:c7279a238fe960b569d66d28578b03ec27c65c9ea1fc188d02e2393320d7116c`, and deployed as Helm revision `139`. Both backend replicas and the prewarmer resolved to the amd64 digest `sha256:bc88f885b9d2505f766e5596caca3440c06d7cfb4faba373504ea534b37ae1b5`; live/ready health passed with database, Redis, and relay up. The authenticated preview reproduction rendered 2500 proposed groups, returned non-zero 30-day token values for all four candidates, emitted the expected source-group membership warnings, and produced no browser console errors. No execute action was submitted.

## 2026-08-19 Allocation and confirmation follow-up

- [x] Cap the automatic recommendation at the eligible member count and prevent an initial preview request from overriding it with an arbitrary Group count.
- [x] Keep explicit Group-count resize available only when replanning an existing mapping.
- [x] Return expected sub2api copy names in preview assignments and replace them with relay-returned names and IDs after execution.
- [x] Add persistent planning-input labels and replace the broken global confirmation message box with a centered, body-appended Element Plus dialog.
- [x] Add focused backend and frontend regression tests.
- [x] Run the complete frontend suite and production build.
- [x] Run the complete backend suite.
- [x] Publish an immutable staging image and deploy only the staging Helm release.
- [x] Verify the affected preview and centered confirmation dialog in an authenticated browser without submitting execution.

Staging revision 140 runs commit `e7f165f418177b9ebb9068cd54a983c16f2bca72` from `ghcr.io/lichking-2234/ai-efficiency:staging-e7f165f418177b9ebb9068cd54a983c16f2bca72`, manifest `sha256:ba705c2f2659ae4ecdb6ac2662262a279fa57c9ff89595a9260e498fd0bd5bf8`. Backend 2/2 and prewarmer 1/1 were ready, live/ready reported database, Redis, and relay up, and the one backend restart caused by concurrent Ent migration remained stable at one. The authenticated `Group Beta (#2)` / `$2500` preview produced four eligible members, four expected copy names, non-zero 30-day tokens, and no application console errors. The confirmation dialog was fully visible at the default desktop viewport and 390 x 844 mobile viewport; it was canceled without executing migration.

## 2026-08-19 Preview latency follow-up

- [x] Capture a repeatable staging baseline: browser completion `22,871 ms`, backend request `22,161 ms`, with a `5,000 ms` regression threshold.
- [x] Identify the serial critical path: provider-wide 30-day trend took `14,126 ms`, then per-candidate Relay facts took about `7.4 s`.
- [x] Reuse the existing read-only schema-v3 Team Usage prewarm generation for exact 30-day provider usage, with the existing exact Relay path as fail-open fallback.
- [x] Read candidate subscription membership and API Keys concurrently while retaining the generic allowed-group fallback for other Relay providers.
- [x] Add focused cache-hit, cache-miss, prewarm projection, and deterministic concurrency regressions.
- [x] Run `go test ./... -count=1`, focused `go test -race`, `go vet ./...`, and `go build ./...`.
- [x] Publish the latency fix as a new immutable staging image and deploy only `ai-efficiency-staging`.
- [x] Re-run the authenticated `$2500` preview and confirmation timing check below the `5,000 ms` threshold without submitting execution.

Staging revision 143 runs commit `8a6b060f1df072c304d87947595f1b03e1a6cdbe` from `ghcr.io/lichking-2234/ai-efficiency:staging-8a6b060f1df072c304d87947595f1b03e1a6cdbe`, multi-arch manifest `sha256:da49031f57660ec5c1a8c8d8b5f94522ebde46abc044c594e65514f84b118a8f`. Backend 2/2 and prewarmer 1/1 resolved to that digest; staging live/ready and production ready remained healthy. Immediately after the database restore, the first authenticated `Group Beta (#2)` / `$2500` preview recorded a prewarm miss and completed in `17,745 ms` while the UTC 30-day lane was not yet published. After the worker published a valid four-timezone generation, the same preview completed in `2,007 ms` in Chrome and `1,294 ms` in the backend with a prewarm full hit. Refreshing the plan and opening the confirmation dialog completed in `1,311 ms` in Chrome and `981 ms` in the backend. The dialog was canceled, no migration was submitted, and no managed mapping was created.

## 2026-08-19 Stable mapping contract follow-up

- [x] Separate Template Group and Migration Source Group in Preview, persistence, API, and UI.
- [x] Persist editable member assignment/source maps and execute only the final Confirm matrix.
- [x] Keep non-source additions as target-only bindings and retain source subscription/API Key migration for source members.
- [x] Make Replan mapping maintenance only: preserve target IDs, recommend source members within per-target remaining capacity, and remove resize/deactivation/automatic source restoration.
- [x] Add target assignment editing, explicit unassigned display, bilingual labels, and mapping availability/relationship/conflict warnings.
- [x] Add same-Platform Rebind validation, invalid-department suggestions, Confirm protection, retryable operation state, unmanaged-member capacity, and explicit adoption.
- [x] Run focused backend/frontend tests and frontend build.
- [x] Run the complete frontend suite and final code review.
- [x] Run the complete backend suite after the final mapping patch; the exact-head hosted clean checkout passed `go test ./...` after an earlier dirty local checkout was blocked by an unrelated `personalusage` compile error.

The maintenance fixes were not deployed during implementation. Their later explicitly authorized final-PR-head staging publication is recorded below; no production mutation is part of either step.

## 2026-08-20 Review bundle-budget follow-up

- [x] Restore the Initial shell and default English `/usage` bundle ceilings instead of weakening the UI contract.
- [x] Keep Relay Planning and Directory Offboarding messages route-local while preserving bilingual behavior and route names.
- [x] Run the complete frontend suite, measured production build, focused Relay Planning backend test, and final diff check.
- [x] Publish an immutable multi-architecture staging image for the exact application commit.
- [x] Advance only the `ai-efficiency-staging` Helm release through the existing staging rollout procedure.
- [x] Verify staging live/ready health, image identity, build commit, and the affected admin routes without executing a Relay migration.

Staging revision 154 runs commit `1ebf4bd1eaf1b6c57da99db121f2e2a9defb87ce` from `ghcr.io/lichking-2234/ai-efficiency:staging-1ebf4bd1eaf1b6c57da99db121f2e2a9defb87ce`, multi-architecture manifest `sha256:2804874a3e33f348534d6310acde6afed1b9aff4e9cda6bb96f043ecb7a3d949`. The three-phase rollout completed as revisions 152, 153, and 154; the restore Job succeeded, Backend reached 2/2 Ready, and prewarmer reached 1/1 Ready with zero restarts. Live and ready health reported the exact build commit with database, Redis, and relay up. Authenticated browser checks loaded Relay Planning and Directory Offboarding in Chinese with no console warnings or errors; no preview, confirmation, Relay migration, or offboarding action was submitted. Production remained revision 87 on `v0.1.0-preview.87`.

## 2026-08-20 Hosted Node 20 bundle follow-up

- [x] Reproduce the hosted frontend failure in an isolated Linux/Node 20 build environment.
- [x] Keep the documented bundle ceilings and resolve production builds to the official Vue Router browser production entry while retaining the diagnostic entry for development and tests.
- [x] Run the complete frontend suite and both local and Linux/Node 20 measured builds.
- [x] Push the exact fix and obtain passing PR checks.
- [x] Publish and deploy the final application commit to staging through Phase A/B/C.
- [x] Re-verify image identity, health, production isolation, and the two affected Chinese admin routes without submitting mutations.

The first hosted run passed all 782 frontend tests but measured the Initial shell at `72,912` bytes on Linux/Node 20, exceeding the unchanged `72,800` ceiling. The same environment reproduced that exact value locally. After selecting the official Vue Router production entry for builds, the complete 58-file / 782-test suite passed; the local measured build reported Initial shell `72,592` and default English `/usage` `159,090`, while the isolated Linux/Node 20 build reported `72,759` and `159,413`. Both builds passed all structural assertions without changing a bundle ceiling. PR head `1fb77738da64df0d95f0f5b034ccaebf0661109b` then passed `frontend`, `backend`, `ae-cli`, and `deploy-static` checks.

Final staging revision 157 runs commit `1fb77738da64df0d95f0f5b034ccaebf0661109b` from `ghcr.io/lichking-2234/ai-efficiency:staging-1fb77738da64df0d95f0f5b034ccaebf0661109b`, multi-architecture manifest `sha256:226c915523cd7bdada6e2eb2fe8ae5a9ff0ce6ee4f2f74c6edee633518730e32`. Revisions 155, 156, and 157 completed Phase A/B/C; the restore Job succeeded, Backend reached 2/2 Ready, and prewarmer reached 1/1 Ready with zero restarts. All three application Pods resolved to the published manifest. Live and ready health reported the exact commit with database, Redis, and relay up. Authenticated browser checks rendered Relay Planning and Directory Offboarding in Chinese with zero console warnings or errors; no preview, confirmation, migration, or offboarding action was submitted. Production remained revision 87 on `v0.1.0-preview.87`.

Production deployment is not authorized by this follow-up.

## 2026-08-20 Search and Preview Account-assignment bug follow-up

- [x] Reproduce and cover user search issuing one request per input event.
- [x] Debounce user and Account text search for 300 ms and ignore stale responses while keeping pagination immediate.
- [x] Expose same-Platform Account search, add, remove, and ordering directly on every Preview Target.
- [x] Carry the reviewed Account order through Preview fingerprinting, Confirm summaries, Relay reconciliation, and mapping persistence.
- [x] Preserve Template Account defaults, explicit empty Account pools, retry behavior, and existing non-contiguous priorities.
- [x] Preserve duplicate existing Relay Account priorities found during authenticated staging Preview validation.
- [x] Update the current Relay Group mapping contract and use explicit bilingual Account-assignment labels.
- [x] Run 16 focused Relay Planning frontend tests, the complete 58-file / 798-test frontend suite, focused handler regressions, `go test ./internal/relayplanning -count=1`, focused race checks, focused vet, the complete backend suite, measured production build, and diff checks.
- [x] Commit and push the exact fix to PR #318 and obtain passing exact-head checks.
- [x] Publish the exact PR head as a new immutable staging image and advance only `ai-efficiency-staging`.
- [x] Verify the Chinese Preview Account controls and debounced user search in an authenticated browser without submitting the final execution.

The final measured production build reported Initial shell at 72,638 gzip bytes and default English `/usage` at 158,925 gzip bytes; structural assertions passed. The focused race run emitted the known macOS `LC_DYSYMTAB` linker warning and exited successfully.

Staging revision 161 runs commit `d06495845ec9e4db9b817982099ed1e386b80130` from `ghcr.io/lichking-2234/ai-efficiency:staging-d06495845ec9e4db9b817982099ed1e386b80130`, OCI index `sha256:f9891bed19c1488dff8572a7f0c031e98ccfd34d7e27aa2ad355f19097524d07`. Both backend replicas and the prewarmer became Ready with zero restarts and the database was preserved. Authenticated Preview testing then exposed the editable-target-count follow-up below before the original browser checklist could be closed.

## 2026-08-20 Editable suggested Group follow-up

- [x] Reproduce the reported `422` when two selected users are submitted with four reviewed target assignments.
- [x] Add HTTP and rendered-page regression coverage for administrator-edited target counts.
- [x] Accept the initial Preview assignment list as authoritative while keeping existing-mapping Replan target IDs fixed.
- [x] Add initial-Preview controls to add and delete suggested Groups with continuous indexes and Template Account defaults.
- [x] Run focused and complete backend/frontend verification.
- [x] Push the exact fix, obtain passing PR checks, publish staging, and complete authenticated browser verification without executing Confirm.

The editable-target-count follow-up passed the focused 26-test frontend run, the complete 58-file / 799-test frontend suite, the reported-payload HTTP regression, focused race checks, focused vet, `go test ./... -count=1`, and `git diff --check`. The measured production build reported Initial shell at 72,632 gzip bytes, default English `/usage` at 158,944 gzip bytes, and default English `/admin/users` at 247,838 gzip bytes; structural assertions passed. The focused race run emitted the known macOS `LC_DYSYMTAB` linker warning and exited successfully.

PR head `e1c3811b79139609bbec0eceb9f1dedeb843758a` passed all four checks in CI run `32383188454` and was published as `ghcr.io/lichking-2234/ai-efficiency:staging-e1c3811b79139609bbec0eceb9f1dedeb843758a`, OCI index `sha256:d0449e718828f178b14e5fe2519f33a06abf87fe2e7b52286b490069cd8806bd`. Staging revision 162 preserved the existing database; both Backend replicas and the prewarmer became Ready on that digest with zero restarts, and live/ready reported the exact commit with database, Redis, and Relay up. Authenticated Chinese verification submitted two selected members with four reviewed assignments, opened the four-Target confirmation without a `422`, added a fifth proposed Group, deleted it, and deleted a member-bearing Group; the removed member returned to Unassigned and indexes remained continuous. User search returned an arbitrary local user, and the page emitted zero application warnings or errors. The final `Create Groups and Migrate` action was not submitted, no mapping was created, and production remained revision 88 on `v0.1.0-preview.88`.

## 2026-08-21 Mapping read performance and Rebind dialog follow-up

- [x] Reproduce the Rebind layout failure and trace mapping-list/Replan Relay fan-out.
- [x] Add red HTTP regressions for batch subscription-directory reads and request-local same-platform Account reuse.
- [x] Replace the chained global Rebind prompts with one centered, name-aware Element Plus form dialog and a single final submission.
- [x] Make sub2api request active subscription facts explicitly and use the batch result for mapping drift and unmanaged-member detection.
- [x] Run focused and complete backend/frontend tests, focused race/vet, measured production build, and final diff checks.

Staging was inspected before implementation and remained on revision 162. The
authenticated route available to the browser contained no saved mapping, so no
live Rebind mutation was attempted. No staging or production rollout is claimed
for this local follow-up.

The follow-up passed the three new HTTP/provider regressions, 17 focused Relay
Planning frontend tests, `go test ./... -count=1`, all 58 frontend files / 799
tests, focused race checks, focused `go vet`, and `git diff --check`. The measured
production build passed structural assertions with Initial shell at 72,638 gzip
bytes, default English `/usage` at 158,799 gzip bytes, and default English
`/admin/users` at 247,685 gzip bytes. The focused race run emitted the known
macOS `LC_DYSYMTAB` linker warning and exited successfully.

## 2026-08-21 Mapping-read and Rebind staging publication

- [x] Merge the current `main`, resolve the architecture/i18n conflicts, and obtain passing exact-head PR checks.
- [x] Publish the exact application commit as an immutable amd64/arm64 staging image.
- [x] Advance only `ai-efficiency-staging` with `postgres.restore.enabled=false` and preserve the existing database PVC.
- [x] Verify Helm status, Pod image identity and restart counts, live/ready health, and production isolation.
- [x] Load the authenticated Chinese Relay Planning route repeatedly and confirm a non-empty UI with no console warnings or errors.
- [x] Exercise a live production mapping Replan and open its Rebind dialog; the dialog was canceled without submitting a rebind or any Relay mutation.

Staging revision 163 runs application commit
`945fc56a7e0b600818fa0d7f5c3fee7410658cc8` from
`ghcr.io/lichking-2234/ai-efficiency:staging-945fc56a7e0b600818fa0d7f5c3fee7410658cc8`,
OCI index `sha256:2c2e18737a1a7570f6ac5e4f43f8cb90b2e6d97a364d30807331bbf6008e4ff1`.
Both backend replicas and the prewarmer became Ready on that digest with zero
restarts. The database PVC retained UID
`0deae8cd-c2e2-4887-b4d7-62895bda2d10`; live/ready reported the exact commit
with database, Redis, and Relay up. Three authenticated full-route reloads,
including options and mapping-list loading through the rendered empty state,
completed in 479 ms, 546 ms, and 491 ms with zero console warnings or errors.
No mapping, Replan, Rebind submission, or Relay migration was executed.

## 2026-08-21 Production publication and mapping synchronization

- [x] Publish `v0.1.0-preview.91` from the exact merged `main` commit and complete the hosted frontend, backend, release-embedding, deploy-contract, binary, and multi-architecture image jobs.
- [x] Verify the GitHub Release, repository latest-release metadata, and the amd64/arm64 GHCR manifest before changing production.
- [x] Validate that staging and production provider identity, referenced local-user identity, current directory department, and live Relay Group/Account relationships agree.
- [x] Synchronize only `relay_group_mappings` from staging to production in one transaction, retaining the pre-sync production baseline that the table did not exist.
- [x] Advance `ai-efficiency-prod` with an atomic Helm upgrade and verify runtime identity, health dependencies, restart counts, mapping hash, Replan, and the Rebind dialog.

Release workflow `32463272042` completed successfully. The final image is
`ghcr.io/lichking-2234/ai-efficiency:v0.1.0-preview.91`, OCI index
`sha256:65519779d1764b415c485891f029d30b8b7ce96bf046ebce729e306b59d09042`.
Production Helm revision 91 runs chart `0.1.77`; the Backend and prewarmer are
Ready with zero restarts, and live/ready report the exact application commit
with database, Redis, and Relay up.

The data synchronization copied no users, departments, subscriptions, API
Keys, or other business tables. Before import, the production mapping table did
not exist. After import and rollout, staging and production each contained one
mapping with three managed Target Groups and initialized Account management;
their canonical row hash was identical. The authenticated production page read
the mapping, Replan resolved all three active schedulable Account pools without
unavailable-relationship, Account-drift, or cross-mapping warnings, and the
Rebind dialog opened and was canceled without submission.

## 2026-08-20 Maintenance review follow-up

- [x] Add regression coverage and minimal fixes for relationship identity validation, retryable explicit removals, per-target Account reconciliation, paginated user search, the shared wide-content breakpoint, and Rebind submission locking.
- [x] Run focused Relay Planning handler, service, provider, and frontend view tests.
- [x] Run complete backend and frontend suites, race checks, vet, measured production build, and final diff checks.
- [x] Preserve `Move Here` intent when a failed transfer is reopened or retried.
- [x] Persist destination/source mapping changes atomically and report mapping persistence steps.
- [x] Include retryable explicit-removal effects in the confirmation summary.
- [x] Return safe relationship difference categories for stale fingerprints.
- [x] Warn about reviewed multi-Account and reused-Account target pools without exposing private configuration.
- [x] Register the current mapping contract in the repository Source-of-Truth navigation.
- [x] Skip completed subscription steps during retry, warn when `Add Additionally` retains multiple managed Account pools, and categorize identity-invalid stale plans.
- [x] Bind completed retry steps to their saved Target and preserve reason-specific stale categories.
- [x] Repeat Standards and Spec review against `origin/main...HEAD` and resolve any material findings.
- [x] Commit and push the exact reviewed fixes to PR #318.

Local verification passed with `go test ./... -count=1`, 58 frontend files / 795 tests, focused race checks for the changed retry and relationship paths, focused `go vet`, and `npm run build:measure`. The measured Initial shell was 72,641 gzip bytes and default English `/usage` was 159,271 gzip bytes; structural assertions passed. The race run emitted the known macOS `LC_DYSYMTAB` linker warning and still exited successfully.

No staging image, Helm rollout, or production mutation is part of this follow-up.

## 2026-08-20 Post-main-merge hosted bundle correction

- [x] Confirm the exact hosted failure at PR head `0f9cf5a1`: frontend tests passed, but Linux/Node `20.20.2` measured default English `/usage` at `159,587 / 159,500` gzip bytes.
- [x] Reduce the existing `/usage` closure without raising the documented bundle ceiling or removing Relay Planning navigation.
- [x] Run focused and complete frontend tests plus local and isolated Linux/Node 20 measured builds.
- [x] Commit and push the correction, then obtain passing PR checks.

Backend, ae-cli, and deploy-static passed in run `32365847823`; only the frontend measured build failed. The correction was not deployed during implementation; its later explicitly authorized final-PR-head staging publication is recorded below. No production deployment is part of this correction.

The correction reuses already-retained `Switch`, `Check`, `Plus`, `Refresh`, and `Setting` icons, keeps `Delete` for destructive removal, and uses the smaller official `CaretTop` / `CaretBottom` icons for ordered Account movement. Focused Sidebar and Relay Planning tests passed 37/37, the complete frontend suite passed 58 files / 795 tests, and `git diff --check` passed. The local measured build reported Initial shell `72,638` and default English `/usage` `158,947`; the isolated Linux/Node `20.20.2` build reported `72,799` and `159,267`. Both builds passed structural assertions with the documented ceilings unchanged.

PR run `32368798510` confirmed the same hosted Node 20 measurements at implementation commit `a700ebc3`; frontend, backend, ae-cli, and deploy-static all passed. PR #318 was `MERGEABLE` with `CLEAN` merge state after that run.

## 2026-08-20 Final PR-head staging publication

- [x] Publish the exact PR head as an immutable multi-architecture staging image.
- [x] Advance only `ai-efficiency-staging` while retaining the existing database with `postgres.restore.enabled=false`.
- [x] Verify Helm status, application Pod image identity, restart counts, live/ready health, and production isolation.
- [x] Load the authenticated Relay Planning route and confirm a non-empty Chinese UI with no console warnings or errors, without submitting Preview, Confirm, or any Relay mutation.
- [x] Persist the exact staging image selector to Helm `main` without changing any other value.

Staging revision 159 runs PR head `91df48d1faada7c11b4611993e070fa4683898d8` from `ghcr.io/lichking-2234/ai-efficiency:staging-91df48d1faada7c11b4611993e070fa4683898d8`. The OCI index is `sha256:8b80734f22ee9aada399cbd3e7e3c538bfae0653999871978fff462dff23941f`, with `linux/amd64` manifest `sha256:4dbf9004ea6fa260bd0dbfce740bc1465ef8568f47186bed251a585dba38e99d` and `linux/arm64` manifest `sha256:0675bf1f826d54f77fefd27d0b5bb96bfb90429b7d73984ab94127dbae257aa7`. Both backend replicas and the prewarmer are Ready on the amd64 digest with zero restarts. Live and ready health report the exact commit with database, Redis, and relay up. The authenticated `/admin/relay-planning` route rendered the Chinese `部门分组分配` page with a non-empty app root, no loading mask, and zero console warnings or errors; no Preview, Confirm, or Relay mutation was submitted. Production remains revision 88 on `v0.1.0-preview.88` at commit `740eb1d906e498558decf357b41e5756303b412f` and is ready. Helm commit `5999051` records only the staging image tag change on `main`.

## 2026-08-24 Complete department selection follow-up

- [x] Reuse the bounded department picker for initial planning and mapping Rebind while keeping department selection required.
- [x] Remove Relay Planning's eager first-page department load without changing the administrator API page-size contract or the Preview/Confirm mutation boundary.
- [x] Add focused coverage for a department beyond the first 100 options, debounced query filtering, stable selected-path restoration, and both Relay Planning entry points.
- [x] Run the focused department-picker and Relay Planning view tests.
- [x] Run the complete frontend suite, measured build, and final formatting checks.
- [x] Commit and push the exact change and open PR #340 to close Issue #339.

Focused picker and Relay Planning tests passed 42/42. The complete frontend
suite passed 58 files / 800 tests. `npm run build:measure` passed type checking,
structural assertions, and bundle ceilings with Initial shell at 72,560 gzip
bytes and default English `/usage` at 158,160 gzip bytes. `git diff --check`
also passed.

## 2026-08-24 Department picker pagination and overflow repair

- [x] Add a red-capable component test for preserving visible options while a paginated request is pending.
- [x] Preserve the current option page until the next page succeeds without changing search, pagination, or selection state.
- [x] Keep long selected department paths within the picker and Relay Planning grid column.
- [x] Run focused tests, the complete frontend suite, measured build, and browser viewport verification.

Focused picker and Relay Planning tests passed 44/44. The complete frontend
suite passed 58 files / 808 tests. `npm run build:measure` passed type checking,
structural assertions, and bundle ceilings with Initial shell at 72,598 gzip
bytes and default English `/usage` at 158,256 gzip bytes. Browser verification
confirmed the pending and settled page states plus long-path truncation without
horizontal overflow at 390, 768, 1024, 1280, and 1440 pixels. The browser
console had no warnings or errors, and `git diff --check` passed.
Implementation commit `0898fc88` is published in PR #349 against `main`.
