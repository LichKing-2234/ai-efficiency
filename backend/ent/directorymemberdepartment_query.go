// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/directorymemberdepartment"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// DirectoryMemberDepartmentQuery is the builder for querying DirectoryMemberDepartment entities.
type DirectoryMemberDepartmentQuery struct {
	config
	ctx        *QueryContext
	order      []directorymemberdepartment.OrderOption
	inters     []Interceptor
	predicates []predicate.DirectoryMemberDepartment
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DirectoryMemberDepartmentQuery builder.
func (dmdq *DirectoryMemberDepartmentQuery) Where(ps ...predicate.DirectoryMemberDepartment) *DirectoryMemberDepartmentQuery {
	dmdq.predicates = append(dmdq.predicates, ps...)
	return dmdq
}

// Limit the number of records to be returned by this query.
func (dmdq *DirectoryMemberDepartmentQuery) Limit(limit int) *DirectoryMemberDepartmentQuery {
	dmdq.ctx.Limit = &limit
	return dmdq
}

// Offset to start from.
func (dmdq *DirectoryMemberDepartmentQuery) Offset(offset int) *DirectoryMemberDepartmentQuery {
	dmdq.ctx.Offset = &offset
	return dmdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dmdq *DirectoryMemberDepartmentQuery) Unique(unique bool) *DirectoryMemberDepartmentQuery {
	dmdq.ctx.Unique = &unique
	return dmdq
}

// Order specifies how the records should be ordered.
func (dmdq *DirectoryMemberDepartmentQuery) Order(o ...directorymemberdepartment.OrderOption) *DirectoryMemberDepartmentQuery {
	dmdq.order = append(dmdq.order, o...)
	return dmdq
}

// First returns the first DirectoryMemberDepartment entity from the query.
// Returns a *NotFoundError when no DirectoryMemberDepartment was found.
func (dmdq *DirectoryMemberDepartmentQuery) First(ctx context.Context) (*DirectoryMemberDepartment, error) {
	nodes, err := dmdq.Limit(1).All(setContextOp(ctx, dmdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{directorymemberdepartment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) FirstX(ctx context.Context) *DirectoryMemberDepartment {
	node, err := dmdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DirectoryMemberDepartment ID from the query.
// Returns a *NotFoundError when no DirectoryMemberDepartment ID was found.
func (dmdq *DirectoryMemberDepartmentQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dmdq.Limit(1).IDs(setContextOp(ctx, dmdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{directorymemberdepartment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) FirstIDX(ctx context.Context) int {
	id, err := dmdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DirectoryMemberDepartment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DirectoryMemberDepartment entity is found.
// Returns a *NotFoundError when no DirectoryMemberDepartment entities are found.
func (dmdq *DirectoryMemberDepartmentQuery) Only(ctx context.Context) (*DirectoryMemberDepartment, error) {
	nodes, err := dmdq.Limit(2).All(setContextOp(ctx, dmdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{directorymemberdepartment.Label}
	default:
		return nil, &NotSingularError{directorymemberdepartment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) OnlyX(ctx context.Context) *DirectoryMemberDepartment {
	node, err := dmdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DirectoryMemberDepartment ID in the query.
// Returns a *NotSingularError when more than one DirectoryMemberDepartment ID is found.
// Returns a *NotFoundError when no entities are found.
func (dmdq *DirectoryMemberDepartmentQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dmdq.Limit(2).IDs(setContextOp(ctx, dmdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{directorymemberdepartment.Label}
	default:
		err = &NotSingularError{directorymemberdepartment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) OnlyIDX(ctx context.Context) int {
	id, err := dmdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DirectoryMemberDepartments.
func (dmdq *DirectoryMemberDepartmentQuery) All(ctx context.Context) ([]*DirectoryMemberDepartment, error) {
	ctx = setContextOp(ctx, dmdq.ctx, ent.OpQueryAll)
	if err := dmdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DirectoryMemberDepartment, *DirectoryMemberDepartmentQuery]()
	return withInterceptors[[]*DirectoryMemberDepartment](ctx, dmdq, qr, dmdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) AllX(ctx context.Context) []*DirectoryMemberDepartment {
	nodes, err := dmdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DirectoryMemberDepartment IDs.
func (dmdq *DirectoryMemberDepartmentQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dmdq.ctx.Unique == nil && dmdq.path != nil {
		dmdq.Unique(true)
	}
	ctx = setContextOp(ctx, dmdq.ctx, ent.OpQueryIDs)
	if err = dmdq.Select(directorymemberdepartment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) IDsX(ctx context.Context) []int {
	ids, err := dmdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dmdq *DirectoryMemberDepartmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dmdq.ctx, ent.OpQueryCount)
	if err := dmdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dmdq, querierCount[*DirectoryMemberDepartmentQuery](), dmdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) CountX(ctx context.Context) int {
	count, err := dmdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dmdq *DirectoryMemberDepartmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dmdq.ctx, ent.OpQueryExist)
	switch _, err := dmdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dmdq *DirectoryMemberDepartmentQuery) ExistX(ctx context.Context) bool {
	exist, err := dmdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DirectoryMemberDepartmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dmdq *DirectoryMemberDepartmentQuery) Clone() *DirectoryMemberDepartmentQuery {
	if dmdq == nil {
		return nil
	}
	return &DirectoryMemberDepartmentQuery{
		config:     dmdq.config,
		ctx:        dmdq.ctx.Clone(),
		order:      append([]directorymemberdepartment.OrderOption{}, dmdq.order...),
		inters:     append([]Interceptor{}, dmdq.inters...),
		predicates: append([]predicate.DirectoryMemberDepartment{}, dmdq.predicates...),
		// clone intermediate query.
		sql:  dmdq.sql.Clone(),
		path: dmdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DirectoryMemberDepartment.Query().
//		GroupBy(directorymemberdepartment.FieldSourceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dmdq *DirectoryMemberDepartmentQuery) GroupBy(field string, fields ...string) *DirectoryMemberDepartmentGroupBy {
	dmdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DirectoryMemberDepartmentGroupBy{build: dmdq}
	grbuild.flds = &dmdq.ctx.Fields
	grbuild.label = directorymemberdepartment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//	}
//
//	client.DirectoryMemberDepartment.Query().
//		Select(directorymemberdepartment.FieldSourceID).
//		Scan(ctx, &v)
func (dmdq *DirectoryMemberDepartmentQuery) Select(fields ...string) *DirectoryMemberDepartmentSelect {
	dmdq.ctx.Fields = append(dmdq.ctx.Fields, fields...)
	sbuild := &DirectoryMemberDepartmentSelect{DirectoryMemberDepartmentQuery: dmdq}
	sbuild.label = directorymemberdepartment.Label
	sbuild.flds, sbuild.scan = &dmdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DirectoryMemberDepartmentSelect configured with the given aggregations.
func (dmdq *DirectoryMemberDepartmentQuery) Aggregate(fns ...AggregateFunc) *DirectoryMemberDepartmentSelect {
	return dmdq.Select().Aggregate(fns...)
}

func (dmdq *DirectoryMemberDepartmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dmdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dmdq); err != nil {
				return err
			}
		}
	}
	for _, f := range dmdq.ctx.Fields {
		if !directorymemberdepartment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dmdq.path != nil {
		prev, err := dmdq.path(ctx)
		if err != nil {
			return err
		}
		dmdq.sql = prev
	}
	return nil
}

func (dmdq *DirectoryMemberDepartmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DirectoryMemberDepartment, error) {
	var (
		nodes = []*DirectoryMemberDepartment{}
		_spec = dmdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DirectoryMemberDepartment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DirectoryMemberDepartment{config: dmdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dmdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dmdq *DirectoryMemberDepartmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dmdq.querySpec()
	_spec.Node.Columns = dmdq.ctx.Fields
	if len(dmdq.ctx.Fields) > 0 {
		_spec.Unique = dmdq.ctx.Unique != nil && *dmdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dmdq.driver, _spec)
}

func (dmdq *DirectoryMemberDepartmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(directorymemberdepartment.Table, directorymemberdepartment.Columns, sqlgraph.NewFieldSpec(directorymemberdepartment.FieldID, field.TypeInt))
	_spec.From = dmdq.sql
	if unique := dmdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dmdq.path != nil {
		_spec.Unique = true
	}
	if fields := dmdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, directorymemberdepartment.FieldID)
		for i := range fields {
			if fields[i] != directorymemberdepartment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dmdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dmdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dmdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dmdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dmdq *DirectoryMemberDepartmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dmdq.driver.Dialect())
	t1 := builder.Table(directorymemberdepartment.Table)
	columns := dmdq.ctx.Fields
	if len(columns) == 0 {
		columns = directorymemberdepartment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dmdq.sql != nil {
		selector = dmdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dmdq.ctx.Unique != nil && *dmdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dmdq.predicates {
		p(selector)
	}
	for _, p := range dmdq.order {
		p(selector)
	}
	if offset := dmdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dmdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DirectoryMemberDepartmentGroupBy is the group-by builder for DirectoryMemberDepartment entities.
type DirectoryMemberDepartmentGroupBy struct {
	selector
	build *DirectoryMemberDepartmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dmdgb *DirectoryMemberDepartmentGroupBy) Aggregate(fns ...AggregateFunc) *DirectoryMemberDepartmentGroupBy {
	dmdgb.fns = append(dmdgb.fns, fns...)
	return dmdgb
}

// Scan applies the selector query and scans the result into the given value.
func (dmdgb *DirectoryMemberDepartmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dmdgb.build.ctx, ent.OpQueryGroupBy)
	if err := dmdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectoryMemberDepartmentQuery, *DirectoryMemberDepartmentGroupBy](ctx, dmdgb.build, dmdgb, dmdgb.build.inters, v)
}

func (dmdgb *DirectoryMemberDepartmentGroupBy) sqlScan(ctx context.Context, root *DirectoryMemberDepartmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dmdgb.fns))
	for _, fn := range dmdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dmdgb.flds)+len(dmdgb.fns))
		for _, f := range *dmdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dmdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dmdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DirectoryMemberDepartmentSelect is the builder for selecting fields of DirectoryMemberDepartment entities.
type DirectoryMemberDepartmentSelect struct {
	*DirectoryMemberDepartmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dmds *DirectoryMemberDepartmentSelect) Aggregate(fns ...AggregateFunc) *DirectoryMemberDepartmentSelect {
	dmds.fns = append(dmds.fns, fns...)
	return dmds
}

// Scan applies the selector query and scans the result into the given value.
func (dmds *DirectoryMemberDepartmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dmds.ctx, ent.OpQuerySelect)
	if err := dmds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectoryMemberDepartmentQuery, *DirectoryMemberDepartmentSelect](ctx, dmds.DirectoryMemberDepartmentQuery, dmds, dmds.inters, v)
}

func (dmds *DirectoryMemberDepartmentSelect) sqlScan(ctx context.Context, root *DirectoryMemberDepartmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dmds.fns))
	for _, fn := range dmds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dmds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dmds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
