// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ai-efficiency/backend/ent/session"
	"github.com/ai-efficiency/backend/ent/sessionworkspace"
	"github.com/google/uuid"
)

// SessionWorkspace is the model entity for the SessionWorkspace schema.
type SessionWorkspace struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID uuid.UUID `json:"session_id,omitempty"`
	// WorkspaceID holds the value of the "workspace_id" field.
	WorkspaceID string `json:"workspace_id,omitempty"`
	// WorkspaceRoot holds the value of the "workspace_root" field.
	WorkspaceRoot string `json:"workspace_root,omitempty"`
	// GitDir holds the value of the "git_dir" field.
	GitDir string `json:"git_dir,omitempty"`
	// GitCommonDir holds the value of the "git_common_dir" field.
	GitCommonDir string `json:"git_common_dir,omitempty"`
	// FirstSeenAt holds the value of the "first_seen_at" field.
	FirstSeenAt time.Time `json:"first_seen_at,omitempty"`
	// LastSeenAt holds the value of the "last_seen_at" field.
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// BindingSource holds the value of the "binding_source" field.
	BindingSource sessionworkspace.BindingSource `json:"binding_source,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SessionWorkspaceQuery when eager-loading is set.
	Edges        SessionWorkspaceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SessionWorkspaceEdges holds the relations/edges for other nodes in the graph.
type SessionWorkspaceEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionWorkspaceEdges) SessionOrErr() (*Session, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: session.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionWorkspace) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessionworkspace.FieldID:
			values[i] = new(sql.NullInt64)
		case sessionworkspace.FieldWorkspaceID, sessionworkspace.FieldWorkspaceRoot, sessionworkspace.FieldGitDir, sessionworkspace.FieldGitCommonDir, sessionworkspace.FieldBindingSource:
			values[i] = new(sql.NullString)
		case sessionworkspace.FieldFirstSeenAt, sessionworkspace.FieldLastSeenAt:
			values[i] = new(sql.NullTime)
		case sessionworkspace.FieldSessionID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionWorkspace fields.
func (sw *SessionWorkspace) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessionworkspace.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sw.ID = int(value.Int64)
		case sessionworkspace.FieldSessionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value != nil {
				sw.SessionID = *value
			}
		case sessionworkspace.FieldWorkspaceID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field workspace_id", values[i])
			} else if value.Valid {
				sw.WorkspaceID = value.String
			}
		case sessionworkspace.FieldWorkspaceRoot:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field workspace_root", values[i])
			} else if value.Valid {
				sw.WorkspaceRoot = value.String
			}
		case sessionworkspace.FieldGitDir:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field git_dir", values[i])
			} else if value.Valid {
				sw.GitDir = value.String
			}
		case sessionworkspace.FieldGitCommonDir:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field git_common_dir", values[i])
			} else if value.Valid {
				sw.GitCommonDir = value.String
			}
		case sessionworkspace.FieldFirstSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field first_seen_at", values[i])
			} else if value.Valid {
				sw.FirstSeenAt = value.Time
			}
		case sessionworkspace.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				sw.LastSeenAt = value.Time
			}
		case sessionworkspace.FieldBindingSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field binding_source", values[i])
			} else if value.Valid {
				sw.BindingSource = sessionworkspace.BindingSource(value.String)
			}
		default:
			sw.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionWorkspace.
// This includes values selected through modifiers, order, etc.
func (sw *SessionWorkspace) Value(name string) (ent.Value, error) {
	return sw.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the SessionWorkspace entity.
func (sw *SessionWorkspace) QuerySession() *SessionQuery {
	return NewSessionWorkspaceClient(sw.config).QuerySession(sw)
}

// Update returns a builder for updating this SessionWorkspace.
// Note that you need to call SessionWorkspace.Unwrap() before calling this method if this SessionWorkspace
// was returned from a transaction, and the transaction was committed or rolled back.
func (sw *SessionWorkspace) Update() *SessionWorkspaceUpdateOne {
	return NewSessionWorkspaceClient(sw.config).UpdateOne(sw)
}

// Unwrap unwraps the SessionWorkspace entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sw *SessionWorkspace) Unwrap() *SessionWorkspace {
	_tx, ok := sw.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionWorkspace is not a transactional entity")
	}
	sw.config.driver = _tx.drv
	return sw
}

// String implements the fmt.Stringer.
func (sw *SessionWorkspace) String() string {
	var builder strings.Builder
	builder.WriteString("SessionWorkspace(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sw.ID))
	builder.WriteString("session_id=")
	builder.WriteString(fmt.Sprintf("%v", sw.SessionID))
	builder.WriteString(", ")
	builder.WriteString("workspace_id=")
	builder.WriteString(sw.WorkspaceID)
	builder.WriteString(", ")
	builder.WriteString("workspace_root=")
	builder.WriteString(sw.WorkspaceRoot)
	builder.WriteString(", ")
	builder.WriteString("git_dir=")
	builder.WriteString(sw.GitDir)
	builder.WriteString(", ")
	builder.WriteString("git_common_dir=")
	builder.WriteString(sw.GitCommonDir)
	builder.WriteString(", ")
	builder.WriteString("first_seen_at=")
	builder.WriteString(sw.FirstSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(sw.LastSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("binding_source=")
	builder.WriteString(fmt.Sprintf("%v", sw.BindingSource))
	builder.WriteByte(')')
	return builder.String()
}

// SessionWorkspaces is a parsable slice of SessionWorkspace.
type SessionWorkspaces []*SessionWorkspace
