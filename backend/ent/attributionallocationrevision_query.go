// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/attributionallocationrevision"
	"github.com/ai-efficiency/backend/ent/attributionusagebucket"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// AttributionAllocationRevisionQuery is the builder for querying AttributionAllocationRevision entities.
type AttributionAllocationRevisionQuery struct {
	config
	ctx             *QueryContext
	order           []attributionallocationrevision.OrderOption
	inters          []Interceptor
	predicates      []predicate.AttributionAllocationRevision
	withUsageBucket *AttributionUsageBucketQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributionAllocationRevisionQuery builder.
func (aarq *AttributionAllocationRevisionQuery) Where(ps ...predicate.AttributionAllocationRevision) *AttributionAllocationRevisionQuery {
	aarq.predicates = append(aarq.predicates, ps...)
	return aarq
}

// Limit the number of records to be returned by this query.
func (aarq *AttributionAllocationRevisionQuery) Limit(limit int) *AttributionAllocationRevisionQuery {
	aarq.ctx.Limit = &limit
	return aarq
}

// Offset to start from.
func (aarq *AttributionAllocationRevisionQuery) Offset(offset int) *AttributionAllocationRevisionQuery {
	aarq.ctx.Offset = &offset
	return aarq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aarq *AttributionAllocationRevisionQuery) Unique(unique bool) *AttributionAllocationRevisionQuery {
	aarq.ctx.Unique = &unique
	return aarq
}

// Order specifies how the records should be ordered.
func (aarq *AttributionAllocationRevisionQuery) Order(o ...attributionallocationrevision.OrderOption) *AttributionAllocationRevisionQuery {
	aarq.order = append(aarq.order, o...)
	return aarq
}

// QueryUsageBucket chains the current query on the "usage_bucket" edge.
func (aarq *AttributionAllocationRevisionQuery) QueryUsageBucket() *AttributionUsageBucketQuery {
	query := (&AttributionUsageBucketClient{config: aarq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aarq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aarq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attributionallocationrevision.Table, attributionallocationrevision.FieldID, selector),
			sqlgraph.To(attributionusagebucket.Table, attributionusagebucket.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attributionallocationrevision.UsageBucketTable, attributionallocationrevision.UsageBucketColumn),
		)
		fromU = sqlgraph.SetNeighbors(aarq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AttributionAllocationRevision entity from the query.
// Returns a *NotFoundError when no AttributionAllocationRevision was found.
func (aarq *AttributionAllocationRevisionQuery) First(ctx context.Context) (*AttributionAllocationRevision, error) {
	nodes, err := aarq.Limit(1).All(setContextOp(ctx, aarq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributionallocationrevision.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) FirstX(ctx context.Context) *AttributionAllocationRevision {
	node, err := aarq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributionAllocationRevision ID from the query.
// Returns a *NotFoundError when no AttributionAllocationRevision ID was found.
func (aarq *AttributionAllocationRevisionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aarq.Limit(1).IDs(setContextOp(ctx, aarq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributionallocationrevision.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) FirstIDX(ctx context.Context) int {
	id, err := aarq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributionAllocationRevision entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributionAllocationRevision entity is found.
// Returns a *NotFoundError when no AttributionAllocationRevision entities are found.
func (aarq *AttributionAllocationRevisionQuery) Only(ctx context.Context) (*AttributionAllocationRevision, error) {
	nodes, err := aarq.Limit(2).All(setContextOp(ctx, aarq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributionallocationrevision.Label}
	default:
		return nil, &NotSingularError{attributionallocationrevision.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) OnlyX(ctx context.Context) *AttributionAllocationRevision {
	node, err := aarq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributionAllocationRevision ID in the query.
// Returns a *NotSingularError when more than one AttributionAllocationRevision ID is found.
// Returns a *NotFoundError when no entities are found.
func (aarq *AttributionAllocationRevisionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aarq.Limit(2).IDs(setContextOp(ctx, aarq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributionallocationrevision.Label}
	default:
		err = &NotSingularError{attributionallocationrevision.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) OnlyIDX(ctx context.Context) int {
	id, err := aarq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributionAllocationRevisions.
func (aarq *AttributionAllocationRevisionQuery) All(ctx context.Context) ([]*AttributionAllocationRevision, error) {
	ctx = setContextOp(ctx, aarq.ctx, ent.OpQueryAll)
	if err := aarq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributionAllocationRevision, *AttributionAllocationRevisionQuery]()
	return withInterceptors[[]*AttributionAllocationRevision](ctx, aarq, qr, aarq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) AllX(ctx context.Context) []*AttributionAllocationRevision {
	nodes, err := aarq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributionAllocationRevision IDs.
func (aarq *AttributionAllocationRevisionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aarq.ctx.Unique == nil && aarq.path != nil {
		aarq.Unique(true)
	}
	ctx = setContextOp(ctx, aarq.ctx, ent.OpQueryIDs)
	if err = aarq.Select(attributionallocationrevision.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) IDsX(ctx context.Context) []int {
	ids, err := aarq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aarq *AttributionAllocationRevisionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aarq.ctx, ent.OpQueryCount)
	if err := aarq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aarq, querierCount[*AttributionAllocationRevisionQuery](), aarq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) CountX(ctx context.Context) int {
	count, err := aarq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aarq *AttributionAllocationRevisionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aarq.ctx, ent.OpQueryExist)
	switch _, err := aarq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aarq *AttributionAllocationRevisionQuery) ExistX(ctx context.Context) bool {
	exist, err := aarq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributionAllocationRevisionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aarq *AttributionAllocationRevisionQuery) Clone() *AttributionAllocationRevisionQuery {
	if aarq == nil {
		return nil
	}
	return &AttributionAllocationRevisionQuery{
		config:          aarq.config,
		ctx:             aarq.ctx.Clone(),
		order:           append([]attributionallocationrevision.OrderOption{}, aarq.order...),
		inters:          append([]Interceptor{}, aarq.inters...),
		predicates:      append([]predicate.AttributionAllocationRevision{}, aarq.predicates...),
		withUsageBucket: aarq.withUsageBucket.Clone(),
		// clone intermediate query.
		sql:  aarq.sql.Clone(),
		path: aarq.path,
	}
}

// WithUsageBucket tells the query-builder to eager-load the nodes that are connected to
// the "usage_bucket" edge. The optional arguments are used to configure the query builder of the edge.
func (aarq *AttributionAllocationRevisionQuery) WithUsageBucket(opts ...func(*AttributionUsageBucketQuery)) *AttributionAllocationRevisionQuery {
	query := (&AttributionUsageBucketClient{config: aarq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aarq.withUsageBucket = query
	return aarq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RevisionID string `json:"revision_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributionAllocationRevision.Query().
//		GroupBy(attributionallocationrevision.FieldRevisionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aarq *AttributionAllocationRevisionQuery) GroupBy(field string, fields ...string) *AttributionAllocationRevisionGroupBy {
	aarq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributionAllocationRevisionGroupBy{build: aarq}
	grbuild.flds = &aarq.ctx.Fields
	grbuild.label = attributionallocationrevision.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RevisionID string `json:"revision_id,omitempty"`
//	}
//
//	client.AttributionAllocationRevision.Query().
//		Select(attributionallocationrevision.FieldRevisionID).
//		Scan(ctx, &v)
func (aarq *AttributionAllocationRevisionQuery) Select(fields ...string) *AttributionAllocationRevisionSelect {
	aarq.ctx.Fields = append(aarq.ctx.Fields, fields...)
	sbuild := &AttributionAllocationRevisionSelect{AttributionAllocationRevisionQuery: aarq}
	sbuild.label = attributionallocationrevision.Label
	sbuild.flds, sbuild.scan = &aarq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributionAllocationRevisionSelect configured with the given aggregations.
func (aarq *AttributionAllocationRevisionQuery) Aggregate(fns ...AggregateFunc) *AttributionAllocationRevisionSelect {
	return aarq.Select().Aggregate(fns...)
}

func (aarq *AttributionAllocationRevisionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aarq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aarq); err != nil {
				return err
			}
		}
	}
	for _, f := range aarq.ctx.Fields {
		if !attributionallocationrevision.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aarq.path != nil {
		prev, err := aarq.path(ctx)
		if err != nil {
			return err
		}
		aarq.sql = prev
	}
	return nil
}

func (aarq *AttributionAllocationRevisionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributionAllocationRevision, error) {
	var (
		nodes       = []*AttributionAllocationRevision{}
		_spec       = aarq.querySpec()
		loadedTypes = [1]bool{
			aarq.withUsageBucket != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributionAllocationRevision).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributionAllocationRevision{config: aarq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aarq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aarq.withUsageBucket; query != nil {
		if err := aarq.loadUsageBucket(ctx, query, nodes, nil,
			func(n *AttributionAllocationRevision, e *AttributionUsageBucket) { n.Edges.UsageBucket = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aarq *AttributionAllocationRevisionQuery) loadUsageBucket(ctx context.Context, query *AttributionUsageBucketQuery, nodes []*AttributionAllocationRevision, init func(*AttributionAllocationRevision), assign func(*AttributionAllocationRevision, *AttributionUsageBucket)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AttributionAllocationRevision)
	for i := range nodes {
		fk := nodes[i].UsageBucketID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(attributionusagebucket.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "usage_bucket_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (aarq *AttributionAllocationRevisionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aarq.querySpec()
	_spec.Node.Columns = aarq.ctx.Fields
	if len(aarq.ctx.Fields) > 0 {
		_spec.Unique = aarq.ctx.Unique != nil && *aarq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aarq.driver, _spec)
}

func (aarq *AttributionAllocationRevisionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributionallocationrevision.Table, attributionallocationrevision.Columns, sqlgraph.NewFieldSpec(attributionallocationrevision.FieldID, field.TypeInt))
	_spec.From = aarq.sql
	if unique := aarq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aarq.path != nil {
		_spec.Unique = true
	}
	if fields := aarq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributionallocationrevision.FieldID)
		for i := range fields {
			if fields[i] != attributionallocationrevision.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if aarq.withUsageBucket != nil {
			_spec.Node.AddColumnOnce(attributionallocationrevision.FieldUsageBucketID)
		}
	}
	if ps := aarq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aarq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aarq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aarq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aarq *AttributionAllocationRevisionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aarq.driver.Dialect())
	t1 := builder.Table(attributionallocationrevision.Table)
	columns := aarq.ctx.Fields
	if len(columns) == 0 {
		columns = attributionallocationrevision.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aarq.sql != nil {
		selector = aarq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aarq.ctx.Unique != nil && *aarq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aarq.predicates {
		p(selector)
	}
	for _, p := range aarq.order {
		p(selector)
	}
	if offset := aarq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aarq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttributionAllocationRevisionGroupBy is the group-by builder for AttributionAllocationRevision entities.
type AttributionAllocationRevisionGroupBy struct {
	selector
	build *AttributionAllocationRevisionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aargb *AttributionAllocationRevisionGroupBy) Aggregate(fns ...AggregateFunc) *AttributionAllocationRevisionGroupBy {
	aargb.fns = append(aargb.fns, fns...)
	return aargb
}

// Scan applies the selector query and scans the result into the given value.
func (aargb *AttributionAllocationRevisionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aargb.build.ctx, ent.OpQueryGroupBy)
	if err := aargb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionAllocationRevisionQuery, *AttributionAllocationRevisionGroupBy](ctx, aargb.build, aargb, aargb.build.inters, v)
}

func (aargb *AttributionAllocationRevisionGroupBy) sqlScan(ctx context.Context, root *AttributionAllocationRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aargb.fns))
	for _, fn := range aargb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aargb.flds)+len(aargb.fns))
		for _, f := range *aargb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aargb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aargb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributionAllocationRevisionSelect is the builder for selecting fields of AttributionAllocationRevision entities.
type AttributionAllocationRevisionSelect struct {
	*AttributionAllocationRevisionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aars *AttributionAllocationRevisionSelect) Aggregate(fns ...AggregateFunc) *AttributionAllocationRevisionSelect {
	aars.fns = append(aars.fns, fns...)
	return aars
}

// Scan applies the selector query and scans the result into the given value.
func (aars *AttributionAllocationRevisionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aars.ctx, ent.OpQuerySelect)
	if err := aars.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionAllocationRevisionQuery, *AttributionAllocationRevisionSelect](ctx, aars.AttributionAllocationRevisionQuery, aars, aars.inters, v)
}

func (aars *AttributionAllocationRevisionSelect) sqlScan(ctx context.Context, root *AttributionAllocationRevisionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aars.fns))
	for _, fn := range aars.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aars.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aars.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
