// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/attributionusagepool"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// AttributionUsagePoolQuery is the builder for querying AttributionUsagePool entities.
type AttributionUsagePoolQuery struct {
	config
	ctx        *QueryContext
	order      []attributionusagepool.OrderOption
	inters     []Interceptor
	predicates []predicate.AttributionUsagePool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributionUsagePoolQuery builder.
func (aupq *AttributionUsagePoolQuery) Where(ps ...predicate.AttributionUsagePool) *AttributionUsagePoolQuery {
	aupq.predicates = append(aupq.predicates, ps...)
	return aupq
}

// Limit the number of records to be returned by this query.
func (aupq *AttributionUsagePoolQuery) Limit(limit int) *AttributionUsagePoolQuery {
	aupq.ctx.Limit = &limit
	return aupq
}

// Offset to start from.
func (aupq *AttributionUsagePoolQuery) Offset(offset int) *AttributionUsagePoolQuery {
	aupq.ctx.Offset = &offset
	return aupq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aupq *AttributionUsagePoolQuery) Unique(unique bool) *AttributionUsagePoolQuery {
	aupq.ctx.Unique = &unique
	return aupq
}

// Order specifies how the records should be ordered.
func (aupq *AttributionUsagePoolQuery) Order(o ...attributionusagepool.OrderOption) *AttributionUsagePoolQuery {
	aupq.order = append(aupq.order, o...)
	return aupq
}

// First returns the first AttributionUsagePool entity from the query.
// Returns a *NotFoundError when no AttributionUsagePool was found.
func (aupq *AttributionUsagePoolQuery) First(ctx context.Context) (*AttributionUsagePool, error) {
	nodes, err := aupq.Limit(1).All(setContextOp(ctx, aupq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributionusagepool.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) FirstX(ctx context.Context) *AttributionUsagePool {
	node, err := aupq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributionUsagePool ID from the query.
// Returns a *NotFoundError when no AttributionUsagePool ID was found.
func (aupq *AttributionUsagePoolQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aupq.Limit(1).IDs(setContextOp(ctx, aupq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributionusagepool.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) FirstIDX(ctx context.Context) int {
	id, err := aupq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributionUsagePool entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributionUsagePool entity is found.
// Returns a *NotFoundError when no AttributionUsagePool entities are found.
func (aupq *AttributionUsagePoolQuery) Only(ctx context.Context) (*AttributionUsagePool, error) {
	nodes, err := aupq.Limit(2).All(setContextOp(ctx, aupq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributionusagepool.Label}
	default:
		return nil, &NotSingularError{attributionusagepool.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) OnlyX(ctx context.Context) *AttributionUsagePool {
	node, err := aupq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributionUsagePool ID in the query.
// Returns a *NotSingularError when more than one AttributionUsagePool ID is found.
// Returns a *NotFoundError when no entities are found.
func (aupq *AttributionUsagePoolQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aupq.Limit(2).IDs(setContextOp(ctx, aupq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributionusagepool.Label}
	default:
		err = &NotSingularError{attributionusagepool.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) OnlyIDX(ctx context.Context) int {
	id, err := aupq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributionUsagePools.
func (aupq *AttributionUsagePoolQuery) All(ctx context.Context) ([]*AttributionUsagePool, error) {
	ctx = setContextOp(ctx, aupq.ctx, ent.OpQueryAll)
	if err := aupq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributionUsagePool, *AttributionUsagePoolQuery]()
	return withInterceptors[[]*AttributionUsagePool](ctx, aupq, qr, aupq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) AllX(ctx context.Context) []*AttributionUsagePool {
	nodes, err := aupq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributionUsagePool IDs.
func (aupq *AttributionUsagePoolQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aupq.ctx.Unique == nil && aupq.path != nil {
		aupq.Unique(true)
	}
	ctx = setContextOp(ctx, aupq.ctx, ent.OpQueryIDs)
	if err = aupq.Select(attributionusagepool.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) IDsX(ctx context.Context) []int {
	ids, err := aupq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aupq *AttributionUsagePoolQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aupq.ctx, ent.OpQueryCount)
	if err := aupq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aupq, querierCount[*AttributionUsagePoolQuery](), aupq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) CountX(ctx context.Context) int {
	count, err := aupq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aupq *AttributionUsagePoolQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aupq.ctx, ent.OpQueryExist)
	switch _, err := aupq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aupq *AttributionUsagePoolQuery) ExistX(ctx context.Context) bool {
	exist, err := aupq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributionUsagePoolQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aupq *AttributionUsagePoolQuery) Clone() *AttributionUsagePoolQuery {
	if aupq == nil {
		return nil
	}
	return &AttributionUsagePoolQuery{
		config:     aupq.config,
		ctx:        aupq.ctx.Clone(),
		order:      append([]attributionusagepool.OrderOption{}, aupq.order...),
		inters:     append([]Interceptor{}, aupq.inters...),
		predicates: append([]predicate.AttributionUsagePool{}, aupq.predicates...),
		// clone intermediate query.
		sql:  aupq.sql.Clone(),
		path: aupq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CanonicalPoolKey string `json:"canonical_pool_key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributionUsagePool.Query().
//		GroupBy(attributionusagepool.FieldCanonicalPoolKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aupq *AttributionUsagePoolQuery) GroupBy(field string, fields ...string) *AttributionUsagePoolGroupBy {
	aupq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributionUsagePoolGroupBy{build: aupq}
	grbuild.flds = &aupq.ctx.Fields
	grbuild.label = attributionusagepool.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CanonicalPoolKey string `json:"canonical_pool_key,omitempty"`
//	}
//
//	client.AttributionUsagePool.Query().
//		Select(attributionusagepool.FieldCanonicalPoolKey).
//		Scan(ctx, &v)
func (aupq *AttributionUsagePoolQuery) Select(fields ...string) *AttributionUsagePoolSelect {
	aupq.ctx.Fields = append(aupq.ctx.Fields, fields...)
	sbuild := &AttributionUsagePoolSelect{AttributionUsagePoolQuery: aupq}
	sbuild.label = attributionusagepool.Label
	sbuild.flds, sbuild.scan = &aupq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributionUsagePoolSelect configured with the given aggregations.
func (aupq *AttributionUsagePoolQuery) Aggregate(fns ...AggregateFunc) *AttributionUsagePoolSelect {
	return aupq.Select().Aggregate(fns...)
}

func (aupq *AttributionUsagePoolQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aupq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aupq); err != nil {
				return err
			}
		}
	}
	for _, f := range aupq.ctx.Fields {
		if !attributionusagepool.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aupq.path != nil {
		prev, err := aupq.path(ctx)
		if err != nil {
			return err
		}
		aupq.sql = prev
	}
	return nil
}

func (aupq *AttributionUsagePoolQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributionUsagePool, error) {
	var (
		nodes = []*AttributionUsagePool{}
		_spec = aupq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributionUsagePool).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributionUsagePool{config: aupq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aupq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aupq *AttributionUsagePoolQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aupq.querySpec()
	_spec.Node.Columns = aupq.ctx.Fields
	if len(aupq.ctx.Fields) > 0 {
		_spec.Unique = aupq.ctx.Unique != nil && *aupq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aupq.driver, _spec)
}

func (aupq *AttributionUsagePoolQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributionusagepool.Table, attributionusagepool.Columns, sqlgraph.NewFieldSpec(attributionusagepool.FieldID, field.TypeInt))
	_spec.From = aupq.sql
	if unique := aupq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aupq.path != nil {
		_spec.Unique = true
	}
	if fields := aupq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributionusagepool.FieldID)
		for i := range fields {
			if fields[i] != attributionusagepool.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aupq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aupq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aupq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aupq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aupq *AttributionUsagePoolQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aupq.driver.Dialect())
	t1 := builder.Table(attributionusagepool.Table)
	columns := aupq.ctx.Fields
	if len(columns) == 0 {
		columns = attributionusagepool.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aupq.sql != nil {
		selector = aupq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aupq.ctx.Unique != nil && *aupq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aupq.predicates {
		p(selector)
	}
	for _, p := range aupq.order {
		p(selector)
	}
	if offset := aupq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aupq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttributionUsagePoolGroupBy is the group-by builder for AttributionUsagePool entities.
type AttributionUsagePoolGroupBy struct {
	selector
	build *AttributionUsagePoolQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aupgb *AttributionUsagePoolGroupBy) Aggregate(fns ...AggregateFunc) *AttributionUsagePoolGroupBy {
	aupgb.fns = append(aupgb.fns, fns...)
	return aupgb
}

// Scan applies the selector query and scans the result into the given value.
func (aupgb *AttributionUsagePoolGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aupgb.build.ctx, ent.OpQueryGroupBy)
	if err := aupgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionUsagePoolQuery, *AttributionUsagePoolGroupBy](ctx, aupgb.build, aupgb, aupgb.build.inters, v)
}

func (aupgb *AttributionUsagePoolGroupBy) sqlScan(ctx context.Context, root *AttributionUsagePoolQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aupgb.fns))
	for _, fn := range aupgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aupgb.flds)+len(aupgb.fns))
		for _, f := range *aupgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aupgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aupgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributionUsagePoolSelect is the builder for selecting fields of AttributionUsagePool entities.
type AttributionUsagePoolSelect struct {
	*AttributionUsagePoolQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aups *AttributionUsagePoolSelect) Aggregate(fns ...AggregateFunc) *AttributionUsagePoolSelect {
	aups.fns = append(aups.fns, fns...)
	return aups
}

// Scan applies the selector query and scans the result into the given value.
func (aups *AttributionUsagePoolSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aups.ctx, ent.OpQuerySelect)
	if err := aups.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionUsagePoolQuery, *AttributionUsagePoolSelect](ctx, aups.AttributionUsagePoolQuery, aups, aups.inters, v)
}

func (aups *AttributionUsagePoolSelect) sqlScan(ctx context.Context, root *AttributionUsagePoolQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aups.fns))
	for _, fn := range aups.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aups.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aups.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
