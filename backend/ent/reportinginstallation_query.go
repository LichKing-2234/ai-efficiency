// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/reportinginstallation"
	"github.com/ai-efficiency/backend/ent/user"
)

// ReportingInstallationQuery is the builder for querying ReportingInstallation entities.
type ReportingInstallationQuery struct {
	config
	ctx        *QueryContext
	order      []reportinginstallation.OrderOption
	inters     []Interceptor
	predicates []predicate.ReportingInstallation
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReportingInstallationQuery builder.
func (riq *ReportingInstallationQuery) Where(ps ...predicate.ReportingInstallation) *ReportingInstallationQuery {
	riq.predicates = append(riq.predicates, ps...)
	return riq
}

// Limit the number of records to be returned by this query.
func (riq *ReportingInstallationQuery) Limit(limit int) *ReportingInstallationQuery {
	riq.ctx.Limit = &limit
	return riq
}

// Offset to start from.
func (riq *ReportingInstallationQuery) Offset(offset int) *ReportingInstallationQuery {
	riq.ctx.Offset = &offset
	return riq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (riq *ReportingInstallationQuery) Unique(unique bool) *ReportingInstallationQuery {
	riq.ctx.Unique = &unique
	return riq
}

// Order specifies how the records should be ordered.
func (riq *ReportingInstallationQuery) Order(o ...reportinginstallation.OrderOption) *ReportingInstallationQuery {
	riq.order = append(riq.order, o...)
	return riq
}

// QueryUser chains the current query on the "user" edge.
func (riq *ReportingInstallationQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: riq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := riq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := riq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reportinginstallation.Table, reportinginstallation.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, reportinginstallation.UserTable, reportinginstallation.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(riq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ReportingInstallation entity from the query.
// Returns a *NotFoundError when no ReportingInstallation was found.
func (riq *ReportingInstallationQuery) First(ctx context.Context) (*ReportingInstallation, error) {
	nodes, err := riq.Limit(1).All(setContextOp(ctx, riq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reportinginstallation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (riq *ReportingInstallationQuery) FirstX(ctx context.Context) *ReportingInstallation {
	node, err := riq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReportingInstallation ID from the query.
// Returns a *NotFoundError when no ReportingInstallation ID was found.
func (riq *ReportingInstallationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = riq.Limit(1).IDs(setContextOp(ctx, riq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reportinginstallation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (riq *ReportingInstallationQuery) FirstIDX(ctx context.Context) int {
	id, err := riq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReportingInstallation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReportingInstallation entity is found.
// Returns a *NotFoundError when no ReportingInstallation entities are found.
func (riq *ReportingInstallationQuery) Only(ctx context.Context) (*ReportingInstallation, error) {
	nodes, err := riq.Limit(2).All(setContextOp(ctx, riq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reportinginstallation.Label}
	default:
		return nil, &NotSingularError{reportinginstallation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (riq *ReportingInstallationQuery) OnlyX(ctx context.Context) *ReportingInstallation {
	node, err := riq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReportingInstallation ID in the query.
// Returns a *NotSingularError when more than one ReportingInstallation ID is found.
// Returns a *NotFoundError when no entities are found.
func (riq *ReportingInstallationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = riq.Limit(2).IDs(setContextOp(ctx, riq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reportinginstallation.Label}
	default:
		err = &NotSingularError{reportinginstallation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (riq *ReportingInstallationQuery) OnlyIDX(ctx context.Context) int {
	id, err := riq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReportingInstallations.
func (riq *ReportingInstallationQuery) All(ctx context.Context) ([]*ReportingInstallation, error) {
	ctx = setContextOp(ctx, riq.ctx, ent.OpQueryAll)
	if err := riq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ReportingInstallation, *ReportingInstallationQuery]()
	return withInterceptors[[]*ReportingInstallation](ctx, riq, qr, riq.inters)
}

// AllX is like All, but panics if an error occurs.
func (riq *ReportingInstallationQuery) AllX(ctx context.Context) []*ReportingInstallation {
	nodes, err := riq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReportingInstallation IDs.
func (riq *ReportingInstallationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if riq.ctx.Unique == nil && riq.path != nil {
		riq.Unique(true)
	}
	ctx = setContextOp(ctx, riq.ctx, ent.OpQueryIDs)
	if err = riq.Select(reportinginstallation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (riq *ReportingInstallationQuery) IDsX(ctx context.Context) []int {
	ids, err := riq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (riq *ReportingInstallationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, riq.ctx, ent.OpQueryCount)
	if err := riq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, riq, querierCount[*ReportingInstallationQuery](), riq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (riq *ReportingInstallationQuery) CountX(ctx context.Context) int {
	count, err := riq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (riq *ReportingInstallationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, riq.ctx, ent.OpQueryExist)
	switch _, err := riq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (riq *ReportingInstallationQuery) ExistX(ctx context.Context) bool {
	exist, err := riq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReportingInstallationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (riq *ReportingInstallationQuery) Clone() *ReportingInstallationQuery {
	if riq == nil {
		return nil
	}
	return &ReportingInstallationQuery{
		config:     riq.config,
		ctx:        riq.ctx.Clone(),
		order:      append([]reportinginstallation.OrderOption{}, riq.order...),
		inters:     append([]Interceptor{}, riq.inters...),
		predicates: append([]predicate.ReportingInstallation{}, riq.predicates...),
		withUser:   riq.withUser.Clone(),
		// clone intermediate query.
		sql:  riq.sql.Clone(),
		path: riq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (riq *ReportingInstallationQuery) WithUser(opts ...func(*UserQuery)) *ReportingInstallationQuery {
	query := (&UserClient{config: riq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	riq.withUser = query
	return riq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		InstallationID string `json:"installation_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReportingInstallation.Query().
//		GroupBy(reportinginstallation.FieldInstallationID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (riq *ReportingInstallationQuery) GroupBy(field string, fields ...string) *ReportingInstallationGroupBy {
	riq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReportingInstallationGroupBy{build: riq}
	grbuild.flds = &riq.ctx.Fields
	grbuild.label = reportinginstallation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		InstallationID string `json:"installation_id,omitempty"`
//	}
//
//	client.ReportingInstallation.Query().
//		Select(reportinginstallation.FieldInstallationID).
//		Scan(ctx, &v)
func (riq *ReportingInstallationQuery) Select(fields ...string) *ReportingInstallationSelect {
	riq.ctx.Fields = append(riq.ctx.Fields, fields...)
	sbuild := &ReportingInstallationSelect{ReportingInstallationQuery: riq}
	sbuild.label = reportinginstallation.Label
	sbuild.flds, sbuild.scan = &riq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReportingInstallationSelect configured with the given aggregations.
func (riq *ReportingInstallationQuery) Aggregate(fns ...AggregateFunc) *ReportingInstallationSelect {
	return riq.Select().Aggregate(fns...)
}

func (riq *ReportingInstallationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range riq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, riq); err != nil {
				return err
			}
		}
	}
	for _, f := range riq.ctx.Fields {
		if !reportinginstallation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if riq.path != nil {
		prev, err := riq.path(ctx)
		if err != nil {
			return err
		}
		riq.sql = prev
	}
	return nil
}

func (riq *ReportingInstallationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReportingInstallation, error) {
	var (
		nodes       = []*ReportingInstallation{}
		_spec       = riq.querySpec()
		loadedTypes = [1]bool{
			riq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ReportingInstallation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ReportingInstallation{config: riq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, riq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := riq.withUser; query != nil {
		if err := riq.loadUser(ctx, query, nodes, nil,
			func(n *ReportingInstallation, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (riq *ReportingInstallationQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ReportingInstallation, init func(*ReportingInstallation), assign func(*ReportingInstallation, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ReportingInstallation)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (riq *ReportingInstallationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := riq.querySpec()
	_spec.Node.Columns = riq.ctx.Fields
	if len(riq.ctx.Fields) > 0 {
		_spec.Unique = riq.ctx.Unique != nil && *riq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, riq.driver, _spec)
}

func (riq *ReportingInstallationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(reportinginstallation.Table, reportinginstallation.Columns, sqlgraph.NewFieldSpec(reportinginstallation.FieldID, field.TypeInt))
	_spec.From = riq.sql
	if unique := riq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if riq.path != nil {
		_spec.Unique = true
	}
	if fields := riq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reportinginstallation.FieldID)
		for i := range fields {
			if fields[i] != reportinginstallation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if riq.withUser != nil {
			_spec.Node.AddColumnOnce(reportinginstallation.FieldUserID)
		}
	}
	if ps := riq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := riq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := riq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := riq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (riq *ReportingInstallationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(riq.driver.Dialect())
	t1 := builder.Table(reportinginstallation.Table)
	columns := riq.ctx.Fields
	if len(columns) == 0 {
		columns = reportinginstallation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if riq.sql != nil {
		selector = riq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if riq.ctx.Unique != nil && *riq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range riq.predicates {
		p(selector)
	}
	for _, p := range riq.order {
		p(selector)
	}
	if offset := riq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := riq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReportingInstallationGroupBy is the group-by builder for ReportingInstallation entities.
type ReportingInstallationGroupBy struct {
	selector
	build *ReportingInstallationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rigb *ReportingInstallationGroupBy) Aggregate(fns ...AggregateFunc) *ReportingInstallationGroupBy {
	rigb.fns = append(rigb.fns, fns...)
	return rigb
}

// Scan applies the selector query and scans the result into the given value.
func (rigb *ReportingInstallationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rigb.build.ctx, ent.OpQueryGroupBy)
	if err := rigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReportingInstallationQuery, *ReportingInstallationGroupBy](ctx, rigb.build, rigb, rigb.build.inters, v)
}

func (rigb *ReportingInstallationGroupBy) sqlScan(ctx context.Context, root *ReportingInstallationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rigb.fns))
	for _, fn := range rigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rigb.flds)+len(rigb.fns))
		for _, f := range *rigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReportingInstallationSelect is the builder for selecting fields of ReportingInstallation entities.
type ReportingInstallationSelect struct {
	*ReportingInstallationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ris *ReportingInstallationSelect) Aggregate(fns ...AggregateFunc) *ReportingInstallationSelect {
	ris.fns = append(ris.fns, fns...)
	return ris
}

// Scan applies the selector query and scans the result into the given value.
func (ris *ReportingInstallationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ris.ctx, ent.OpQuerySelect)
	if err := ris.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReportingInstallationQuery, *ReportingInstallationSelect](ctx, ris.ReportingInstallationQuery, ris, ris.inters, v)
}

func (ris *ReportingInstallationSelect) sqlScan(ctx context.Context, root *ReportingInstallationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ris.fns))
	for _, fn := range ris.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ris.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ris.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
