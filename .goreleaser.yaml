version: 2

project_name: ai-efficiency

builds:
  - id: backend-server
    dir: backend
    main: ./cmd/server
    binary: ai-efficiency-server
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/ai-efficiency/backend/internal/buildinfo.BuildVersion=v{{ .Version }}
      - -X github.com/ai-efficiency/backend/internal/buildinfo.BuildCommit={{ .Commit }}
      - -X github.com/ai-efficiency/backend/internal/buildinfo.BuildTime={{ .Date }}

archives:
  - id: backend-bundle
    ids:
      - backend-server
    name_template: "ai-efficiency-backend_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - deploy/README.md
      - deploy/config.example.yaml
      - deploy/.env.example
      - deploy/docker-compose.yml
      - deploy/docker-compose.external.yml
      - deploy/docker-compose.bootstrap.yml
      - deploy/docker-deploy.sh
      - deploy/install.sh
      - deploy/ai-efficiency.service

checksum:
  name_template: checksums.txt
  algorithm: sha256

release:
  github:
    owner: '{{ envOrDefault "GITHUB_REPO_OWNER" "ai-efficiency" }}'
    name: '{{ envOrDefault "GITHUB_REPO_NAME" "ai-efficiency" }}'
  draft: false
  prerelease: auto
  name_template: "ai-efficiency {{ .Version }}"
