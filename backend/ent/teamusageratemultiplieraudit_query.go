// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/teamusageratemultiplieraudit"
)

// TeamUsageRateMultiplierAuditQuery is the builder for querying TeamUsageRateMultiplierAudit entities.
type TeamUsageRateMultiplierAuditQuery struct {
	config
	ctx        *QueryContext
	order      []teamusageratemultiplieraudit.OrderOption
	inters     []Interceptor
	predicates []predicate.TeamUsageRateMultiplierAudit
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TeamUsageRateMultiplierAuditQuery builder.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Where(ps ...predicate.TeamUsageRateMultiplierAudit) *TeamUsageRateMultiplierAuditQuery {
	turmaq.predicates = append(turmaq.predicates, ps...)
	return turmaq
}

// Limit the number of records to be returned by this query.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Limit(limit int) *TeamUsageRateMultiplierAuditQuery {
	turmaq.ctx.Limit = &limit
	return turmaq
}

// Offset to start from.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Offset(offset int) *TeamUsageRateMultiplierAuditQuery {
	turmaq.ctx.Offset = &offset
	return turmaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Unique(unique bool) *TeamUsageRateMultiplierAuditQuery {
	turmaq.ctx.Unique = &unique
	return turmaq
}

// Order specifies how the records should be ordered.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Order(o ...teamusageratemultiplieraudit.OrderOption) *TeamUsageRateMultiplierAuditQuery {
	turmaq.order = append(turmaq.order, o...)
	return turmaq
}

// First returns the first TeamUsageRateMultiplierAudit entity from the query.
// Returns a *NotFoundError when no TeamUsageRateMultiplierAudit was found.
func (turmaq *TeamUsageRateMultiplierAuditQuery) First(ctx context.Context) (*TeamUsageRateMultiplierAudit, error) {
	nodes, err := turmaq.Limit(1).All(setContextOp(ctx, turmaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{teamusageratemultiplieraudit.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) FirstX(ctx context.Context) *TeamUsageRateMultiplierAudit {
	node, err := turmaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TeamUsageRateMultiplierAudit ID from the query.
// Returns a *NotFoundError when no TeamUsageRateMultiplierAudit ID was found.
func (turmaq *TeamUsageRateMultiplierAuditQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = turmaq.Limit(1).IDs(setContextOp(ctx, turmaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{teamusageratemultiplieraudit.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) FirstIDX(ctx context.Context) int {
	id, err := turmaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TeamUsageRateMultiplierAudit entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TeamUsageRateMultiplierAudit entity is found.
// Returns a *NotFoundError when no TeamUsageRateMultiplierAudit entities are found.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Only(ctx context.Context) (*TeamUsageRateMultiplierAudit, error) {
	nodes, err := turmaq.Limit(2).All(setContextOp(ctx, turmaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{teamusageratemultiplieraudit.Label}
	default:
		return nil, &NotSingularError{teamusageratemultiplieraudit.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) OnlyX(ctx context.Context) *TeamUsageRateMultiplierAudit {
	node, err := turmaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TeamUsageRateMultiplierAudit ID in the query.
// Returns a *NotSingularError when more than one TeamUsageRateMultiplierAudit ID is found.
// Returns a *NotFoundError when no entities are found.
func (turmaq *TeamUsageRateMultiplierAuditQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = turmaq.Limit(2).IDs(setContextOp(ctx, turmaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{teamusageratemultiplieraudit.Label}
	default:
		err = &NotSingularError{teamusageratemultiplieraudit.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) OnlyIDX(ctx context.Context) int {
	id, err := turmaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TeamUsageRateMultiplierAudits.
func (turmaq *TeamUsageRateMultiplierAuditQuery) All(ctx context.Context) ([]*TeamUsageRateMultiplierAudit, error) {
	ctx = setContextOp(ctx, turmaq.ctx, ent.OpQueryAll)
	if err := turmaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TeamUsageRateMultiplierAudit, *TeamUsageRateMultiplierAuditQuery]()
	return withInterceptors[[]*TeamUsageRateMultiplierAudit](ctx, turmaq, qr, turmaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) AllX(ctx context.Context) []*TeamUsageRateMultiplierAudit {
	nodes, err := turmaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TeamUsageRateMultiplierAudit IDs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) IDs(ctx context.Context) (ids []int, err error) {
	if turmaq.ctx.Unique == nil && turmaq.path != nil {
		turmaq.Unique(true)
	}
	ctx = setContextOp(ctx, turmaq.ctx, ent.OpQueryIDs)
	if err = turmaq.Select(teamusageratemultiplieraudit.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) IDsX(ctx context.Context) []int {
	ids, err := turmaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, turmaq.ctx, ent.OpQueryCount)
	if err := turmaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, turmaq, querierCount[*TeamUsageRateMultiplierAuditQuery](), turmaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) CountX(ctx context.Context) int {
	count, err := turmaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, turmaq.ctx, ent.OpQueryExist)
	switch _, err := turmaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (turmaq *TeamUsageRateMultiplierAuditQuery) ExistX(ctx context.Context) bool {
	exist, err := turmaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TeamUsageRateMultiplierAuditQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Clone() *TeamUsageRateMultiplierAuditQuery {
	if turmaq == nil {
		return nil
	}
	return &TeamUsageRateMultiplierAuditQuery{
		config:     turmaq.config,
		ctx:        turmaq.ctx.Clone(),
		order:      append([]teamusageratemultiplieraudit.OrderOption{}, turmaq.order...),
		inters:     append([]Interceptor{}, turmaq.inters...),
		predicates: append([]predicate.TeamUsageRateMultiplierAudit{}, turmaq.predicates...),
		// clone intermediate query.
		sql:  turmaq.sql.Clone(),
		path: turmaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ActorUserID int `json:"actor_user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TeamUsageRateMultiplierAudit.Query().
//		GroupBy(teamusageratemultiplieraudit.FieldActorUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (turmaq *TeamUsageRateMultiplierAuditQuery) GroupBy(field string, fields ...string) *TeamUsageRateMultiplierAuditGroupBy {
	turmaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TeamUsageRateMultiplierAuditGroupBy{build: turmaq}
	grbuild.flds = &turmaq.ctx.Fields
	grbuild.label = teamusageratemultiplieraudit.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ActorUserID int `json:"actor_user_id,omitempty"`
//	}
//
//	client.TeamUsageRateMultiplierAudit.Query().
//		Select(teamusageratemultiplieraudit.FieldActorUserID).
//		Scan(ctx, &v)
func (turmaq *TeamUsageRateMultiplierAuditQuery) Select(fields ...string) *TeamUsageRateMultiplierAuditSelect {
	turmaq.ctx.Fields = append(turmaq.ctx.Fields, fields...)
	sbuild := &TeamUsageRateMultiplierAuditSelect{TeamUsageRateMultiplierAuditQuery: turmaq}
	sbuild.label = teamusageratemultiplieraudit.Label
	sbuild.flds, sbuild.scan = &turmaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TeamUsageRateMultiplierAuditSelect configured with the given aggregations.
func (turmaq *TeamUsageRateMultiplierAuditQuery) Aggregate(fns ...AggregateFunc) *TeamUsageRateMultiplierAuditSelect {
	return turmaq.Select().Aggregate(fns...)
}

func (turmaq *TeamUsageRateMultiplierAuditQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range turmaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, turmaq); err != nil {
				return err
			}
		}
	}
	for _, f := range turmaq.ctx.Fields {
		if !teamusageratemultiplieraudit.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if turmaq.path != nil {
		prev, err := turmaq.path(ctx)
		if err != nil {
			return err
		}
		turmaq.sql = prev
	}
	return nil
}

func (turmaq *TeamUsageRateMultiplierAuditQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TeamUsageRateMultiplierAudit, error) {
	var (
		nodes = []*TeamUsageRateMultiplierAudit{}
		_spec = turmaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TeamUsageRateMultiplierAudit).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TeamUsageRateMultiplierAudit{config: turmaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, turmaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (turmaq *TeamUsageRateMultiplierAuditQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := turmaq.querySpec()
	_spec.Node.Columns = turmaq.ctx.Fields
	if len(turmaq.ctx.Fields) > 0 {
		_spec.Unique = turmaq.ctx.Unique != nil && *turmaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, turmaq.driver, _spec)
}

func (turmaq *TeamUsageRateMultiplierAuditQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(teamusageratemultiplieraudit.Table, teamusageratemultiplieraudit.Columns, sqlgraph.NewFieldSpec(teamusageratemultiplieraudit.FieldID, field.TypeInt))
	_spec.From = turmaq.sql
	if unique := turmaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if turmaq.path != nil {
		_spec.Unique = true
	}
	if fields := turmaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, teamusageratemultiplieraudit.FieldID)
		for i := range fields {
			if fields[i] != teamusageratemultiplieraudit.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := turmaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := turmaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := turmaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := turmaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (turmaq *TeamUsageRateMultiplierAuditQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(turmaq.driver.Dialect())
	t1 := builder.Table(teamusageratemultiplieraudit.Table)
	columns := turmaq.ctx.Fields
	if len(columns) == 0 {
		columns = teamusageratemultiplieraudit.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if turmaq.sql != nil {
		selector = turmaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if turmaq.ctx.Unique != nil && *turmaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range turmaq.predicates {
		p(selector)
	}
	for _, p := range turmaq.order {
		p(selector)
	}
	if offset := turmaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := turmaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TeamUsageRateMultiplierAuditGroupBy is the group-by builder for TeamUsageRateMultiplierAudit entities.
type TeamUsageRateMultiplierAuditGroupBy struct {
	selector
	build *TeamUsageRateMultiplierAuditQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (turmagb *TeamUsageRateMultiplierAuditGroupBy) Aggregate(fns ...AggregateFunc) *TeamUsageRateMultiplierAuditGroupBy {
	turmagb.fns = append(turmagb.fns, fns...)
	return turmagb
}

// Scan applies the selector query and scans the result into the given value.
func (turmagb *TeamUsageRateMultiplierAuditGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, turmagb.build.ctx, ent.OpQueryGroupBy)
	if err := turmagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamUsageRateMultiplierAuditQuery, *TeamUsageRateMultiplierAuditGroupBy](ctx, turmagb.build, turmagb, turmagb.build.inters, v)
}

func (turmagb *TeamUsageRateMultiplierAuditGroupBy) sqlScan(ctx context.Context, root *TeamUsageRateMultiplierAuditQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(turmagb.fns))
	for _, fn := range turmagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*turmagb.flds)+len(turmagb.fns))
		for _, f := range *turmagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*turmagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := turmagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TeamUsageRateMultiplierAuditSelect is the builder for selecting fields of TeamUsageRateMultiplierAudit entities.
type TeamUsageRateMultiplierAuditSelect struct {
	*TeamUsageRateMultiplierAuditQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (turmas *TeamUsageRateMultiplierAuditSelect) Aggregate(fns ...AggregateFunc) *TeamUsageRateMultiplierAuditSelect {
	turmas.fns = append(turmas.fns, fns...)
	return turmas
}

// Scan applies the selector query and scans the result into the given value.
func (turmas *TeamUsageRateMultiplierAuditSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, turmas.ctx, ent.OpQuerySelect)
	if err := turmas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamUsageRateMultiplierAuditQuery, *TeamUsageRateMultiplierAuditSelect](ctx, turmas.TeamUsageRateMultiplierAuditQuery, turmas, turmas.inters, v)
}

func (turmas *TeamUsageRateMultiplierAuditSelect) sqlScan(ctx context.Context, root *TeamUsageRateMultiplierAuditQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(turmas.fns))
	for _, fn := range turmas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*turmas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := turmas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
