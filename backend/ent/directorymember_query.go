// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/directorymember"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// DirectoryMemberQuery is the builder for querying DirectoryMember entities.
type DirectoryMemberQuery struct {
	config
	ctx        *QueryContext
	order      []directorymember.OrderOption
	inters     []Interceptor
	predicates []predicate.DirectoryMember
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DirectoryMemberQuery builder.
func (dmq *DirectoryMemberQuery) Where(ps ...predicate.DirectoryMember) *DirectoryMemberQuery {
	dmq.predicates = append(dmq.predicates, ps...)
	return dmq
}

// Limit the number of records to be returned by this query.
func (dmq *DirectoryMemberQuery) Limit(limit int) *DirectoryMemberQuery {
	dmq.ctx.Limit = &limit
	return dmq
}

// Offset to start from.
func (dmq *DirectoryMemberQuery) Offset(offset int) *DirectoryMemberQuery {
	dmq.ctx.Offset = &offset
	return dmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dmq *DirectoryMemberQuery) Unique(unique bool) *DirectoryMemberQuery {
	dmq.ctx.Unique = &unique
	return dmq
}

// Order specifies how the records should be ordered.
func (dmq *DirectoryMemberQuery) Order(o ...directorymember.OrderOption) *DirectoryMemberQuery {
	dmq.order = append(dmq.order, o...)
	return dmq
}

// First returns the first DirectoryMember entity from the query.
// Returns a *NotFoundError when no DirectoryMember was found.
func (dmq *DirectoryMemberQuery) First(ctx context.Context) (*DirectoryMember, error) {
	nodes, err := dmq.Limit(1).All(setContextOp(ctx, dmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{directorymember.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) FirstX(ctx context.Context) *DirectoryMember {
	node, err := dmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DirectoryMember ID from the query.
// Returns a *NotFoundError when no DirectoryMember ID was found.
func (dmq *DirectoryMemberQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dmq.Limit(1).IDs(setContextOp(ctx, dmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{directorymember.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) FirstIDX(ctx context.Context) int {
	id, err := dmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DirectoryMember entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DirectoryMember entity is found.
// Returns a *NotFoundError when no DirectoryMember entities are found.
func (dmq *DirectoryMemberQuery) Only(ctx context.Context) (*DirectoryMember, error) {
	nodes, err := dmq.Limit(2).All(setContextOp(ctx, dmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{directorymember.Label}
	default:
		return nil, &NotSingularError{directorymember.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) OnlyX(ctx context.Context) *DirectoryMember {
	node, err := dmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DirectoryMember ID in the query.
// Returns a *NotSingularError when more than one DirectoryMember ID is found.
// Returns a *NotFoundError when no entities are found.
func (dmq *DirectoryMemberQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dmq.Limit(2).IDs(setContextOp(ctx, dmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{directorymember.Label}
	default:
		err = &NotSingularError{directorymember.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) OnlyIDX(ctx context.Context) int {
	id, err := dmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DirectoryMembers.
func (dmq *DirectoryMemberQuery) All(ctx context.Context) ([]*DirectoryMember, error) {
	ctx = setContextOp(ctx, dmq.ctx, ent.OpQueryAll)
	if err := dmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DirectoryMember, *DirectoryMemberQuery]()
	return withInterceptors[[]*DirectoryMember](ctx, dmq, qr, dmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) AllX(ctx context.Context) []*DirectoryMember {
	nodes, err := dmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DirectoryMember IDs.
func (dmq *DirectoryMemberQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dmq.ctx.Unique == nil && dmq.path != nil {
		dmq.Unique(true)
	}
	ctx = setContextOp(ctx, dmq.ctx, ent.OpQueryIDs)
	if err = dmq.Select(directorymember.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) IDsX(ctx context.Context) []int {
	ids, err := dmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dmq *DirectoryMemberQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dmq.ctx, ent.OpQueryCount)
	if err := dmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dmq, querierCount[*DirectoryMemberQuery](), dmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) CountX(ctx context.Context) int {
	count, err := dmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dmq *DirectoryMemberQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dmq.ctx, ent.OpQueryExist)
	switch _, err := dmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dmq *DirectoryMemberQuery) ExistX(ctx context.Context) bool {
	exist, err := dmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DirectoryMemberQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dmq *DirectoryMemberQuery) Clone() *DirectoryMemberQuery {
	if dmq == nil {
		return nil
	}
	return &DirectoryMemberQuery{
		config:     dmq.config,
		ctx:        dmq.ctx.Clone(),
		order:      append([]directorymember.OrderOption{}, dmq.order...),
		inters:     append([]Interceptor{}, dmq.inters...),
		predicates: append([]predicate.DirectoryMember{}, dmq.predicates...),
		// clone intermediate query.
		sql:  dmq.sql.Clone(),
		path: dmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DirectoryMember.Query().
//		GroupBy(directorymember.FieldSourceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dmq *DirectoryMemberQuery) GroupBy(field string, fields ...string) *DirectoryMemberGroupBy {
	dmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DirectoryMemberGroupBy{build: dmq}
	grbuild.flds = &dmq.ctx.Fields
	grbuild.label = directorymember.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//	}
//
//	client.DirectoryMember.Query().
//		Select(directorymember.FieldSourceID).
//		Scan(ctx, &v)
func (dmq *DirectoryMemberQuery) Select(fields ...string) *DirectoryMemberSelect {
	dmq.ctx.Fields = append(dmq.ctx.Fields, fields...)
	sbuild := &DirectoryMemberSelect{DirectoryMemberQuery: dmq}
	sbuild.label = directorymember.Label
	sbuild.flds, sbuild.scan = &dmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DirectoryMemberSelect configured with the given aggregations.
func (dmq *DirectoryMemberQuery) Aggregate(fns ...AggregateFunc) *DirectoryMemberSelect {
	return dmq.Select().Aggregate(fns...)
}

func (dmq *DirectoryMemberQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dmq); err != nil {
				return err
			}
		}
	}
	for _, f := range dmq.ctx.Fields {
		if !directorymember.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dmq.path != nil {
		prev, err := dmq.path(ctx)
		if err != nil {
			return err
		}
		dmq.sql = prev
	}
	return nil
}

func (dmq *DirectoryMemberQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DirectoryMember, error) {
	var (
		nodes = []*DirectoryMember{}
		_spec = dmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DirectoryMember).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DirectoryMember{config: dmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dmq *DirectoryMemberQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dmq.querySpec()
	_spec.Node.Columns = dmq.ctx.Fields
	if len(dmq.ctx.Fields) > 0 {
		_spec.Unique = dmq.ctx.Unique != nil && *dmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dmq.driver, _spec)
}

func (dmq *DirectoryMemberQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(directorymember.Table, directorymember.Columns, sqlgraph.NewFieldSpec(directorymember.FieldID, field.TypeInt))
	_spec.From = dmq.sql
	if unique := dmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dmq.path != nil {
		_spec.Unique = true
	}
	if fields := dmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, directorymember.FieldID)
		for i := range fields {
			if fields[i] != directorymember.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dmq *DirectoryMemberQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dmq.driver.Dialect())
	t1 := builder.Table(directorymember.Table)
	columns := dmq.ctx.Fields
	if len(columns) == 0 {
		columns = directorymember.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dmq.sql != nil {
		selector = dmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dmq.ctx.Unique != nil && *dmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dmq.predicates {
		p(selector)
	}
	for _, p := range dmq.order {
		p(selector)
	}
	if offset := dmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DirectoryMemberGroupBy is the group-by builder for DirectoryMember entities.
type DirectoryMemberGroupBy struct {
	selector
	build *DirectoryMemberQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dmgb *DirectoryMemberGroupBy) Aggregate(fns ...AggregateFunc) *DirectoryMemberGroupBy {
	dmgb.fns = append(dmgb.fns, fns...)
	return dmgb
}

// Scan applies the selector query and scans the result into the given value.
func (dmgb *DirectoryMemberGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dmgb.build.ctx, ent.OpQueryGroupBy)
	if err := dmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectoryMemberQuery, *DirectoryMemberGroupBy](ctx, dmgb.build, dmgb, dmgb.build.inters, v)
}

func (dmgb *DirectoryMemberGroupBy) sqlScan(ctx context.Context, root *DirectoryMemberQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dmgb.fns))
	for _, fn := range dmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dmgb.flds)+len(dmgb.fns))
		for _, f := range *dmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DirectoryMemberSelect is the builder for selecting fields of DirectoryMember entities.
type DirectoryMemberSelect struct {
	*DirectoryMemberQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dms *DirectoryMemberSelect) Aggregate(fns ...AggregateFunc) *DirectoryMemberSelect {
	dms.fns = append(dms.fns, fns...)
	return dms
}

// Scan applies the selector query and scans the result into the given value.
func (dms *DirectoryMemberSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dms.ctx, ent.OpQuerySelect)
	if err := dms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectoryMemberQuery, *DirectoryMemberSelect](ctx, dms.DirectoryMemberQuery, dms, dms.inters, v)
}

func (dms *DirectoryMemberSelect) sqlScan(ctx context.Context, root *DirectoryMemberQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dms.fns))
	for _, fn := range dms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
