// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/efficiencymetric"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/repoconfig"
)

// EfficiencyMetricQuery is the builder for querying EfficiencyMetric entities.
type EfficiencyMetricQuery struct {
	config
	ctx            *QueryContext
	order          []efficiencymetric.OrderOption
	inters         []Interceptor
	predicates     []predicate.EfficiencyMetric
	withRepoConfig *RepoConfigQuery
	withFKs        bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EfficiencyMetricQuery builder.
func (emq *EfficiencyMetricQuery) Where(ps ...predicate.EfficiencyMetric) *EfficiencyMetricQuery {
	emq.predicates = append(emq.predicates, ps...)
	return emq
}

// Limit the number of records to be returned by this query.
func (emq *EfficiencyMetricQuery) Limit(limit int) *EfficiencyMetricQuery {
	emq.ctx.Limit = &limit
	return emq
}

// Offset to start from.
func (emq *EfficiencyMetricQuery) Offset(offset int) *EfficiencyMetricQuery {
	emq.ctx.Offset = &offset
	return emq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (emq *EfficiencyMetricQuery) Unique(unique bool) *EfficiencyMetricQuery {
	emq.ctx.Unique = &unique
	return emq
}

// Order specifies how the records should be ordered.
func (emq *EfficiencyMetricQuery) Order(o ...efficiencymetric.OrderOption) *EfficiencyMetricQuery {
	emq.order = append(emq.order, o...)
	return emq
}

// QueryRepoConfig chains the current query on the "repo_config" edge.
func (emq *EfficiencyMetricQuery) QueryRepoConfig() *RepoConfigQuery {
	query := (&RepoConfigClient{config: emq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := emq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := emq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(efficiencymetric.Table, efficiencymetric.FieldID, selector),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, efficiencymetric.RepoConfigTable, efficiencymetric.RepoConfigColumn),
		)
		fromU = sqlgraph.SetNeighbors(emq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EfficiencyMetric entity from the query.
// Returns a *NotFoundError when no EfficiencyMetric was found.
func (emq *EfficiencyMetricQuery) First(ctx context.Context) (*EfficiencyMetric, error) {
	nodes, err := emq.Limit(1).All(setContextOp(ctx, emq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{efficiencymetric.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) FirstX(ctx context.Context) *EfficiencyMetric {
	node, err := emq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EfficiencyMetric ID from the query.
// Returns a *NotFoundError when no EfficiencyMetric ID was found.
func (emq *EfficiencyMetricQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = emq.Limit(1).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{efficiencymetric.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) FirstIDX(ctx context.Context) int {
	id, err := emq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EfficiencyMetric entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EfficiencyMetric entity is found.
// Returns a *NotFoundError when no EfficiencyMetric entities are found.
func (emq *EfficiencyMetricQuery) Only(ctx context.Context) (*EfficiencyMetric, error) {
	nodes, err := emq.Limit(2).All(setContextOp(ctx, emq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{efficiencymetric.Label}
	default:
		return nil, &NotSingularError{efficiencymetric.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) OnlyX(ctx context.Context) *EfficiencyMetric {
	node, err := emq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EfficiencyMetric ID in the query.
// Returns a *NotSingularError when more than one EfficiencyMetric ID is found.
// Returns a *NotFoundError when no entities are found.
func (emq *EfficiencyMetricQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = emq.Limit(2).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{efficiencymetric.Label}
	default:
		err = &NotSingularError{efficiencymetric.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) OnlyIDX(ctx context.Context) int {
	id, err := emq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EfficiencyMetrics.
func (emq *EfficiencyMetricQuery) All(ctx context.Context) ([]*EfficiencyMetric, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryAll)
	if err := emq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EfficiencyMetric, *EfficiencyMetricQuery]()
	return withInterceptors[[]*EfficiencyMetric](ctx, emq, qr, emq.inters)
}

// AllX is like All, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) AllX(ctx context.Context) []*EfficiencyMetric {
	nodes, err := emq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EfficiencyMetric IDs.
func (emq *EfficiencyMetricQuery) IDs(ctx context.Context) (ids []int, err error) {
	if emq.ctx.Unique == nil && emq.path != nil {
		emq.Unique(true)
	}
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryIDs)
	if err = emq.Select(efficiencymetric.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) IDsX(ctx context.Context) []int {
	ids, err := emq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (emq *EfficiencyMetricQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryCount)
	if err := emq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, emq, querierCount[*EfficiencyMetricQuery](), emq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) CountX(ctx context.Context) int {
	count, err := emq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (emq *EfficiencyMetricQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryExist)
	switch _, err := emq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (emq *EfficiencyMetricQuery) ExistX(ctx context.Context) bool {
	exist, err := emq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EfficiencyMetricQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (emq *EfficiencyMetricQuery) Clone() *EfficiencyMetricQuery {
	if emq == nil {
		return nil
	}
	return &EfficiencyMetricQuery{
		config:         emq.config,
		ctx:            emq.ctx.Clone(),
		order:          append([]efficiencymetric.OrderOption{}, emq.order...),
		inters:         append([]Interceptor{}, emq.inters...),
		predicates:     append([]predicate.EfficiencyMetric{}, emq.predicates...),
		withRepoConfig: emq.withRepoConfig.Clone(),
		// clone intermediate query.
		sql:  emq.sql.Clone(),
		path: emq.path,
	}
}

// WithRepoConfig tells the query-builder to eager-load the nodes that are connected to
// the "repo_config" edge. The optional arguments are used to configure the query builder of the edge.
func (emq *EfficiencyMetricQuery) WithRepoConfig(opts ...func(*RepoConfigQuery)) *EfficiencyMetricQuery {
	query := (&RepoConfigClient{config: emq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	emq.withRepoConfig = query
	return emq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EfficiencyMetric.Query().
//		GroupBy(efficiencymetric.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (emq *EfficiencyMetricQuery) GroupBy(field string, fields ...string) *EfficiencyMetricGroupBy {
	emq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EfficiencyMetricGroupBy{build: emq}
	grbuild.flds = &emq.ctx.Fields
	grbuild.label = efficiencymetric.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.EfficiencyMetric.Query().
//		Select(efficiencymetric.FieldUserID).
//		Scan(ctx, &v)
func (emq *EfficiencyMetricQuery) Select(fields ...string) *EfficiencyMetricSelect {
	emq.ctx.Fields = append(emq.ctx.Fields, fields...)
	sbuild := &EfficiencyMetricSelect{EfficiencyMetricQuery: emq}
	sbuild.label = efficiencymetric.Label
	sbuild.flds, sbuild.scan = &emq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EfficiencyMetricSelect configured with the given aggregations.
func (emq *EfficiencyMetricQuery) Aggregate(fns ...AggregateFunc) *EfficiencyMetricSelect {
	return emq.Select().Aggregate(fns...)
}

func (emq *EfficiencyMetricQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range emq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, emq); err != nil {
				return err
			}
		}
	}
	for _, f := range emq.ctx.Fields {
		if !efficiencymetric.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if emq.path != nil {
		prev, err := emq.path(ctx)
		if err != nil {
			return err
		}
		emq.sql = prev
	}
	return nil
}

func (emq *EfficiencyMetricQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EfficiencyMetric, error) {
	var (
		nodes       = []*EfficiencyMetric{}
		withFKs     = emq.withFKs
		_spec       = emq.querySpec()
		loadedTypes = [1]bool{
			emq.withRepoConfig != nil,
		}
	)
	if emq.withRepoConfig != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, efficiencymetric.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EfficiencyMetric).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EfficiencyMetric{config: emq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, emq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := emq.withRepoConfig; query != nil {
		if err := emq.loadRepoConfig(ctx, query, nodes, nil,
			func(n *EfficiencyMetric, e *RepoConfig) { n.Edges.RepoConfig = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (emq *EfficiencyMetricQuery) loadRepoConfig(ctx context.Context, query *RepoConfigQuery, nodes []*EfficiencyMetric, init func(*EfficiencyMetric), assign func(*EfficiencyMetric, *RepoConfig)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EfficiencyMetric)
	for i := range nodes {
		if nodes[i].repo_config_efficiency_metrics == nil {
			continue
		}
		fk := *nodes[i].repo_config_efficiency_metrics
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(repoconfig.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "repo_config_efficiency_metrics" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (emq *EfficiencyMetricQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := emq.querySpec()
	_spec.Node.Columns = emq.ctx.Fields
	if len(emq.ctx.Fields) > 0 {
		_spec.Unique = emq.ctx.Unique != nil && *emq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, emq.driver, _spec)
}

func (emq *EfficiencyMetricQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(efficiencymetric.Table, efficiencymetric.Columns, sqlgraph.NewFieldSpec(efficiencymetric.FieldID, field.TypeInt))
	_spec.From = emq.sql
	if unique := emq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if emq.path != nil {
		_spec.Unique = true
	}
	if fields := emq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, efficiencymetric.FieldID)
		for i := range fields {
			if fields[i] != efficiencymetric.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := emq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := emq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := emq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := emq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (emq *EfficiencyMetricQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(emq.driver.Dialect())
	t1 := builder.Table(efficiencymetric.Table)
	columns := emq.ctx.Fields
	if len(columns) == 0 {
		columns = efficiencymetric.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if emq.sql != nil {
		selector = emq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if emq.ctx.Unique != nil && *emq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range emq.predicates {
		p(selector)
	}
	for _, p := range emq.order {
		p(selector)
	}
	if offset := emq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := emq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EfficiencyMetricGroupBy is the group-by builder for EfficiencyMetric entities.
type EfficiencyMetricGroupBy struct {
	selector
	build *EfficiencyMetricQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (emgb *EfficiencyMetricGroupBy) Aggregate(fns ...AggregateFunc) *EfficiencyMetricGroupBy {
	emgb.fns = append(emgb.fns, fns...)
	return emgb
}

// Scan applies the selector query and scans the result into the given value.
func (emgb *EfficiencyMetricGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, emgb.build.ctx, ent.OpQueryGroupBy)
	if err := emgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EfficiencyMetricQuery, *EfficiencyMetricGroupBy](ctx, emgb.build, emgb, emgb.build.inters, v)
}

func (emgb *EfficiencyMetricGroupBy) sqlScan(ctx context.Context, root *EfficiencyMetricQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(emgb.fns))
	for _, fn := range emgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*emgb.flds)+len(emgb.fns))
		for _, f := range *emgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*emgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := emgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EfficiencyMetricSelect is the builder for selecting fields of EfficiencyMetric entities.
type EfficiencyMetricSelect struct {
	*EfficiencyMetricQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ems *EfficiencyMetricSelect) Aggregate(fns ...AggregateFunc) *EfficiencyMetricSelect {
	ems.fns = append(ems.fns, fns...)
	return ems
}

// Scan applies the selector query and scans the result into the given value.
func (ems *EfficiencyMetricSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ems.ctx, ent.OpQuerySelect)
	if err := ems.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EfficiencyMetricQuery, *EfficiencyMetricSelect](ctx, ems.EfficiencyMetricQuery, ems, ems.inters, v)
}

func (ems *EfficiencyMetricSelect) sqlScan(ctx context.Context, root *EfficiencyMetricQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ems.fns))
	for _, fn := range ems.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ems.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ems.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
