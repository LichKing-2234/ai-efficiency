// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/aiscanresult"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/repoconfig"
)

// AiScanResultQuery is the builder for querying AiScanResult entities.
type AiScanResultQuery struct {
	config
	ctx            *QueryContext
	order          []aiscanresult.OrderOption
	inters         []Interceptor
	predicates     []predicate.AiScanResult
	withRepoConfig *RepoConfigQuery
	withFKs        bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AiScanResultQuery builder.
func (asrq *AiScanResultQuery) Where(ps ...predicate.AiScanResult) *AiScanResultQuery {
	asrq.predicates = append(asrq.predicates, ps...)
	return asrq
}

// Limit the number of records to be returned by this query.
func (asrq *AiScanResultQuery) Limit(limit int) *AiScanResultQuery {
	asrq.ctx.Limit = &limit
	return asrq
}

// Offset to start from.
func (asrq *AiScanResultQuery) Offset(offset int) *AiScanResultQuery {
	asrq.ctx.Offset = &offset
	return asrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (asrq *AiScanResultQuery) Unique(unique bool) *AiScanResultQuery {
	asrq.ctx.Unique = &unique
	return asrq
}

// Order specifies how the records should be ordered.
func (asrq *AiScanResultQuery) Order(o ...aiscanresult.OrderOption) *AiScanResultQuery {
	asrq.order = append(asrq.order, o...)
	return asrq
}

// QueryRepoConfig chains the current query on the "repo_config" edge.
func (asrq *AiScanResultQuery) QueryRepoConfig() *RepoConfigQuery {
	query := (&RepoConfigClient{config: asrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := asrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := asrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiscanresult.Table, aiscanresult.FieldID, selector),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, aiscanresult.RepoConfigTable, aiscanresult.RepoConfigColumn),
		)
		fromU = sqlgraph.SetNeighbors(asrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AiScanResult entity from the query.
// Returns a *NotFoundError when no AiScanResult was found.
func (asrq *AiScanResultQuery) First(ctx context.Context) (*AiScanResult, error) {
	nodes, err := asrq.Limit(1).All(setContextOp(ctx, asrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{aiscanresult.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (asrq *AiScanResultQuery) FirstX(ctx context.Context) *AiScanResult {
	node, err := asrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AiScanResult ID from the query.
// Returns a *NotFoundError when no AiScanResult ID was found.
func (asrq *AiScanResultQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = asrq.Limit(1).IDs(setContextOp(ctx, asrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{aiscanresult.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (asrq *AiScanResultQuery) FirstIDX(ctx context.Context) int {
	id, err := asrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AiScanResult entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AiScanResult entity is found.
// Returns a *NotFoundError when no AiScanResult entities are found.
func (asrq *AiScanResultQuery) Only(ctx context.Context) (*AiScanResult, error) {
	nodes, err := asrq.Limit(2).All(setContextOp(ctx, asrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{aiscanresult.Label}
	default:
		return nil, &NotSingularError{aiscanresult.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (asrq *AiScanResultQuery) OnlyX(ctx context.Context) *AiScanResult {
	node, err := asrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AiScanResult ID in the query.
// Returns a *NotSingularError when more than one AiScanResult ID is found.
// Returns a *NotFoundError when no entities are found.
func (asrq *AiScanResultQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = asrq.Limit(2).IDs(setContextOp(ctx, asrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{aiscanresult.Label}
	default:
		err = &NotSingularError{aiscanresult.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (asrq *AiScanResultQuery) OnlyIDX(ctx context.Context) int {
	id, err := asrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AiScanResults.
func (asrq *AiScanResultQuery) All(ctx context.Context) ([]*AiScanResult, error) {
	ctx = setContextOp(ctx, asrq.ctx, ent.OpQueryAll)
	if err := asrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AiScanResult, *AiScanResultQuery]()
	return withInterceptors[[]*AiScanResult](ctx, asrq, qr, asrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (asrq *AiScanResultQuery) AllX(ctx context.Context) []*AiScanResult {
	nodes, err := asrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AiScanResult IDs.
func (asrq *AiScanResultQuery) IDs(ctx context.Context) (ids []int, err error) {
	if asrq.ctx.Unique == nil && asrq.path != nil {
		asrq.Unique(true)
	}
	ctx = setContextOp(ctx, asrq.ctx, ent.OpQueryIDs)
	if err = asrq.Select(aiscanresult.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (asrq *AiScanResultQuery) IDsX(ctx context.Context) []int {
	ids, err := asrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (asrq *AiScanResultQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, asrq.ctx, ent.OpQueryCount)
	if err := asrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, asrq, querierCount[*AiScanResultQuery](), asrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (asrq *AiScanResultQuery) CountX(ctx context.Context) int {
	count, err := asrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (asrq *AiScanResultQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, asrq.ctx, ent.OpQueryExist)
	switch _, err := asrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (asrq *AiScanResultQuery) ExistX(ctx context.Context) bool {
	exist, err := asrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AiScanResultQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (asrq *AiScanResultQuery) Clone() *AiScanResultQuery {
	if asrq == nil {
		return nil
	}
	return &AiScanResultQuery{
		config:         asrq.config,
		ctx:            asrq.ctx.Clone(),
		order:          append([]aiscanresult.OrderOption{}, asrq.order...),
		inters:         append([]Interceptor{}, asrq.inters...),
		predicates:     append([]predicate.AiScanResult{}, asrq.predicates...),
		withRepoConfig: asrq.withRepoConfig.Clone(),
		// clone intermediate query.
		sql:  asrq.sql.Clone(),
		path: asrq.path,
	}
}

// WithRepoConfig tells the query-builder to eager-load the nodes that are connected to
// the "repo_config" edge. The optional arguments are used to configure the query builder of the edge.
func (asrq *AiScanResultQuery) WithRepoConfig(opts ...func(*RepoConfigQuery)) *AiScanResultQuery {
	query := (&RepoConfigClient{config: asrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	asrq.withRepoConfig = query
	return asrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Score int `json:"score,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AiScanResult.Query().
//		GroupBy(aiscanresult.FieldScore).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (asrq *AiScanResultQuery) GroupBy(field string, fields ...string) *AiScanResultGroupBy {
	asrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AiScanResultGroupBy{build: asrq}
	grbuild.flds = &asrq.ctx.Fields
	grbuild.label = aiscanresult.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Score int `json:"score,omitempty"`
//	}
//
//	client.AiScanResult.Query().
//		Select(aiscanresult.FieldScore).
//		Scan(ctx, &v)
func (asrq *AiScanResultQuery) Select(fields ...string) *AiScanResultSelect {
	asrq.ctx.Fields = append(asrq.ctx.Fields, fields...)
	sbuild := &AiScanResultSelect{AiScanResultQuery: asrq}
	sbuild.label = aiscanresult.Label
	sbuild.flds, sbuild.scan = &asrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AiScanResultSelect configured with the given aggregations.
func (asrq *AiScanResultQuery) Aggregate(fns ...AggregateFunc) *AiScanResultSelect {
	return asrq.Select().Aggregate(fns...)
}

func (asrq *AiScanResultQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range asrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, asrq); err != nil {
				return err
			}
		}
	}
	for _, f := range asrq.ctx.Fields {
		if !aiscanresult.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if asrq.path != nil {
		prev, err := asrq.path(ctx)
		if err != nil {
			return err
		}
		asrq.sql = prev
	}
	return nil
}

func (asrq *AiScanResultQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AiScanResult, error) {
	var (
		nodes       = []*AiScanResult{}
		withFKs     = asrq.withFKs
		_spec       = asrq.querySpec()
		loadedTypes = [1]bool{
			asrq.withRepoConfig != nil,
		}
	)
	if asrq.withRepoConfig != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, aiscanresult.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AiScanResult).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AiScanResult{config: asrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, asrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := asrq.withRepoConfig; query != nil {
		if err := asrq.loadRepoConfig(ctx, query, nodes, nil,
			func(n *AiScanResult, e *RepoConfig) { n.Edges.RepoConfig = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (asrq *AiScanResultQuery) loadRepoConfig(ctx context.Context, query *RepoConfigQuery, nodes []*AiScanResult, init func(*AiScanResult), assign func(*AiScanResult, *RepoConfig)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AiScanResult)
	for i := range nodes {
		if nodes[i].repo_config_ai_scan_results == nil {
			continue
		}
		fk := *nodes[i].repo_config_ai_scan_results
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(repoconfig.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "repo_config_ai_scan_results" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (asrq *AiScanResultQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := asrq.querySpec()
	_spec.Node.Columns = asrq.ctx.Fields
	if len(asrq.ctx.Fields) > 0 {
		_spec.Unique = asrq.ctx.Unique != nil && *asrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, asrq.driver, _spec)
}

func (asrq *AiScanResultQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(aiscanresult.Table, aiscanresult.Columns, sqlgraph.NewFieldSpec(aiscanresult.FieldID, field.TypeInt))
	_spec.From = asrq.sql
	if unique := asrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if asrq.path != nil {
		_spec.Unique = true
	}
	if fields := asrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, aiscanresult.FieldID)
		for i := range fields {
			if fields[i] != aiscanresult.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := asrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := asrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := asrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := asrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (asrq *AiScanResultQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(asrq.driver.Dialect())
	t1 := builder.Table(aiscanresult.Table)
	columns := asrq.ctx.Fields
	if len(columns) == 0 {
		columns = aiscanresult.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if asrq.sql != nil {
		selector = asrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if asrq.ctx.Unique != nil && *asrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range asrq.predicates {
		p(selector)
	}
	for _, p := range asrq.order {
		p(selector)
	}
	if offset := asrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := asrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AiScanResultGroupBy is the group-by builder for AiScanResult entities.
type AiScanResultGroupBy struct {
	selector
	build *AiScanResultQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (asrgb *AiScanResultGroupBy) Aggregate(fns ...AggregateFunc) *AiScanResultGroupBy {
	asrgb.fns = append(asrgb.fns, fns...)
	return asrgb
}

// Scan applies the selector query and scans the result into the given value.
func (asrgb *AiScanResultGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asrgb.build.ctx, ent.OpQueryGroupBy)
	if err := asrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AiScanResultQuery, *AiScanResultGroupBy](ctx, asrgb.build, asrgb, asrgb.build.inters, v)
}

func (asrgb *AiScanResultGroupBy) sqlScan(ctx context.Context, root *AiScanResultQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(asrgb.fns))
	for _, fn := range asrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*asrgb.flds)+len(asrgb.fns))
		for _, f := range *asrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*asrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AiScanResultSelect is the builder for selecting fields of AiScanResult entities.
type AiScanResultSelect struct {
	*AiScanResultQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (asrs *AiScanResultSelect) Aggregate(fns ...AggregateFunc) *AiScanResultSelect {
	asrs.fns = append(asrs.fns, fns...)
	return asrs
}

// Scan applies the selector query and scans the result into the given value.
func (asrs *AiScanResultSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asrs.ctx, ent.OpQuerySelect)
	if err := asrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AiScanResultQuery, *AiScanResultSelect](ctx, asrs.AiScanResultQuery, asrs, asrs.inters, v)
}

func (asrs *AiScanResultSelect) sqlScan(ctx context.Context, root *AiScanResultQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(asrs.fns))
	for _, fn := range asrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*asrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
