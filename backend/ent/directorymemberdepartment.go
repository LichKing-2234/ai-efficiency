// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ai-efficiency/backend/ent/directorymemberdepartment"
)

// DirectoryMemberDepartment is the model entity for the DirectoryMemberDepartment schema.
type DirectoryMemberDepartment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SourceID holds the value of the "source_id" field.
	SourceID int `json:"source_id,omitempty"`
	// DirectoryMemberID holds the value of the "directory_member_id" field.
	DirectoryMemberID int `json:"directory_member_id,omitempty"`
	// MemberExternalID holds the value of the "member_external_id" field.
	MemberExternalID string `json:"member_external_id,omitempty"`
	// MemberEmailNormalized holds the value of the "member_email_normalized" field.
	MemberEmailNormalized string `json:"member_email_normalized,omitempty"`
	// DepartmentExternalID holds the value of the "department_external_id" field.
	DepartmentExternalID string `json:"department_external_id,omitempty"`
	// LastSeenRunID holds the value of the "last_seen_run_id" field.
	LastSeenRunID int `json:"last_seen_run_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DirectoryMemberDepartment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case directorymemberdepartment.FieldID, directorymemberdepartment.FieldSourceID, directorymemberdepartment.FieldDirectoryMemberID, directorymemberdepartment.FieldLastSeenRunID:
			values[i] = new(sql.NullInt64)
		case directorymemberdepartment.FieldMemberExternalID, directorymemberdepartment.FieldMemberEmailNormalized, directorymemberdepartment.FieldDepartmentExternalID:
			values[i] = new(sql.NullString)
		case directorymemberdepartment.FieldCreatedAt, directorymemberdepartment.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DirectoryMemberDepartment fields.
func (dmd *DirectoryMemberDepartment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case directorymemberdepartment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dmd.ID = int(value.Int64)
		case directorymemberdepartment.FieldSourceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field source_id", values[i])
			} else if value.Valid {
				dmd.SourceID = int(value.Int64)
			}
		case directorymemberdepartment.FieldDirectoryMemberID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field directory_member_id", values[i])
			} else if value.Valid {
				dmd.DirectoryMemberID = int(value.Int64)
			}
		case directorymemberdepartment.FieldMemberExternalID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field member_external_id", values[i])
			} else if value.Valid {
				dmd.MemberExternalID = value.String
			}
		case directorymemberdepartment.FieldMemberEmailNormalized:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field member_email_normalized", values[i])
			} else if value.Valid {
				dmd.MemberEmailNormalized = value.String
			}
		case directorymemberdepartment.FieldDepartmentExternalID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field department_external_id", values[i])
			} else if value.Valid {
				dmd.DepartmentExternalID = value.String
			}
		case directorymemberdepartment.FieldLastSeenRunID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_run_id", values[i])
			} else if value.Valid {
				dmd.LastSeenRunID = int(value.Int64)
			}
		case directorymemberdepartment.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dmd.CreatedAt = value.Time
			}
		case directorymemberdepartment.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dmd.UpdatedAt = value.Time
			}
		default:
			dmd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DirectoryMemberDepartment.
// This includes values selected through modifiers, order, etc.
func (dmd *DirectoryMemberDepartment) Value(name string) (ent.Value, error) {
	return dmd.selectValues.Get(name)
}

// Update returns a builder for updating this DirectoryMemberDepartment.
// Note that you need to call DirectoryMemberDepartment.Unwrap() before calling this method if this DirectoryMemberDepartment
// was returned from a transaction, and the transaction was committed or rolled back.
func (dmd *DirectoryMemberDepartment) Update() *DirectoryMemberDepartmentUpdateOne {
	return NewDirectoryMemberDepartmentClient(dmd.config).UpdateOne(dmd)
}

// Unwrap unwraps the DirectoryMemberDepartment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dmd *DirectoryMemberDepartment) Unwrap() *DirectoryMemberDepartment {
	_tx, ok := dmd.config.driver.(*txDriver)
	if !ok {
		panic("ent: DirectoryMemberDepartment is not a transactional entity")
	}
	dmd.config.driver = _tx.drv
	return dmd
}

// String implements the fmt.Stringer.
func (dmd *DirectoryMemberDepartment) String() string {
	var builder strings.Builder
	builder.WriteString("DirectoryMemberDepartment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dmd.ID))
	builder.WriteString("source_id=")
	builder.WriteString(fmt.Sprintf("%v", dmd.SourceID))
	builder.WriteString(", ")
	builder.WriteString("directory_member_id=")
	builder.WriteString(fmt.Sprintf("%v", dmd.DirectoryMemberID))
	builder.WriteString(", ")
	builder.WriteString("member_external_id=")
	builder.WriteString(dmd.MemberExternalID)
	builder.WriteString(", ")
	builder.WriteString("member_email_normalized=")
	builder.WriteString(dmd.MemberEmailNormalized)
	builder.WriteString(", ")
	builder.WriteString("department_external_id=")
	builder.WriteString(dmd.DepartmentExternalID)
	builder.WriteString(", ")
	builder.WriteString("last_seen_run_id=")
	builder.WriteString(fmt.Sprintf("%v", dmd.LastSeenRunID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dmd.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dmd.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DirectoryMemberDepartments is a parsable slice of DirectoryMemberDepartment.
type DirectoryMemberDepartments []*DirectoryMemberDepartment
