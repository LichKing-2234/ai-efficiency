// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ai-efficiency/backend/ent/scmprovider"
)

// ScmProvider is the model entity for the ScmProvider schema.
type ScmProvider struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Type holds the value of the "type" field.
	Type scmprovider.Type `json:"type,omitempty"`
	// BaseURL holds the value of the "base_url" field.
	BaseURL string `json:"base_url,omitempty"`
	// Credentials holds the value of the "credentials" field.
	Credentials string `json:"-"`
	// Status holds the value of the "status" field.
	Status scmprovider.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ScmProviderQuery when eager-loading is set.
	Edges        ScmProviderEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ScmProviderEdges holds the relations/edges for other nodes in the graph.
type ScmProviderEdges struct {
	// RepoConfigs holds the value of the repo_configs edge.
	RepoConfigs []*RepoConfig `json:"repo_configs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RepoConfigsOrErr returns the RepoConfigs value or an error if the edge
// was not loaded in eager-loading.
func (e ScmProviderEdges) RepoConfigsOrErr() ([]*RepoConfig, error) {
	if e.loadedTypes[0] {
		return e.RepoConfigs, nil
	}
	return nil, &NotLoadedError{edge: "repo_configs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ScmProvider) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case scmprovider.FieldID:
			values[i] = new(sql.NullInt64)
		case scmprovider.FieldName, scmprovider.FieldType, scmprovider.FieldBaseURL, scmprovider.FieldCredentials, scmprovider.FieldStatus:
			values[i] = new(sql.NullString)
		case scmprovider.FieldCreatedAt, scmprovider.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ScmProvider fields.
func (sp *ScmProvider) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case scmprovider.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sp.ID = int(value.Int64)
		case scmprovider.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sp.Name = value.String
			}
		case scmprovider.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				sp.Type = scmprovider.Type(value.String)
			}
		case scmprovider.FieldBaseURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field base_url", values[i])
			} else if value.Valid {
				sp.BaseURL = value.String
			}
		case scmprovider.FieldCredentials:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field credentials", values[i])
			} else if value.Valid {
				sp.Credentials = value.String
			}
		case scmprovider.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				sp.Status = scmprovider.Status(value.String)
			}
		case scmprovider.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sp.CreatedAt = value.Time
			}
		case scmprovider.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sp.UpdatedAt = value.Time
			}
		default:
			sp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ScmProvider.
// This includes values selected through modifiers, order, etc.
func (sp *ScmProvider) Value(name string) (ent.Value, error) {
	return sp.selectValues.Get(name)
}

// QueryRepoConfigs queries the "repo_configs" edge of the ScmProvider entity.
func (sp *ScmProvider) QueryRepoConfigs() *RepoConfigQuery {
	return NewScmProviderClient(sp.config).QueryRepoConfigs(sp)
}

// Update returns a builder for updating this ScmProvider.
// Note that you need to call ScmProvider.Unwrap() before calling this method if this ScmProvider
// was returned from a transaction, and the transaction was committed or rolled back.
func (sp *ScmProvider) Update() *ScmProviderUpdateOne {
	return NewScmProviderClient(sp.config).UpdateOne(sp)
}

// Unwrap unwraps the ScmProvider entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sp *ScmProvider) Unwrap() *ScmProvider {
	_tx, ok := sp.config.driver.(*txDriver)
	if !ok {
		panic("ent: ScmProvider is not a transactional entity")
	}
	sp.config.driver = _tx.drv
	return sp
}

// String implements the fmt.Stringer.
func (sp *ScmProvider) String() string {
	var builder strings.Builder
	builder.WriteString("ScmProvider(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sp.ID))
	builder.WriteString("name=")
	builder.WriteString(sp.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", sp.Type))
	builder.WriteString(", ")
	builder.WriteString("base_url=")
	builder.WriteString(sp.BaseURL)
	builder.WriteString(", ")
	builder.WriteString("credentials=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", sp.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sp.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sp.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ScmProviders is a parsable slice of ScmProvider.
type ScmProviders []*ScmProvider
