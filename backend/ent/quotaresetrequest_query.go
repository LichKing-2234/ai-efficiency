// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/quotaresetrequest"
)

// QuotaResetRequestQuery is the builder for querying QuotaResetRequest entities.
type QuotaResetRequestQuery struct {
	config
	ctx        *QueryContext
	order      []quotaresetrequest.OrderOption
	inters     []Interceptor
	predicates []predicate.QuotaResetRequest
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the QuotaResetRequestQuery builder.
func (qrrq *QuotaResetRequestQuery) Where(ps ...predicate.QuotaResetRequest) *QuotaResetRequestQuery {
	qrrq.predicates = append(qrrq.predicates, ps...)
	return qrrq
}

// Limit the number of records to be returned by this query.
func (qrrq *QuotaResetRequestQuery) Limit(limit int) *QuotaResetRequestQuery {
	qrrq.ctx.Limit = &limit
	return qrrq
}

// Offset to start from.
func (qrrq *QuotaResetRequestQuery) Offset(offset int) *QuotaResetRequestQuery {
	qrrq.ctx.Offset = &offset
	return qrrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (qrrq *QuotaResetRequestQuery) Unique(unique bool) *QuotaResetRequestQuery {
	qrrq.ctx.Unique = &unique
	return qrrq
}

// Order specifies how the records should be ordered.
func (qrrq *QuotaResetRequestQuery) Order(o ...quotaresetrequest.OrderOption) *QuotaResetRequestQuery {
	qrrq.order = append(qrrq.order, o...)
	return qrrq
}

// First returns the first QuotaResetRequest entity from the query.
// Returns a *NotFoundError when no QuotaResetRequest was found.
func (qrrq *QuotaResetRequestQuery) First(ctx context.Context) (*QuotaResetRequest, error) {
	nodes, err := qrrq.Limit(1).All(setContextOp(ctx, qrrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{quotaresetrequest.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) FirstX(ctx context.Context) *QuotaResetRequest {
	node, err := qrrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first QuotaResetRequest ID from the query.
// Returns a *NotFoundError when no QuotaResetRequest ID was found.
func (qrrq *QuotaResetRequestQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = qrrq.Limit(1).IDs(setContextOp(ctx, qrrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{quotaresetrequest.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) FirstIDX(ctx context.Context) int {
	id, err := qrrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single QuotaResetRequest entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one QuotaResetRequest entity is found.
// Returns a *NotFoundError when no QuotaResetRequest entities are found.
func (qrrq *QuotaResetRequestQuery) Only(ctx context.Context) (*QuotaResetRequest, error) {
	nodes, err := qrrq.Limit(2).All(setContextOp(ctx, qrrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{quotaresetrequest.Label}
	default:
		return nil, &NotSingularError{quotaresetrequest.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) OnlyX(ctx context.Context) *QuotaResetRequest {
	node, err := qrrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only QuotaResetRequest ID in the query.
// Returns a *NotSingularError when more than one QuotaResetRequest ID is found.
// Returns a *NotFoundError when no entities are found.
func (qrrq *QuotaResetRequestQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = qrrq.Limit(2).IDs(setContextOp(ctx, qrrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{quotaresetrequest.Label}
	default:
		err = &NotSingularError{quotaresetrequest.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) OnlyIDX(ctx context.Context) int {
	id, err := qrrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of QuotaResetRequests.
func (qrrq *QuotaResetRequestQuery) All(ctx context.Context) ([]*QuotaResetRequest, error) {
	ctx = setContextOp(ctx, qrrq.ctx, ent.OpQueryAll)
	if err := qrrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*QuotaResetRequest, *QuotaResetRequestQuery]()
	return withInterceptors[[]*QuotaResetRequest](ctx, qrrq, qr, qrrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) AllX(ctx context.Context) []*QuotaResetRequest {
	nodes, err := qrrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of QuotaResetRequest IDs.
func (qrrq *QuotaResetRequestQuery) IDs(ctx context.Context) (ids []int, err error) {
	if qrrq.ctx.Unique == nil && qrrq.path != nil {
		qrrq.Unique(true)
	}
	ctx = setContextOp(ctx, qrrq.ctx, ent.OpQueryIDs)
	if err = qrrq.Select(quotaresetrequest.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) IDsX(ctx context.Context) []int {
	ids, err := qrrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (qrrq *QuotaResetRequestQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, qrrq.ctx, ent.OpQueryCount)
	if err := qrrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, qrrq, querierCount[*QuotaResetRequestQuery](), qrrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) CountX(ctx context.Context) int {
	count, err := qrrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (qrrq *QuotaResetRequestQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, qrrq.ctx, ent.OpQueryExist)
	switch _, err := qrrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (qrrq *QuotaResetRequestQuery) ExistX(ctx context.Context) bool {
	exist, err := qrrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the QuotaResetRequestQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (qrrq *QuotaResetRequestQuery) Clone() *QuotaResetRequestQuery {
	if qrrq == nil {
		return nil
	}
	return &QuotaResetRequestQuery{
		config:     qrrq.config,
		ctx:        qrrq.ctx.Clone(),
		order:      append([]quotaresetrequest.OrderOption{}, qrrq.order...),
		inters:     append([]Interceptor{}, qrrq.inters...),
		predicates: append([]predicate.QuotaResetRequest{}, qrrq.predicates...),
		// clone intermediate query.
		sql:  qrrq.sql.Clone(),
		path: qrrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RequesterUserID int `json:"requester_user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.QuotaResetRequest.Query().
//		GroupBy(quotaresetrequest.FieldRequesterUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (qrrq *QuotaResetRequestQuery) GroupBy(field string, fields ...string) *QuotaResetRequestGroupBy {
	qrrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &QuotaResetRequestGroupBy{build: qrrq}
	grbuild.flds = &qrrq.ctx.Fields
	grbuild.label = quotaresetrequest.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RequesterUserID int `json:"requester_user_id,omitempty"`
//	}
//
//	client.QuotaResetRequest.Query().
//		Select(quotaresetrequest.FieldRequesterUserID).
//		Scan(ctx, &v)
func (qrrq *QuotaResetRequestQuery) Select(fields ...string) *QuotaResetRequestSelect {
	qrrq.ctx.Fields = append(qrrq.ctx.Fields, fields...)
	sbuild := &QuotaResetRequestSelect{QuotaResetRequestQuery: qrrq}
	sbuild.label = quotaresetrequest.Label
	sbuild.flds, sbuild.scan = &qrrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a QuotaResetRequestSelect configured with the given aggregations.
func (qrrq *QuotaResetRequestQuery) Aggregate(fns ...AggregateFunc) *QuotaResetRequestSelect {
	return qrrq.Select().Aggregate(fns...)
}

func (qrrq *QuotaResetRequestQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range qrrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, qrrq); err != nil {
				return err
			}
		}
	}
	for _, f := range qrrq.ctx.Fields {
		if !quotaresetrequest.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if qrrq.path != nil {
		prev, err := qrrq.path(ctx)
		if err != nil {
			return err
		}
		qrrq.sql = prev
	}
	return nil
}

func (qrrq *QuotaResetRequestQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*QuotaResetRequest, error) {
	var (
		nodes = []*QuotaResetRequest{}
		_spec = qrrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*QuotaResetRequest).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &QuotaResetRequest{config: qrrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, qrrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (qrrq *QuotaResetRequestQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := qrrq.querySpec()
	_spec.Node.Columns = qrrq.ctx.Fields
	if len(qrrq.ctx.Fields) > 0 {
		_spec.Unique = qrrq.ctx.Unique != nil && *qrrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, qrrq.driver, _spec)
}

func (qrrq *QuotaResetRequestQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(quotaresetrequest.Table, quotaresetrequest.Columns, sqlgraph.NewFieldSpec(quotaresetrequest.FieldID, field.TypeInt))
	_spec.From = qrrq.sql
	if unique := qrrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if qrrq.path != nil {
		_spec.Unique = true
	}
	if fields := qrrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, quotaresetrequest.FieldID)
		for i := range fields {
			if fields[i] != quotaresetrequest.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := qrrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := qrrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := qrrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := qrrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (qrrq *QuotaResetRequestQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(qrrq.driver.Dialect())
	t1 := builder.Table(quotaresetrequest.Table)
	columns := qrrq.ctx.Fields
	if len(columns) == 0 {
		columns = quotaresetrequest.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if qrrq.sql != nil {
		selector = qrrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if qrrq.ctx.Unique != nil && *qrrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range qrrq.predicates {
		p(selector)
	}
	for _, p := range qrrq.order {
		p(selector)
	}
	if offset := qrrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := qrrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// QuotaResetRequestGroupBy is the group-by builder for QuotaResetRequest entities.
type QuotaResetRequestGroupBy struct {
	selector
	build *QuotaResetRequestQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (qrrgb *QuotaResetRequestGroupBy) Aggregate(fns ...AggregateFunc) *QuotaResetRequestGroupBy {
	qrrgb.fns = append(qrrgb.fns, fns...)
	return qrrgb
}

// Scan applies the selector query and scans the result into the given value.
func (qrrgb *QuotaResetRequestGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, qrrgb.build.ctx, ent.OpQueryGroupBy)
	if err := qrrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuotaResetRequestQuery, *QuotaResetRequestGroupBy](ctx, qrrgb.build, qrrgb, qrrgb.build.inters, v)
}

func (qrrgb *QuotaResetRequestGroupBy) sqlScan(ctx context.Context, root *QuotaResetRequestQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(qrrgb.fns))
	for _, fn := range qrrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*qrrgb.flds)+len(qrrgb.fns))
		for _, f := range *qrrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*qrrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := qrrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// QuotaResetRequestSelect is the builder for selecting fields of QuotaResetRequest entities.
type QuotaResetRequestSelect struct {
	*QuotaResetRequestQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (qrrs *QuotaResetRequestSelect) Aggregate(fns ...AggregateFunc) *QuotaResetRequestSelect {
	qrrs.fns = append(qrrs.fns, fns...)
	return qrrs
}

// Scan applies the selector query and scans the result into the given value.
func (qrrs *QuotaResetRequestSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, qrrs.ctx, ent.OpQuerySelect)
	if err := qrrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuotaResetRequestQuery, *QuotaResetRequestSelect](ctx, qrrs.QuotaResetRequestQuery, qrrs, qrrs.inters, v)
}

func (qrrs *QuotaResetRequestSelect) sqlScan(ctx context.Context, root *QuotaResetRequestQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(qrrs.fns))
	for _, fn := range qrrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*qrrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := qrrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
