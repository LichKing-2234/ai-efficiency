// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/commitcheckpoint"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/repoconfig"
	"github.com/ai-efficiency/backend/ent/toolusageevent"
	"github.com/ai-efficiency/backend/ent/user"
)

// ToolUsageEventQuery is the builder for querying ToolUsageEvent entities.
type ToolUsageEventQuery struct {
	config
	ctx                  *QueryContext
	order                []toolusageevent.OrderOption
	inters               []Interceptor
	predicates           []predicate.ToolUsageEvent
	withRepoConfig       *RepoConfigQuery
	withUser             *UserQuery
	withCommitCheckpoint *CommitCheckpointQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ToolUsageEventQuery builder.
func (tueq *ToolUsageEventQuery) Where(ps ...predicate.ToolUsageEvent) *ToolUsageEventQuery {
	tueq.predicates = append(tueq.predicates, ps...)
	return tueq
}

// Limit the number of records to be returned by this query.
func (tueq *ToolUsageEventQuery) Limit(limit int) *ToolUsageEventQuery {
	tueq.ctx.Limit = &limit
	return tueq
}

// Offset to start from.
func (tueq *ToolUsageEventQuery) Offset(offset int) *ToolUsageEventQuery {
	tueq.ctx.Offset = &offset
	return tueq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tueq *ToolUsageEventQuery) Unique(unique bool) *ToolUsageEventQuery {
	tueq.ctx.Unique = &unique
	return tueq
}

// Order specifies how the records should be ordered.
func (tueq *ToolUsageEventQuery) Order(o ...toolusageevent.OrderOption) *ToolUsageEventQuery {
	tueq.order = append(tueq.order, o...)
	return tueq
}

// QueryRepoConfig chains the current query on the "repo_config" edge.
func (tueq *ToolUsageEventQuery) QueryRepoConfig() *RepoConfigQuery {
	query := (&RepoConfigClient{config: tueq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tueq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tueq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(toolusageevent.Table, toolusageevent.FieldID, selector),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, toolusageevent.RepoConfigTable, toolusageevent.RepoConfigColumn),
		)
		fromU = sqlgraph.SetNeighbors(tueq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (tueq *ToolUsageEventQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: tueq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tueq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tueq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(toolusageevent.Table, toolusageevent.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, toolusageevent.UserTable, toolusageevent.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(tueq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCommitCheckpoint chains the current query on the "commit_checkpoint" edge.
func (tueq *ToolUsageEventQuery) QueryCommitCheckpoint() *CommitCheckpointQuery {
	query := (&CommitCheckpointClient{config: tueq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tueq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tueq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(toolusageevent.Table, toolusageevent.FieldID, selector),
			sqlgraph.To(commitcheckpoint.Table, commitcheckpoint.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, toolusageevent.CommitCheckpointTable, toolusageevent.CommitCheckpointColumn),
		)
		fromU = sqlgraph.SetNeighbors(tueq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ToolUsageEvent entity from the query.
// Returns a *NotFoundError when no ToolUsageEvent was found.
func (tueq *ToolUsageEventQuery) First(ctx context.Context) (*ToolUsageEvent, error) {
	nodes, err := tueq.Limit(1).All(setContextOp(ctx, tueq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{toolusageevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) FirstX(ctx context.Context) *ToolUsageEvent {
	node, err := tueq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ToolUsageEvent ID from the query.
// Returns a *NotFoundError when no ToolUsageEvent ID was found.
func (tueq *ToolUsageEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tueq.Limit(1).IDs(setContextOp(ctx, tueq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{toolusageevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) FirstIDX(ctx context.Context) int {
	id, err := tueq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ToolUsageEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ToolUsageEvent entity is found.
// Returns a *NotFoundError when no ToolUsageEvent entities are found.
func (tueq *ToolUsageEventQuery) Only(ctx context.Context) (*ToolUsageEvent, error) {
	nodes, err := tueq.Limit(2).All(setContextOp(ctx, tueq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{toolusageevent.Label}
	default:
		return nil, &NotSingularError{toolusageevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) OnlyX(ctx context.Context) *ToolUsageEvent {
	node, err := tueq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ToolUsageEvent ID in the query.
// Returns a *NotSingularError when more than one ToolUsageEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (tueq *ToolUsageEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tueq.Limit(2).IDs(setContextOp(ctx, tueq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{toolusageevent.Label}
	default:
		err = &NotSingularError{toolusageevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := tueq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ToolUsageEvents.
func (tueq *ToolUsageEventQuery) All(ctx context.Context) ([]*ToolUsageEvent, error) {
	ctx = setContextOp(ctx, tueq.ctx, ent.OpQueryAll)
	if err := tueq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ToolUsageEvent, *ToolUsageEventQuery]()
	return withInterceptors[[]*ToolUsageEvent](ctx, tueq, qr, tueq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) AllX(ctx context.Context) []*ToolUsageEvent {
	nodes, err := tueq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ToolUsageEvent IDs.
func (tueq *ToolUsageEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tueq.ctx.Unique == nil && tueq.path != nil {
		tueq.Unique(true)
	}
	ctx = setContextOp(ctx, tueq.ctx, ent.OpQueryIDs)
	if err = tueq.Select(toolusageevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) IDsX(ctx context.Context) []int {
	ids, err := tueq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tueq *ToolUsageEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tueq.ctx, ent.OpQueryCount)
	if err := tueq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tueq, querierCount[*ToolUsageEventQuery](), tueq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) CountX(ctx context.Context) int {
	count, err := tueq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tueq *ToolUsageEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tueq.ctx, ent.OpQueryExist)
	switch _, err := tueq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tueq *ToolUsageEventQuery) ExistX(ctx context.Context) bool {
	exist, err := tueq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ToolUsageEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tueq *ToolUsageEventQuery) Clone() *ToolUsageEventQuery {
	if tueq == nil {
		return nil
	}
	return &ToolUsageEventQuery{
		config:               tueq.config,
		ctx:                  tueq.ctx.Clone(),
		order:                append([]toolusageevent.OrderOption{}, tueq.order...),
		inters:               append([]Interceptor{}, tueq.inters...),
		predicates:           append([]predicate.ToolUsageEvent{}, tueq.predicates...),
		withRepoConfig:       tueq.withRepoConfig.Clone(),
		withUser:             tueq.withUser.Clone(),
		withCommitCheckpoint: tueq.withCommitCheckpoint.Clone(),
		// clone intermediate query.
		sql:  tueq.sql.Clone(),
		path: tueq.path,
	}
}

// WithRepoConfig tells the query-builder to eager-load the nodes that are connected to
// the "repo_config" edge. The optional arguments are used to configure the query builder of the edge.
func (tueq *ToolUsageEventQuery) WithRepoConfig(opts ...func(*RepoConfigQuery)) *ToolUsageEventQuery {
	query := (&RepoConfigClient{config: tueq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tueq.withRepoConfig = query
	return tueq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (tueq *ToolUsageEventQuery) WithUser(opts ...func(*UserQuery)) *ToolUsageEventQuery {
	query := (&UserClient{config: tueq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tueq.withUser = query
	return tueq
}

// WithCommitCheckpoint tells the query-builder to eager-load the nodes that are connected to
// the "commit_checkpoint" edge. The optional arguments are used to configure the query builder of the edge.
func (tueq *ToolUsageEventQuery) WithCommitCheckpoint(opts ...func(*CommitCheckpointQuery)) *ToolUsageEventQuery {
	query := (&CommitCheckpointClient{config: tueq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tueq.withCommitCheckpoint = query
	return tueq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Tool string `json:"tool,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ToolUsageEvent.Query().
//		GroupBy(toolusageevent.FieldTool).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tueq *ToolUsageEventQuery) GroupBy(field string, fields ...string) *ToolUsageEventGroupBy {
	tueq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ToolUsageEventGroupBy{build: tueq}
	grbuild.flds = &tueq.ctx.Fields
	grbuild.label = toolusageevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Tool string `json:"tool,omitempty"`
//	}
//
//	client.ToolUsageEvent.Query().
//		Select(toolusageevent.FieldTool).
//		Scan(ctx, &v)
func (tueq *ToolUsageEventQuery) Select(fields ...string) *ToolUsageEventSelect {
	tueq.ctx.Fields = append(tueq.ctx.Fields, fields...)
	sbuild := &ToolUsageEventSelect{ToolUsageEventQuery: tueq}
	sbuild.label = toolusageevent.Label
	sbuild.flds, sbuild.scan = &tueq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ToolUsageEventSelect configured with the given aggregations.
func (tueq *ToolUsageEventQuery) Aggregate(fns ...AggregateFunc) *ToolUsageEventSelect {
	return tueq.Select().Aggregate(fns...)
}

func (tueq *ToolUsageEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tueq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tueq); err != nil {
				return err
			}
		}
	}
	for _, f := range tueq.ctx.Fields {
		if !toolusageevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tueq.path != nil {
		prev, err := tueq.path(ctx)
		if err != nil {
			return err
		}
		tueq.sql = prev
	}
	return nil
}

func (tueq *ToolUsageEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ToolUsageEvent, error) {
	var (
		nodes       = []*ToolUsageEvent{}
		_spec       = tueq.querySpec()
		loadedTypes = [3]bool{
			tueq.withRepoConfig != nil,
			tueq.withUser != nil,
			tueq.withCommitCheckpoint != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ToolUsageEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ToolUsageEvent{config: tueq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tueq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tueq.withRepoConfig; query != nil {
		if err := tueq.loadRepoConfig(ctx, query, nodes, nil,
			func(n *ToolUsageEvent, e *RepoConfig) { n.Edges.RepoConfig = e }); err != nil {
			return nil, err
		}
	}
	if query := tueq.withUser; query != nil {
		if err := tueq.loadUser(ctx, query, nodes, nil,
			func(n *ToolUsageEvent, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := tueq.withCommitCheckpoint; query != nil {
		if err := tueq.loadCommitCheckpoint(ctx, query, nodes, nil,
			func(n *ToolUsageEvent, e *CommitCheckpoint) { n.Edges.CommitCheckpoint = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tueq *ToolUsageEventQuery) loadRepoConfig(ctx context.Context, query *RepoConfigQuery, nodes []*ToolUsageEvent, init func(*ToolUsageEvent), assign func(*ToolUsageEvent, *RepoConfig)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ToolUsageEvent)
	for i := range nodes {
		fk := nodes[i].RepoConfigID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(repoconfig.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "repo_config_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (tueq *ToolUsageEventQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ToolUsageEvent, init func(*ToolUsageEvent), assign func(*ToolUsageEvent, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ToolUsageEvent)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (tueq *ToolUsageEventQuery) loadCommitCheckpoint(ctx context.Context, query *CommitCheckpointQuery, nodes []*ToolUsageEvent, init func(*ToolUsageEvent), assign func(*ToolUsageEvent, *CommitCheckpoint)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ToolUsageEvent)
	for i := range nodes {
		if nodes[i].CommitCheckpointID == nil {
			continue
		}
		fk := *nodes[i].CommitCheckpointID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(commitcheckpoint.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "commit_checkpoint_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tueq *ToolUsageEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tueq.querySpec()
	_spec.Node.Columns = tueq.ctx.Fields
	if len(tueq.ctx.Fields) > 0 {
		_spec.Unique = tueq.ctx.Unique != nil && *tueq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tueq.driver, _spec)
}

func (tueq *ToolUsageEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(toolusageevent.Table, toolusageevent.Columns, sqlgraph.NewFieldSpec(toolusageevent.FieldID, field.TypeInt))
	_spec.From = tueq.sql
	if unique := tueq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tueq.path != nil {
		_spec.Unique = true
	}
	if fields := tueq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, toolusageevent.FieldID)
		for i := range fields {
			if fields[i] != toolusageevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if tueq.withRepoConfig != nil {
			_spec.Node.AddColumnOnce(toolusageevent.FieldRepoConfigID)
		}
		if tueq.withUser != nil {
			_spec.Node.AddColumnOnce(toolusageevent.FieldUserID)
		}
		if tueq.withCommitCheckpoint != nil {
			_spec.Node.AddColumnOnce(toolusageevent.FieldCommitCheckpointID)
		}
	}
	if ps := tueq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tueq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tueq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tueq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tueq *ToolUsageEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tueq.driver.Dialect())
	t1 := builder.Table(toolusageevent.Table)
	columns := tueq.ctx.Fields
	if len(columns) == 0 {
		columns = toolusageevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tueq.sql != nil {
		selector = tueq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tueq.ctx.Unique != nil && *tueq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tueq.predicates {
		p(selector)
	}
	for _, p := range tueq.order {
		p(selector)
	}
	if offset := tueq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tueq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ToolUsageEventGroupBy is the group-by builder for ToolUsageEvent entities.
type ToolUsageEventGroupBy struct {
	selector
	build *ToolUsageEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tuegb *ToolUsageEventGroupBy) Aggregate(fns ...AggregateFunc) *ToolUsageEventGroupBy {
	tuegb.fns = append(tuegb.fns, fns...)
	return tuegb
}

// Scan applies the selector query and scans the result into the given value.
func (tuegb *ToolUsageEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tuegb.build.ctx, ent.OpQueryGroupBy)
	if err := tuegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ToolUsageEventQuery, *ToolUsageEventGroupBy](ctx, tuegb.build, tuegb, tuegb.build.inters, v)
}

func (tuegb *ToolUsageEventGroupBy) sqlScan(ctx context.Context, root *ToolUsageEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tuegb.fns))
	for _, fn := range tuegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tuegb.flds)+len(tuegb.fns))
		for _, f := range *tuegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tuegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tuegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ToolUsageEventSelect is the builder for selecting fields of ToolUsageEvent entities.
type ToolUsageEventSelect struct {
	*ToolUsageEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tues *ToolUsageEventSelect) Aggregate(fns ...AggregateFunc) *ToolUsageEventSelect {
	tues.fns = append(tues.fns, fns...)
	return tues
}

// Scan applies the selector query and scans the result into the given value.
func (tues *ToolUsageEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tues.ctx, ent.OpQuerySelect)
	if err := tues.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ToolUsageEventQuery, *ToolUsageEventSelect](ctx, tues.ToolUsageEventQuery, tues, tues.inters, v)
}

func (tues *ToolUsageEventSelect) sqlScan(ctx context.Context, root *ToolUsageEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tues.fns))
	for _, fn := range tues.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tues.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tues.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
