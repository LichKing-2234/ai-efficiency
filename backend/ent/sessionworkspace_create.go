// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/session"
	"github.com/ai-efficiency/backend/ent/sessionworkspace"
	"github.com/google/uuid"
)

// SessionWorkspaceCreate is the builder for creating a SessionWorkspace entity.
type SessionWorkspaceCreate struct {
	config
	mutation *SessionWorkspaceMutation
	hooks    []Hook
}

// SetSessionID sets the "session_id" field.
func (swc *SessionWorkspaceCreate) SetSessionID(u uuid.UUID) *SessionWorkspaceCreate {
	swc.mutation.SetSessionID(u)
	return swc
}

// SetWorkspaceID sets the "workspace_id" field.
func (swc *SessionWorkspaceCreate) SetWorkspaceID(s string) *SessionWorkspaceCreate {
	swc.mutation.SetWorkspaceID(s)
	return swc
}

// SetWorkspaceRoot sets the "workspace_root" field.
func (swc *SessionWorkspaceCreate) SetWorkspaceRoot(s string) *SessionWorkspaceCreate {
	swc.mutation.SetWorkspaceRoot(s)
	return swc
}

// SetGitDir sets the "git_dir" field.
func (swc *SessionWorkspaceCreate) SetGitDir(s string) *SessionWorkspaceCreate {
	swc.mutation.SetGitDir(s)
	return swc
}

// SetGitCommonDir sets the "git_common_dir" field.
func (swc *SessionWorkspaceCreate) SetGitCommonDir(s string) *SessionWorkspaceCreate {
	swc.mutation.SetGitCommonDir(s)
	return swc
}

// SetFirstSeenAt sets the "first_seen_at" field.
func (swc *SessionWorkspaceCreate) SetFirstSeenAt(t time.Time) *SessionWorkspaceCreate {
	swc.mutation.SetFirstSeenAt(t)
	return swc
}

// SetNillableFirstSeenAt sets the "first_seen_at" field if the given value is not nil.
func (swc *SessionWorkspaceCreate) SetNillableFirstSeenAt(t *time.Time) *SessionWorkspaceCreate {
	if t != nil {
		swc.SetFirstSeenAt(*t)
	}
	return swc
}

// SetLastSeenAt sets the "last_seen_at" field.
func (swc *SessionWorkspaceCreate) SetLastSeenAt(t time.Time) *SessionWorkspaceCreate {
	swc.mutation.SetLastSeenAt(t)
	return swc
}

// SetNillableLastSeenAt sets the "last_seen_at" field if the given value is not nil.
func (swc *SessionWorkspaceCreate) SetNillableLastSeenAt(t *time.Time) *SessionWorkspaceCreate {
	if t != nil {
		swc.SetLastSeenAt(*t)
	}
	return swc
}

// SetBindingSource sets the "binding_source" field.
func (swc *SessionWorkspaceCreate) SetBindingSource(ss sessionworkspace.BindingSource) *SessionWorkspaceCreate {
	swc.mutation.SetBindingSource(ss)
	return swc
}

// SetSession sets the "session" edge to the Session entity.
func (swc *SessionWorkspaceCreate) SetSession(s *Session) *SessionWorkspaceCreate {
	return swc.SetSessionID(s.ID)
}

// Mutation returns the SessionWorkspaceMutation object of the builder.
func (swc *SessionWorkspaceCreate) Mutation() *SessionWorkspaceMutation {
	return swc.mutation
}

// Save creates the SessionWorkspace in the database.
func (swc *SessionWorkspaceCreate) Save(ctx context.Context) (*SessionWorkspace, error) {
	swc.defaults()
	return withHooks(ctx, swc.sqlSave, swc.mutation, swc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (swc *SessionWorkspaceCreate) SaveX(ctx context.Context) *SessionWorkspace {
	v, err := swc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (swc *SessionWorkspaceCreate) Exec(ctx context.Context) error {
	_, err := swc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (swc *SessionWorkspaceCreate) ExecX(ctx context.Context) {
	if err := swc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (swc *SessionWorkspaceCreate) defaults() {
	if _, ok := swc.mutation.FirstSeenAt(); !ok {
		v := sessionworkspace.DefaultFirstSeenAt()
		swc.mutation.SetFirstSeenAt(v)
	}
	if _, ok := swc.mutation.LastSeenAt(); !ok {
		v := sessionworkspace.DefaultLastSeenAt()
		swc.mutation.SetLastSeenAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (swc *SessionWorkspaceCreate) check() error {
	if _, ok := swc.mutation.SessionID(); !ok {
		return &ValidationError{Name: "session_id", err: errors.New(`ent: missing required field "SessionWorkspace.session_id"`)}
	}
	if _, ok := swc.mutation.WorkspaceID(); !ok {
		return &ValidationError{Name: "workspace_id", err: errors.New(`ent: missing required field "SessionWorkspace.workspace_id"`)}
	}
	if v, ok := swc.mutation.WorkspaceID(); ok {
		if err := sessionworkspace.WorkspaceIDValidator(v); err != nil {
			return &ValidationError{Name: "workspace_id", err: fmt.Errorf(`ent: validator failed for field "SessionWorkspace.workspace_id": %w`, err)}
		}
	}
	if _, ok := swc.mutation.WorkspaceRoot(); !ok {
		return &ValidationError{Name: "workspace_root", err: errors.New(`ent: missing required field "SessionWorkspace.workspace_root"`)}
	}
	if v, ok := swc.mutation.WorkspaceRoot(); ok {
		if err := sessionworkspace.WorkspaceRootValidator(v); err != nil {
			return &ValidationError{Name: "workspace_root", err: fmt.Errorf(`ent: validator failed for field "SessionWorkspace.workspace_root": %w`, err)}
		}
	}
	if _, ok := swc.mutation.GitDir(); !ok {
		return &ValidationError{Name: "git_dir", err: errors.New(`ent: missing required field "SessionWorkspace.git_dir"`)}
	}
	if v, ok := swc.mutation.GitDir(); ok {
		if err := sessionworkspace.GitDirValidator(v); err != nil {
			return &ValidationError{Name: "git_dir", err: fmt.Errorf(`ent: validator failed for field "SessionWorkspace.git_dir": %w`, err)}
		}
	}
	if _, ok := swc.mutation.GitCommonDir(); !ok {
		return &ValidationError{Name: "git_common_dir", err: errors.New(`ent: missing required field "SessionWorkspace.git_common_dir"`)}
	}
	if v, ok := swc.mutation.GitCommonDir(); ok {
		if err := sessionworkspace.GitCommonDirValidator(v); err != nil {
			return &ValidationError{Name: "git_common_dir", err: fmt.Errorf(`ent: validator failed for field "SessionWorkspace.git_common_dir": %w`, err)}
		}
	}
	if _, ok := swc.mutation.FirstSeenAt(); !ok {
		return &ValidationError{Name: "first_seen_at", err: errors.New(`ent: missing required field "SessionWorkspace.first_seen_at"`)}
	}
	if _, ok := swc.mutation.LastSeenAt(); !ok {
		return &ValidationError{Name: "last_seen_at", err: errors.New(`ent: missing required field "SessionWorkspace.last_seen_at"`)}
	}
	if _, ok := swc.mutation.BindingSource(); !ok {
		return &ValidationError{Name: "binding_source", err: errors.New(`ent: missing required field "SessionWorkspace.binding_source"`)}
	}
	if v, ok := swc.mutation.BindingSource(); ok {
		if err := sessionworkspace.BindingSourceValidator(v); err != nil {
			return &ValidationError{Name: "binding_source", err: fmt.Errorf(`ent: validator failed for field "SessionWorkspace.binding_source": %w`, err)}
		}
	}
	if len(swc.mutation.SessionIDs()) == 0 {
		return &ValidationError{Name: "session", err: errors.New(`ent: missing required edge "SessionWorkspace.session"`)}
	}
	return nil
}

func (swc *SessionWorkspaceCreate) sqlSave(ctx context.Context) (*SessionWorkspace, error) {
	if err := swc.check(); err != nil {
		return nil, err
	}
	_node, _spec := swc.createSpec()
	if err := sqlgraph.CreateNode(ctx, swc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	swc.mutation.id = &_node.ID
	swc.mutation.done = true
	return _node, nil
}

func (swc *SessionWorkspaceCreate) createSpec() (*SessionWorkspace, *sqlgraph.CreateSpec) {
	var (
		_node = &SessionWorkspace{config: swc.config}
		_spec = sqlgraph.NewCreateSpec(sessionworkspace.Table, sqlgraph.NewFieldSpec(sessionworkspace.FieldID, field.TypeInt))
	)
	if value, ok := swc.mutation.WorkspaceID(); ok {
		_spec.SetField(sessionworkspace.FieldWorkspaceID, field.TypeString, value)
		_node.WorkspaceID = value
	}
	if value, ok := swc.mutation.WorkspaceRoot(); ok {
		_spec.SetField(sessionworkspace.FieldWorkspaceRoot, field.TypeString, value)
		_node.WorkspaceRoot = value
	}
	if value, ok := swc.mutation.GitDir(); ok {
		_spec.SetField(sessionworkspace.FieldGitDir, field.TypeString, value)
		_node.GitDir = value
	}
	if value, ok := swc.mutation.GitCommonDir(); ok {
		_spec.SetField(sessionworkspace.FieldGitCommonDir, field.TypeString, value)
		_node.GitCommonDir = value
	}
	if value, ok := swc.mutation.FirstSeenAt(); ok {
		_spec.SetField(sessionworkspace.FieldFirstSeenAt, field.TypeTime, value)
		_node.FirstSeenAt = value
	}
	if value, ok := swc.mutation.LastSeenAt(); ok {
		_spec.SetField(sessionworkspace.FieldLastSeenAt, field.TypeTime, value)
		_node.LastSeenAt = value
	}
	if value, ok := swc.mutation.BindingSource(); ok {
		_spec.SetField(sessionworkspace.FieldBindingSource, field.TypeEnum, value)
		_node.BindingSource = value
	}
	if nodes := swc.mutation.SessionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   sessionworkspace.SessionTable,
			Columns: []string{sessionworkspace.SessionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(session.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.SessionID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// SessionWorkspaceCreateBulk is the builder for creating many SessionWorkspace entities in bulk.
type SessionWorkspaceCreateBulk struct {
	config
	err      error
	builders []*SessionWorkspaceCreate
}

// Save creates the SessionWorkspace entities in the database.
func (swcb *SessionWorkspaceCreateBulk) Save(ctx context.Context) ([]*SessionWorkspace, error) {
	if swcb.err != nil {
		return nil, swcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(swcb.builders))
	nodes := make([]*SessionWorkspace, len(swcb.builders))
	mutators := make([]Mutator, len(swcb.builders))
	for i := range swcb.builders {
		func(i int, root context.Context) {
			builder := swcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SessionWorkspaceMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, swcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, swcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, swcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (swcb *SessionWorkspaceCreateBulk) SaveX(ctx context.Context) []*SessionWorkspace {
	v, err := swcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (swcb *SessionWorkspaceCreateBulk) Exec(ctx context.Context) error {
	_, err := swcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (swcb *SessionWorkspaceCreateBulk) ExecX(ctx context.Context) {
	if err := swcb.Exec(ctx); err != nil {
		panic(err)
	}
}
