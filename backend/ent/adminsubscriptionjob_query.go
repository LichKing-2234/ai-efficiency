// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/adminsubscriptionjob"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// AdminSubscriptionJobQuery is the builder for querying AdminSubscriptionJob entities.
type AdminSubscriptionJobQuery struct {
	config
	ctx        *QueryContext
	order      []adminsubscriptionjob.OrderOption
	inters     []Interceptor
	predicates []predicate.AdminSubscriptionJob
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AdminSubscriptionJobQuery builder.
func (asjq *AdminSubscriptionJobQuery) Where(ps ...predicate.AdminSubscriptionJob) *AdminSubscriptionJobQuery {
	asjq.predicates = append(asjq.predicates, ps...)
	return asjq
}

// Limit the number of records to be returned by this query.
func (asjq *AdminSubscriptionJobQuery) Limit(limit int) *AdminSubscriptionJobQuery {
	asjq.ctx.Limit = &limit
	return asjq
}

// Offset to start from.
func (asjq *AdminSubscriptionJobQuery) Offset(offset int) *AdminSubscriptionJobQuery {
	asjq.ctx.Offset = &offset
	return asjq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (asjq *AdminSubscriptionJobQuery) Unique(unique bool) *AdminSubscriptionJobQuery {
	asjq.ctx.Unique = &unique
	return asjq
}

// Order specifies how the records should be ordered.
func (asjq *AdminSubscriptionJobQuery) Order(o ...adminsubscriptionjob.OrderOption) *AdminSubscriptionJobQuery {
	asjq.order = append(asjq.order, o...)
	return asjq
}

// First returns the first AdminSubscriptionJob entity from the query.
// Returns a *NotFoundError when no AdminSubscriptionJob was found.
func (asjq *AdminSubscriptionJobQuery) First(ctx context.Context) (*AdminSubscriptionJob, error) {
	nodes, err := asjq.Limit(1).All(setContextOp(ctx, asjq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{adminsubscriptionjob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) FirstX(ctx context.Context) *AdminSubscriptionJob {
	node, err := asjq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AdminSubscriptionJob ID from the query.
// Returns a *NotFoundError when no AdminSubscriptionJob ID was found.
func (asjq *AdminSubscriptionJobQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = asjq.Limit(1).IDs(setContextOp(ctx, asjq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{adminsubscriptionjob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) FirstIDX(ctx context.Context) int {
	id, err := asjq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AdminSubscriptionJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AdminSubscriptionJob entity is found.
// Returns a *NotFoundError when no AdminSubscriptionJob entities are found.
func (asjq *AdminSubscriptionJobQuery) Only(ctx context.Context) (*AdminSubscriptionJob, error) {
	nodes, err := asjq.Limit(2).All(setContextOp(ctx, asjq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{adminsubscriptionjob.Label}
	default:
		return nil, &NotSingularError{adminsubscriptionjob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) OnlyX(ctx context.Context) *AdminSubscriptionJob {
	node, err := asjq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AdminSubscriptionJob ID in the query.
// Returns a *NotSingularError when more than one AdminSubscriptionJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (asjq *AdminSubscriptionJobQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = asjq.Limit(2).IDs(setContextOp(ctx, asjq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{adminsubscriptionjob.Label}
	default:
		err = &NotSingularError{adminsubscriptionjob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) OnlyIDX(ctx context.Context) int {
	id, err := asjq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AdminSubscriptionJobs.
func (asjq *AdminSubscriptionJobQuery) All(ctx context.Context) ([]*AdminSubscriptionJob, error) {
	ctx = setContextOp(ctx, asjq.ctx, ent.OpQueryAll)
	if err := asjq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AdminSubscriptionJob, *AdminSubscriptionJobQuery]()
	return withInterceptors[[]*AdminSubscriptionJob](ctx, asjq, qr, asjq.inters)
}

// AllX is like All, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) AllX(ctx context.Context) []*AdminSubscriptionJob {
	nodes, err := asjq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AdminSubscriptionJob IDs.
func (asjq *AdminSubscriptionJobQuery) IDs(ctx context.Context) (ids []int, err error) {
	if asjq.ctx.Unique == nil && asjq.path != nil {
		asjq.Unique(true)
	}
	ctx = setContextOp(ctx, asjq.ctx, ent.OpQueryIDs)
	if err = asjq.Select(adminsubscriptionjob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) IDsX(ctx context.Context) []int {
	ids, err := asjq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (asjq *AdminSubscriptionJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, asjq.ctx, ent.OpQueryCount)
	if err := asjq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, asjq, querierCount[*AdminSubscriptionJobQuery](), asjq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) CountX(ctx context.Context) int {
	count, err := asjq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (asjq *AdminSubscriptionJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, asjq.ctx, ent.OpQueryExist)
	switch _, err := asjq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (asjq *AdminSubscriptionJobQuery) ExistX(ctx context.Context) bool {
	exist, err := asjq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AdminSubscriptionJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (asjq *AdminSubscriptionJobQuery) Clone() *AdminSubscriptionJobQuery {
	if asjq == nil {
		return nil
	}
	return &AdminSubscriptionJobQuery{
		config:     asjq.config,
		ctx:        asjq.ctx.Clone(),
		order:      append([]adminsubscriptionjob.OrderOption{}, asjq.order...),
		inters:     append([]Interceptor{}, asjq.inters...),
		predicates: append([]predicate.AdminSubscriptionJob{}, asjq.predicates...),
		// clone intermediate query.
		sql:  asjq.sql.Clone(),
		path: asjq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status adminsubscriptionjob.Status `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AdminSubscriptionJob.Query().
//		GroupBy(adminsubscriptionjob.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (asjq *AdminSubscriptionJobQuery) GroupBy(field string, fields ...string) *AdminSubscriptionJobGroupBy {
	asjq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AdminSubscriptionJobGroupBy{build: asjq}
	grbuild.flds = &asjq.ctx.Fields
	grbuild.label = adminsubscriptionjob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status adminsubscriptionjob.Status `json:"status,omitempty"`
//	}
//
//	client.AdminSubscriptionJob.Query().
//		Select(adminsubscriptionjob.FieldStatus).
//		Scan(ctx, &v)
func (asjq *AdminSubscriptionJobQuery) Select(fields ...string) *AdminSubscriptionJobSelect {
	asjq.ctx.Fields = append(asjq.ctx.Fields, fields...)
	sbuild := &AdminSubscriptionJobSelect{AdminSubscriptionJobQuery: asjq}
	sbuild.label = adminsubscriptionjob.Label
	sbuild.flds, sbuild.scan = &asjq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AdminSubscriptionJobSelect configured with the given aggregations.
func (asjq *AdminSubscriptionJobQuery) Aggregate(fns ...AggregateFunc) *AdminSubscriptionJobSelect {
	return asjq.Select().Aggregate(fns...)
}

func (asjq *AdminSubscriptionJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range asjq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, asjq); err != nil {
				return err
			}
		}
	}
	for _, f := range asjq.ctx.Fields {
		if !adminsubscriptionjob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if asjq.path != nil {
		prev, err := asjq.path(ctx)
		if err != nil {
			return err
		}
		asjq.sql = prev
	}
	return nil
}

func (asjq *AdminSubscriptionJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AdminSubscriptionJob, error) {
	var (
		nodes = []*AdminSubscriptionJob{}
		_spec = asjq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AdminSubscriptionJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AdminSubscriptionJob{config: asjq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, asjq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (asjq *AdminSubscriptionJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := asjq.querySpec()
	_spec.Node.Columns = asjq.ctx.Fields
	if len(asjq.ctx.Fields) > 0 {
		_spec.Unique = asjq.ctx.Unique != nil && *asjq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, asjq.driver, _spec)
}

func (asjq *AdminSubscriptionJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(adminsubscriptionjob.Table, adminsubscriptionjob.Columns, sqlgraph.NewFieldSpec(adminsubscriptionjob.FieldID, field.TypeInt))
	_spec.From = asjq.sql
	if unique := asjq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if asjq.path != nil {
		_spec.Unique = true
	}
	if fields := asjq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, adminsubscriptionjob.FieldID)
		for i := range fields {
			if fields[i] != adminsubscriptionjob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := asjq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := asjq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := asjq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := asjq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (asjq *AdminSubscriptionJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(asjq.driver.Dialect())
	t1 := builder.Table(adminsubscriptionjob.Table)
	columns := asjq.ctx.Fields
	if len(columns) == 0 {
		columns = adminsubscriptionjob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if asjq.sql != nil {
		selector = asjq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if asjq.ctx.Unique != nil && *asjq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range asjq.predicates {
		p(selector)
	}
	for _, p := range asjq.order {
		p(selector)
	}
	if offset := asjq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := asjq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AdminSubscriptionJobGroupBy is the group-by builder for AdminSubscriptionJob entities.
type AdminSubscriptionJobGroupBy struct {
	selector
	build *AdminSubscriptionJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (asjgb *AdminSubscriptionJobGroupBy) Aggregate(fns ...AggregateFunc) *AdminSubscriptionJobGroupBy {
	asjgb.fns = append(asjgb.fns, fns...)
	return asjgb
}

// Scan applies the selector query and scans the result into the given value.
func (asjgb *AdminSubscriptionJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asjgb.build.ctx, ent.OpQueryGroupBy)
	if err := asjgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdminSubscriptionJobQuery, *AdminSubscriptionJobGroupBy](ctx, asjgb.build, asjgb, asjgb.build.inters, v)
}

func (asjgb *AdminSubscriptionJobGroupBy) sqlScan(ctx context.Context, root *AdminSubscriptionJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(asjgb.fns))
	for _, fn := range asjgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*asjgb.flds)+len(asjgb.fns))
		for _, f := range *asjgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*asjgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asjgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AdminSubscriptionJobSelect is the builder for selecting fields of AdminSubscriptionJob entities.
type AdminSubscriptionJobSelect struct {
	*AdminSubscriptionJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (asjs *AdminSubscriptionJobSelect) Aggregate(fns ...AggregateFunc) *AdminSubscriptionJobSelect {
	asjs.fns = append(asjs.fns, fns...)
	return asjs
}

// Scan applies the selector query and scans the result into the given value.
func (asjs *AdminSubscriptionJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asjs.ctx, ent.OpQuerySelect)
	if err := asjs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AdminSubscriptionJobQuery, *AdminSubscriptionJobSelect](ctx, asjs.AdminSubscriptionJobQuery, asjs, asjs.inters, v)
}

func (asjs *AdminSubscriptionJobSelect) sqlScan(ctx context.Context, root *AdminSubscriptionJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(asjs.fns))
	for _, fn := range asjs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*asjs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asjs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
