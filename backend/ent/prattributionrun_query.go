// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/prattributionrun"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/prrecord"
)

// PrAttributionRunQuery is the builder for querying PrAttributionRun entities.
type PrAttributionRunQuery struct {
	config
	ctx          *QueryContext
	order        []prattributionrun.OrderOption
	inters       []Interceptor
	predicates   []predicate.PrAttributionRun
	withPrRecord *PrRecordQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PrAttributionRunQuery builder.
func (parq *PrAttributionRunQuery) Where(ps ...predicate.PrAttributionRun) *PrAttributionRunQuery {
	parq.predicates = append(parq.predicates, ps...)
	return parq
}

// Limit the number of records to be returned by this query.
func (parq *PrAttributionRunQuery) Limit(limit int) *PrAttributionRunQuery {
	parq.ctx.Limit = &limit
	return parq
}

// Offset to start from.
func (parq *PrAttributionRunQuery) Offset(offset int) *PrAttributionRunQuery {
	parq.ctx.Offset = &offset
	return parq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (parq *PrAttributionRunQuery) Unique(unique bool) *PrAttributionRunQuery {
	parq.ctx.Unique = &unique
	return parq
}

// Order specifies how the records should be ordered.
func (parq *PrAttributionRunQuery) Order(o ...prattributionrun.OrderOption) *PrAttributionRunQuery {
	parq.order = append(parq.order, o...)
	return parq
}

// QueryPrRecord chains the current query on the "pr_record" edge.
func (parq *PrAttributionRunQuery) QueryPrRecord() *PrRecordQuery {
	query := (&PrRecordClient{config: parq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := parq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := parq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(prattributionrun.Table, prattributionrun.FieldID, selector),
			sqlgraph.To(prrecord.Table, prrecord.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, prattributionrun.PrRecordTable, prattributionrun.PrRecordColumn),
		)
		fromU = sqlgraph.SetNeighbors(parq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PrAttributionRun entity from the query.
// Returns a *NotFoundError when no PrAttributionRun was found.
func (parq *PrAttributionRunQuery) First(ctx context.Context) (*PrAttributionRun, error) {
	nodes, err := parq.Limit(1).All(setContextOp(ctx, parq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{prattributionrun.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (parq *PrAttributionRunQuery) FirstX(ctx context.Context) *PrAttributionRun {
	node, err := parq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PrAttributionRun ID from the query.
// Returns a *NotFoundError when no PrAttributionRun ID was found.
func (parq *PrAttributionRunQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = parq.Limit(1).IDs(setContextOp(ctx, parq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{prattributionrun.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (parq *PrAttributionRunQuery) FirstIDX(ctx context.Context) int {
	id, err := parq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PrAttributionRun entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PrAttributionRun entity is found.
// Returns a *NotFoundError when no PrAttributionRun entities are found.
func (parq *PrAttributionRunQuery) Only(ctx context.Context) (*PrAttributionRun, error) {
	nodes, err := parq.Limit(2).All(setContextOp(ctx, parq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{prattributionrun.Label}
	default:
		return nil, &NotSingularError{prattributionrun.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (parq *PrAttributionRunQuery) OnlyX(ctx context.Context) *PrAttributionRun {
	node, err := parq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PrAttributionRun ID in the query.
// Returns a *NotSingularError when more than one PrAttributionRun ID is found.
// Returns a *NotFoundError when no entities are found.
func (parq *PrAttributionRunQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = parq.Limit(2).IDs(setContextOp(ctx, parq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{prattributionrun.Label}
	default:
		err = &NotSingularError{prattributionrun.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (parq *PrAttributionRunQuery) OnlyIDX(ctx context.Context) int {
	id, err := parq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PrAttributionRuns.
func (parq *PrAttributionRunQuery) All(ctx context.Context) ([]*PrAttributionRun, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryAll)
	if err := parq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PrAttributionRun, *PrAttributionRunQuery]()
	return withInterceptors[[]*PrAttributionRun](ctx, parq, qr, parq.inters)
}

// AllX is like All, but panics if an error occurs.
func (parq *PrAttributionRunQuery) AllX(ctx context.Context) []*PrAttributionRun {
	nodes, err := parq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PrAttributionRun IDs.
func (parq *PrAttributionRunQuery) IDs(ctx context.Context) (ids []int, err error) {
	if parq.ctx.Unique == nil && parq.path != nil {
		parq.Unique(true)
	}
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryIDs)
	if err = parq.Select(prattributionrun.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (parq *PrAttributionRunQuery) IDsX(ctx context.Context) []int {
	ids, err := parq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (parq *PrAttributionRunQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryCount)
	if err := parq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, parq, querierCount[*PrAttributionRunQuery](), parq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (parq *PrAttributionRunQuery) CountX(ctx context.Context) int {
	count, err := parq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (parq *PrAttributionRunQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryExist)
	switch _, err := parq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (parq *PrAttributionRunQuery) ExistX(ctx context.Context) bool {
	exist, err := parq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PrAttributionRunQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (parq *PrAttributionRunQuery) Clone() *PrAttributionRunQuery {
	if parq == nil {
		return nil
	}
	return &PrAttributionRunQuery{
		config:       parq.config,
		ctx:          parq.ctx.Clone(),
		order:        append([]prattributionrun.OrderOption{}, parq.order...),
		inters:       append([]Interceptor{}, parq.inters...),
		predicates:   append([]predicate.PrAttributionRun{}, parq.predicates...),
		withPrRecord: parq.withPrRecord.Clone(),
		// clone intermediate query.
		sql:  parq.sql.Clone(),
		path: parq.path,
	}
}

// WithPrRecord tells the query-builder to eager-load the nodes that are connected to
// the "pr_record" edge. The optional arguments are used to configure the query builder of the edge.
func (parq *PrAttributionRunQuery) WithPrRecord(opts ...func(*PrRecordQuery)) *PrAttributionRunQuery {
	query := (&PrRecordClient{config: parq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	parq.withPrRecord = query
	return parq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PrRecordID int `json:"pr_record_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PrAttributionRun.Query().
//		GroupBy(prattributionrun.FieldPrRecordID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (parq *PrAttributionRunQuery) GroupBy(field string, fields ...string) *PrAttributionRunGroupBy {
	parq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PrAttributionRunGroupBy{build: parq}
	grbuild.flds = &parq.ctx.Fields
	grbuild.label = prattributionrun.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PrRecordID int `json:"pr_record_id,omitempty"`
//	}
//
//	client.PrAttributionRun.Query().
//		Select(prattributionrun.FieldPrRecordID).
//		Scan(ctx, &v)
func (parq *PrAttributionRunQuery) Select(fields ...string) *PrAttributionRunSelect {
	parq.ctx.Fields = append(parq.ctx.Fields, fields...)
	sbuild := &PrAttributionRunSelect{PrAttributionRunQuery: parq}
	sbuild.label = prattributionrun.Label
	sbuild.flds, sbuild.scan = &parq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PrAttributionRunSelect configured with the given aggregations.
func (parq *PrAttributionRunQuery) Aggregate(fns ...AggregateFunc) *PrAttributionRunSelect {
	return parq.Select().Aggregate(fns...)
}

func (parq *PrAttributionRunQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range parq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, parq); err != nil {
				return err
			}
		}
	}
	for _, f := range parq.ctx.Fields {
		if !prattributionrun.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if parq.path != nil {
		prev, err := parq.path(ctx)
		if err != nil {
			return err
		}
		parq.sql = prev
	}
	return nil
}

func (parq *PrAttributionRunQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PrAttributionRun, error) {
	var (
		nodes       = []*PrAttributionRun{}
		_spec       = parq.querySpec()
		loadedTypes = [1]bool{
			parq.withPrRecord != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PrAttributionRun).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PrAttributionRun{config: parq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, parq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := parq.withPrRecord; query != nil {
		if err := parq.loadPrRecord(ctx, query, nodes, nil,
			func(n *PrAttributionRun, e *PrRecord) { n.Edges.PrRecord = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (parq *PrAttributionRunQuery) loadPrRecord(ctx context.Context, query *PrRecordQuery, nodes []*PrAttributionRun, init func(*PrAttributionRun), assign func(*PrAttributionRun, *PrRecord)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PrAttributionRun)
	for i := range nodes {
		fk := nodes[i].PrRecordID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(prrecord.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "pr_record_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (parq *PrAttributionRunQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := parq.querySpec()
	_spec.Node.Columns = parq.ctx.Fields
	if len(parq.ctx.Fields) > 0 {
		_spec.Unique = parq.ctx.Unique != nil && *parq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, parq.driver, _spec)
}

func (parq *PrAttributionRunQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(prattributionrun.Table, prattributionrun.Columns, sqlgraph.NewFieldSpec(prattributionrun.FieldID, field.TypeInt))
	_spec.From = parq.sql
	if unique := parq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if parq.path != nil {
		_spec.Unique = true
	}
	if fields := parq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, prattributionrun.FieldID)
		for i := range fields {
			if fields[i] != prattributionrun.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if parq.withPrRecord != nil {
			_spec.Node.AddColumnOnce(prattributionrun.FieldPrRecordID)
		}
	}
	if ps := parq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := parq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := parq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := parq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (parq *PrAttributionRunQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(parq.driver.Dialect())
	t1 := builder.Table(prattributionrun.Table)
	columns := parq.ctx.Fields
	if len(columns) == 0 {
		columns = prattributionrun.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if parq.sql != nil {
		selector = parq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if parq.ctx.Unique != nil && *parq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range parq.predicates {
		p(selector)
	}
	for _, p := range parq.order {
		p(selector)
	}
	if offset := parq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := parq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PrAttributionRunGroupBy is the group-by builder for PrAttributionRun entities.
type PrAttributionRunGroupBy struct {
	selector
	build *PrAttributionRunQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pargb *PrAttributionRunGroupBy) Aggregate(fns ...AggregateFunc) *PrAttributionRunGroupBy {
	pargb.fns = append(pargb.fns, fns...)
	return pargb
}

// Scan applies the selector query and scans the result into the given value.
func (pargb *PrAttributionRunGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pargb.build.ctx, ent.OpQueryGroupBy)
	if err := pargb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PrAttributionRunQuery, *PrAttributionRunGroupBy](ctx, pargb.build, pargb, pargb.build.inters, v)
}

func (pargb *PrAttributionRunGroupBy) sqlScan(ctx context.Context, root *PrAttributionRunQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pargb.fns))
	for _, fn := range pargb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pargb.flds)+len(pargb.fns))
		for _, f := range *pargb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pargb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pargb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PrAttributionRunSelect is the builder for selecting fields of PrAttributionRun entities.
type PrAttributionRunSelect struct {
	*PrAttributionRunQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pars *PrAttributionRunSelect) Aggregate(fns ...AggregateFunc) *PrAttributionRunSelect {
	pars.fns = append(pars.fns, fns...)
	return pars
}

// Scan applies the selector query and scans the result into the given value.
func (pars *PrAttributionRunSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pars.ctx, ent.OpQuerySelect)
	if err := pars.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PrAttributionRunQuery, *PrAttributionRunSelect](ctx, pars.PrAttributionRunQuery, pars, pars.inters, v)
}

func (pars *PrAttributionRunSelect) sqlScan(ctx context.Context, root *PrAttributionRunQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pars.fns))
	for _, fn := range pars.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pars.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pars.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
