// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/attributionallocationrevision"
	"github.com/ai-efficiency/backend/ent/attributionusagebucket"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/reportinginstallation"
	"github.com/ai-efficiency/backend/ent/user"
)

// AttributionUsageBucketQuery is the builder for querying AttributionUsageBucket entities.
type AttributionUsageBucketQuery struct {
	config
	ctx                       *QueryContext
	order                     []attributionusagebucket.OrderOption
	inters                    []Interceptor
	predicates                []predicate.AttributionUsageBucket
	withReportingInstallation *ReportingInstallationQuery
	withUser                  *UserQuery
	withAllocationRevisions   *AttributionAllocationRevisionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributionUsageBucketQuery builder.
func (aubq *AttributionUsageBucketQuery) Where(ps ...predicate.AttributionUsageBucket) *AttributionUsageBucketQuery {
	aubq.predicates = append(aubq.predicates, ps...)
	return aubq
}

// Limit the number of records to be returned by this query.
func (aubq *AttributionUsageBucketQuery) Limit(limit int) *AttributionUsageBucketQuery {
	aubq.ctx.Limit = &limit
	return aubq
}

// Offset to start from.
func (aubq *AttributionUsageBucketQuery) Offset(offset int) *AttributionUsageBucketQuery {
	aubq.ctx.Offset = &offset
	return aubq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aubq *AttributionUsageBucketQuery) Unique(unique bool) *AttributionUsageBucketQuery {
	aubq.ctx.Unique = &unique
	return aubq
}

// Order specifies how the records should be ordered.
func (aubq *AttributionUsageBucketQuery) Order(o ...attributionusagebucket.OrderOption) *AttributionUsageBucketQuery {
	aubq.order = append(aubq.order, o...)
	return aubq
}

// QueryReportingInstallation chains the current query on the "reporting_installation" edge.
func (aubq *AttributionUsageBucketQuery) QueryReportingInstallation() *ReportingInstallationQuery {
	query := (&ReportingInstallationClient{config: aubq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aubq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aubq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attributionusagebucket.Table, attributionusagebucket.FieldID, selector),
			sqlgraph.To(reportinginstallation.Table, reportinginstallation.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attributionusagebucket.ReportingInstallationTable, attributionusagebucket.ReportingInstallationColumn),
		)
		fromU = sqlgraph.SetNeighbors(aubq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (aubq *AttributionUsageBucketQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: aubq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aubq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aubq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attributionusagebucket.Table, attributionusagebucket.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, attributionusagebucket.UserTable, attributionusagebucket.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(aubq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAllocationRevisions chains the current query on the "allocation_revisions" edge.
func (aubq *AttributionUsageBucketQuery) QueryAllocationRevisions() *AttributionAllocationRevisionQuery {
	query := (&AttributionAllocationRevisionClient{config: aubq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aubq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aubq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(attributionusagebucket.Table, attributionusagebucket.FieldID, selector),
			sqlgraph.To(attributionallocationrevision.Table, attributionallocationrevision.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, attributionusagebucket.AllocationRevisionsTable, attributionusagebucket.AllocationRevisionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(aubq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AttributionUsageBucket entity from the query.
// Returns a *NotFoundError when no AttributionUsageBucket was found.
func (aubq *AttributionUsageBucketQuery) First(ctx context.Context) (*AttributionUsageBucket, error) {
	nodes, err := aubq.Limit(1).All(setContextOp(ctx, aubq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributionusagebucket.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) FirstX(ctx context.Context) *AttributionUsageBucket {
	node, err := aubq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributionUsageBucket ID from the query.
// Returns a *NotFoundError when no AttributionUsageBucket ID was found.
func (aubq *AttributionUsageBucketQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aubq.Limit(1).IDs(setContextOp(ctx, aubq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributionusagebucket.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) FirstIDX(ctx context.Context) int {
	id, err := aubq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributionUsageBucket entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributionUsageBucket entity is found.
// Returns a *NotFoundError when no AttributionUsageBucket entities are found.
func (aubq *AttributionUsageBucketQuery) Only(ctx context.Context) (*AttributionUsageBucket, error) {
	nodes, err := aubq.Limit(2).All(setContextOp(ctx, aubq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributionusagebucket.Label}
	default:
		return nil, &NotSingularError{attributionusagebucket.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) OnlyX(ctx context.Context) *AttributionUsageBucket {
	node, err := aubq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributionUsageBucket ID in the query.
// Returns a *NotSingularError when more than one AttributionUsageBucket ID is found.
// Returns a *NotFoundError when no entities are found.
func (aubq *AttributionUsageBucketQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aubq.Limit(2).IDs(setContextOp(ctx, aubq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributionusagebucket.Label}
	default:
		err = &NotSingularError{attributionusagebucket.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) OnlyIDX(ctx context.Context) int {
	id, err := aubq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributionUsageBuckets.
func (aubq *AttributionUsageBucketQuery) All(ctx context.Context) ([]*AttributionUsageBucket, error) {
	ctx = setContextOp(ctx, aubq.ctx, ent.OpQueryAll)
	if err := aubq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributionUsageBucket, *AttributionUsageBucketQuery]()
	return withInterceptors[[]*AttributionUsageBucket](ctx, aubq, qr, aubq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) AllX(ctx context.Context) []*AttributionUsageBucket {
	nodes, err := aubq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributionUsageBucket IDs.
func (aubq *AttributionUsageBucketQuery) IDs(ctx context.Context) (ids []int, err error) {
	if aubq.ctx.Unique == nil && aubq.path != nil {
		aubq.Unique(true)
	}
	ctx = setContextOp(ctx, aubq.ctx, ent.OpQueryIDs)
	if err = aubq.Select(attributionusagebucket.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) IDsX(ctx context.Context) []int {
	ids, err := aubq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aubq *AttributionUsageBucketQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aubq.ctx, ent.OpQueryCount)
	if err := aubq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aubq, querierCount[*AttributionUsageBucketQuery](), aubq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) CountX(ctx context.Context) int {
	count, err := aubq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aubq *AttributionUsageBucketQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aubq.ctx, ent.OpQueryExist)
	switch _, err := aubq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aubq *AttributionUsageBucketQuery) ExistX(ctx context.Context) bool {
	exist, err := aubq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributionUsageBucketQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aubq *AttributionUsageBucketQuery) Clone() *AttributionUsageBucketQuery {
	if aubq == nil {
		return nil
	}
	return &AttributionUsageBucketQuery{
		config:                    aubq.config,
		ctx:                       aubq.ctx.Clone(),
		order:                     append([]attributionusagebucket.OrderOption{}, aubq.order...),
		inters:                    append([]Interceptor{}, aubq.inters...),
		predicates:                append([]predicate.AttributionUsageBucket{}, aubq.predicates...),
		withReportingInstallation: aubq.withReportingInstallation.Clone(),
		withUser:                  aubq.withUser.Clone(),
		withAllocationRevisions:   aubq.withAllocationRevisions.Clone(),
		// clone intermediate query.
		sql:  aubq.sql.Clone(),
		path: aubq.path,
	}
}

// WithReportingInstallation tells the query-builder to eager-load the nodes that are connected to
// the "reporting_installation" edge. The optional arguments are used to configure the query builder of the edge.
func (aubq *AttributionUsageBucketQuery) WithReportingInstallation(opts ...func(*ReportingInstallationQuery)) *AttributionUsageBucketQuery {
	query := (&ReportingInstallationClient{config: aubq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aubq.withReportingInstallation = query
	return aubq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (aubq *AttributionUsageBucketQuery) WithUser(opts ...func(*UserQuery)) *AttributionUsageBucketQuery {
	query := (&UserClient{config: aubq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aubq.withUser = query
	return aubq
}

// WithAllocationRevisions tells the query-builder to eager-load the nodes that are connected to
// the "allocation_revisions" edge. The optional arguments are used to configure the query builder of the edge.
func (aubq *AttributionUsageBucketQuery) WithAllocationRevisions(opts ...func(*AttributionAllocationRevisionQuery)) *AttributionUsageBucketQuery {
	query := (&AttributionAllocationRevisionClient{config: aubq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aubq.withAllocationRevisions = query
	return aubq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BucketID string `json:"bucket_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributionUsageBucket.Query().
//		GroupBy(attributionusagebucket.FieldBucketID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aubq *AttributionUsageBucketQuery) GroupBy(field string, fields ...string) *AttributionUsageBucketGroupBy {
	aubq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributionUsageBucketGroupBy{build: aubq}
	grbuild.flds = &aubq.ctx.Fields
	grbuild.label = attributionusagebucket.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BucketID string `json:"bucket_id,omitempty"`
//	}
//
//	client.AttributionUsageBucket.Query().
//		Select(attributionusagebucket.FieldBucketID).
//		Scan(ctx, &v)
func (aubq *AttributionUsageBucketQuery) Select(fields ...string) *AttributionUsageBucketSelect {
	aubq.ctx.Fields = append(aubq.ctx.Fields, fields...)
	sbuild := &AttributionUsageBucketSelect{AttributionUsageBucketQuery: aubq}
	sbuild.label = attributionusagebucket.Label
	sbuild.flds, sbuild.scan = &aubq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributionUsageBucketSelect configured with the given aggregations.
func (aubq *AttributionUsageBucketQuery) Aggregate(fns ...AggregateFunc) *AttributionUsageBucketSelect {
	return aubq.Select().Aggregate(fns...)
}

func (aubq *AttributionUsageBucketQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aubq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aubq); err != nil {
				return err
			}
		}
	}
	for _, f := range aubq.ctx.Fields {
		if !attributionusagebucket.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aubq.path != nil {
		prev, err := aubq.path(ctx)
		if err != nil {
			return err
		}
		aubq.sql = prev
	}
	return nil
}

func (aubq *AttributionUsageBucketQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributionUsageBucket, error) {
	var (
		nodes       = []*AttributionUsageBucket{}
		_spec       = aubq.querySpec()
		loadedTypes = [3]bool{
			aubq.withReportingInstallation != nil,
			aubq.withUser != nil,
			aubq.withAllocationRevisions != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributionUsageBucket).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributionUsageBucket{config: aubq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aubq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aubq.withReportingInstallation; query != nil {
		if err := aubq.loadReportingInstallation(ctx, query, nodes, nil,
			func(n *AttributionUsageBucket, e *ReportingInstallation) { n.Edges.ReportingInstallation = e }); err != nil {
			return nil, err
		}
	}
	if query := aubq.withUser; query != nil {
		if err := aubq.loadUser(ctx, query, nodes, nil,
			func(n *AttributionUsageBucket, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := aubq.withAllocationRevisions; query != nil {
		if err := aubq.loadAllocationRevisions(ctx, query, nodes,
			func(n *AttributionUsageBucket) { n.Edges.AllocationRevisions = []*AttributionAllocationRevision{} },
			func(n *AttributionUsageBucket, e *AttributionAllocationRevision) {
				n.Edges.AllocationRevisions = append(n.Edges.AllocationRevisions, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aubq *AttributionUsageBucketQuery) loadReportingInstallation(ctx context.Context, query *ReportingInstallationQuery, nodes []*AttributionUsageBucket, init func(*AttributionUsageBucket), assign func(*AttributionUsageBucket, *ReportingInstallation)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AttributionUsageBucket)
	for i := range nodes {
		fk := nodes[i].ReportingInstallationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(reportinginstallation.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "reporting_installation_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aubq *AttributionUsageBucketQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*AttributionUsageBucket, init func(*AttributionUsageBucket), assign func(*AttributionUsageBucket, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AttributionUsageBucket)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (aubq *AttributionUsageBucketQuery) loadAllocationRevisions(ctx context.Context, query *AttributionAllocationRevisionQuery, nodes []*AttributionUsageBucket, init func(*AttributionUsageBucket), assign func(*AttributionUsageBucket, *AttributionAllocationRevision)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*AttributionUsageBucket)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(attributionallocationrevision.FieldUsageBucketID)
	}
	query.Where(predicate.AttributionAllocationRevision(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(attributionusagebucket.AllocationRevisionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.UsageBucketID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "usage_bucket_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (aubq *AttributionUsageBucketQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aubq.querySpec()
	_spec.Node.Columns = aubq.ctx.Fields
	if len(aubq.ctx.Fields) > 0 {
		_spec.Unique = aubq.ctx.Unique != nil && *aubq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aubq.driver, _spec)
}

func (aubq *AttributionUsageBucketQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributionusagebucket.Table, attributionusagebucket.Columns, sqlgraph.NewFieldSpec(attributionusagebucket.FieldID, field.TypeInt))
	_spec.From = aubq.sql
	if unique := aubq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aubq.path != nil {
		_spec.Unique = true
	}
	if fields := aubq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributionusagebucket.FieldID)
		for i := range fields {
			if fields[i] != attributionusagebucket.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if aubq.withReportingInstallation != nil {
			_spec.Node.AddColumnOnce(attributionusagebucket.FieldReportingInstallationID)
		}
		if aubq.withUser != nil {
			_spec.Node.AddColumnOnce(attributionusagebucket.FieldUserID)
		}
	}
	if ps := aubq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aubq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aubq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aubq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aubq *AttributionUsageBucketQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aubq.driver.Dialect())
	t1 := builder.Table(attributionusagebucket.Table)
	columns := aubq.ctx.Fields
	if len(columns) == 0 {
		columns = attributionusagebucket.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aubq.sql != nil {
		selector = aubq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aubq.ctx.Unique != nil && *aubq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aubq.predicates {
		p(selector)
	}
	for _, p := range aubq.order {
		p(selector)
	}
	if offset := aubq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aubq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttributionUsageBucketGroupBy is the group-by builder for AttributionUsageBucket entities.
type AttributionUsageBucketGroupBy struct {
	selector
	build *AttributionUsageBucketQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aubgb *AttributionUsageBucketGroupBy) Aggregate(fns ...AggregateFunc) *AttributionUsageBucketGroupBy {
	aubgb.fns = append(aubgb.fns, fns...)
	return aubgb
}

// Scan applies the selector query and scans the result into the given value.
func (aubgb *AttributionUsageBucketGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aubgb.build.ctx, ent.OpQueryGroupBy)
	if err := aubgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionUsageBucketQuery, *AttributionUsageBucketGroupBy](ctx, aubgb.build, aubgb, aubgb.build.inters, v)
}

func (aubgb *AttributionUsageBucketGroupBy) sqlScan(ctx context.Context, root *AttributionUsageBucketQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aubgb.fns))
	for _, fn := range aubgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aubgb.flds)+len(aubgb.fns))
		for _, f := range *aubgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aubgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aubgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributionUsageBucketSelect is the builder for selecting fields of AttributionUsageBucket entities.
type AttributionUsageBucketSelect struct {
	*AttributionUsageBucketQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aubs *AttributionUsageBucketSelect) Aggregate(fns ...AggregateFunc) *AttributionUsageBucketSelect {
	aubs.fns = append(aubs.fns, fns...)
	return aubs
}

// Scan applies the selector query and scans the result into the given value.
func (aubs *AttributionUsageBucketSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aubs.ctx, ent.OpQuerySelect)
	if err := aubs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributionUsageBucketQuery, *AttributionUsageBucketSelect](ctx, aubs.AttributionUsageBucketQuery, aubs, aubs.inters, v)
}

func (aubs *AttributionUsageBucketSelect) sqlScan(ctx context.Context, root *AttributionUsageBucketQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aubs.fns))
	for _, fn := range aubs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aubs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aubs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
