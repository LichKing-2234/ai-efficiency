// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/session"
	"github.com/ai-efficiency/backend/ent/sessionusageevent"
	"github.com/google/uuid"
)

// SessionUsageEventQuery is the builder for querying SessionUsageEvent entities.
type SessionUsageEventQuery struct {
	config
	ctx         *QueryContext
	order       []sessionusageevent.OrderOption
	inters      []Interceptor
	predicates  []predicate.SessionUsageEvent
	withSession *SessionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SessionUsageEventQuery builder.
func (sueq *SessionUsageEventQuery) Where(ps ...predicate.SessionUsageEvent) *SessionUsageEventQuery {
	sueq.predicates = append(sueq.predicates, ps...)
	return sueq
}

// Limit the number of records to be returned by this query.
func (sueq *SessionUsageEventQuery) Limit(limit int) *SessionUsageEventQuery {
	sueq.ctx.Limit = &limit
	return sueq
}

// Offset to start from.
func (sueq *SessionUsageEventQuery) Offset(offset int) *SessionUsageEventQuery {
	sueq.ctx.Offset = &offset
	return sueq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sueq *SessionUsageEventQuery) Unique(unique bool) *SessionUsageEventQuery {
	sueq.ctx.Unique = &unique
	return sueq
}

// Order specifies how the records should be ordered.
func (sueq *SessionUsageEventQuery) Order(o ...sessionusageevent.OrderOption) *SessionUsageEventQuery {
	sueq.order = append(sueq.order, o...)
	return sueq
}

// QuerySession chains the current query on the "session" edge.
func (sueq *SessionUsageEventQuery) QuerySession() *SessionQuery {
	query := (&SessionClient{config: sueq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sueq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sueq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionusageevent.Table, sessionusageevent.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionusageevent.SessionTable, sessionusageevent.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(sueq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SessionUsageEvent entity from the query.
// Returns a *NotFoundError when no SessionUsageEvent was found.
func (sueq *SessionUsageEventQuery) First(ctx context.Context) (*SessionUsageEvent, error) {
	nodes, err := sueq.Limit(1).All(setContextOp(ctx, sueq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sessionusageevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) FirstX(ctx context.Context) *SessionUsageEvent {
	node, err := sueq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SessionUsageEvent ID from the query.
// Returns a *NotFoundError when no SessionUsageEvent ID was found.
func (sueq *SessionUsageEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sueq.Limit(1).IDs(setContextOp(ctx, sueq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sessionusageevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) FirstIDX(ctx context.Context) int {
	id, err := sueq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SessionUsageEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SessionUsageEvent entity is found.
// Returns a *NotFoundError when no SessionUsageEvent entities are found.
func (sueq *SessionUsageEventQuery) Only(ctx context.Context) (*SessionUsageEvent, error) {
	nodes, err := sueq.Limit(2).All(setContextOp(ctx, sueq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sessionusageevent.Label}
	default:
		return nil, &NotSingularError{sessionusageevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) OnlyX(ctx context.Context) *SessionUsageEvent {
	node, err := sueq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SessionUsageEvent ID in the query.
// Returns a *NotSingularError when more than one SessionUsageEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (sueq *SessionUsageEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sueq.Limit(2).IDs(setContextOp(ctx, sueq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sessionusageevent.Label}
	default:
		err = &NotSingularError{sessionusageevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := sueq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SessionUsageEvents.
func (sueq *SessionUsageEventQuery) All(ctx context.Context) ([]*SessionUsageEvent, error) {
	ctx = setContextOp(ctx, sueq.ctx, ent.OpQueryAll)
	if err := sueq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SessionUsageEvent, *SessionUsageEventQuery]()
	return withInterceptors[[]*SessionUsageEvent](ctx, sueq, qr, sueq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) AllX(ctx context.Context) []*SessionUsageEvent {
	nodes, err := sueq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SessionUsageEvent IDs.
func (sueq *SessionUsageEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sueq.ctx.Unique == nil && sueq.path != nil {
		sueq.Unique(true)
	}
	ctx = setContextOp(ctx, sueq.ctx, ent.OpQueryIDs)
	if err = sueq.Select(sessionusageevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) IDsX(ctx context.Context) []int {
	ids, err := sueq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sueq *SessionUsageEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sueq.ctx, ent.OpQueryCount)
	if err := sueq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sueq, querierCount[*SessionUsageEventQuery](), sueq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) CountX(ctx context.Context) int {
	count, err := sueq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sueq *SessionUsageEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sueq.ctx, ent.OpQueryExist)
	switch _, err := sueq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sueq *SessionUsageEventQuery) ExistX(ctx context.Context) bool {
	exist, err := sueq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SessionUsageEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sueq *SessionUsageEventQuery) Clone() *SessionUsageEventQuery {
	if sueq == nil {
		return nil
	}
	return &SessionUsageEventQuery{
		config:      sueq.config,
		ctx:         sueq.ctx.Clone(),
		order:       append([]sessionusageevent.OrderOption{}, sueq.order...),
		inters:      append([]Interceptor{}, sueq.inters...),
		predicates:  append([]predicate.SessionUsageEvent{}, sueq.predicates...),
		withSession: sueq.withSession.Clone(),
		// clone intermediate query.
		sql:  sueq.sql.Clone(),
		path: sueq.path,
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (sueq *SessionUsageEventQuery) WithSession(opts ...func(*SessionQuery)) *SessionUsageEventQuery {
	query := (&SessionClient{config: sueq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sueq.withSession = query
	return sueq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EventID string `json:"event_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SessionUsageEvent.Query().
//		GroupBy(sessionusageevent.FieldEventID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sueq *SessionUsageEventQuery) GroupBy(field string, fields ...string) *SessionUsageEventGroupBy {
	sueq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SessionUsageEventGroupBy{build: sueq}
	grbuild.flds = &sueq.ctx.Fields
	grbuild.label = sessionusageevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EventID string `json:"event_id,omitempty"`
//	}
//
//	client.SessionUsageEvent.Query().
//		Select(sessionusageevent.FieldEventID).
//		Scan(ctx, &v)
func (sueq *SessionUsageEventQuery) Select(fields ...string) *SessionUsageEventSelect {
	sueq.ctx.Fields = append(sueq.ctx.Fields, fields...)
	sbuild := &SessionUsageEventSelect{SessionUsageEventQuery: sueq}
	sbuild.label = sessionusageevent.Label
	sbuild.flds, sbuild.scan = &sueq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SessionUsageEventSelect configured with the given aggregations.
func (sueq *SessionUsageEventQuery) Aggregate(fns ...AggregateFunc) *SessionUsageEventSelect {
	return sueq.Select().Aggregate(fns...)
}

func (sueq *SessionUsageEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sueq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sueq); err != nil {
				return err
			}
		}
	}
	for _, f := range sueq.ctx.Fields {
		if !sessionusageevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sueq.path != nil {
		prev, err := sueq.path(ctx)
		if err != nil {
			return err
		}
		sueq.sql = prev
	}
	return nil
}

func (sueq *SessionUsageEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SessionUsageEvent, error) {
	var (
		nodes       = []*SessionUsageEvent{}
		_spec       = sueq.querySpec()
		loadedTypes = [1]bool{
			sueq.withSession != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SessionUsageEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SessionUsageEvent{config: sueq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sueq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sueq.withSession; query != nil {
		if err := sueq.loadSession(ctx, query, nodes, nil,
			func(n *SessionUsageEvent, e *Session) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sueq *SessionUsageEventQuery) loadSession(ctx context.Context, query *SessionQuery, nodes []*SessionUsageEvent, init func(*SessionUsageEvent), assign func(*SessionUsageEvent, *Session)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SessionUsageEvent)
	for i := range nodes {
		fk := nodes[i].SessionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(session.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sueq *SessionUsageEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sueq.querySpec()
	_spec.Node.Columns = sueq.ctx.Fields
	if len(sueq.ctx.Fields) > 0 {
		_spec.Unique = sueq.ctx.Unique != nil && *sueq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sueq.driver, _spec)
}

func (sueq *SessionUsageEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sessionusageevent.Table, sessionusageevent.Columns, sqlgraph.NewFieldSpec(sessionusageevent.FieldID, field.TypeInt))
	_spec.From = sueq.sql
	if unique := sueq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sueq.path != nil {
		_spec.Unique = true
	}
	if fields := sueq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sessionusageevent.FieldID)
		for i := range fields {
			if fields[i] != sessionusageevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sueq.withSession != nil {
			_spec.Node.AddColumnOnce(sessionusageevent.FieldSessionID)
		}
	}
	if ps := sueq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sueq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sueq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sueq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sueq *SessionUsageEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sueq.driver.Dialect())
	t1 := builder.Table(sessionusageevent.Table)
	columns := sueq.ctx.Fields
	if len(columns) == 0 {
		columns = sessionusageevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sueq.sql != nil {
		selector = sueq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sueq.ctx.Unique != nil && *sueq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sueq.predicates {
		p(selector)
	}
	for _, p := range sueq.order {
		p(selector)
	}
	if offset := sueq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sueq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SessionUsageEventGroupBy is the group-by builder for SessionUsageEvent entities.
type SessionUsageEventGroupBy struct {
	selector
	build *SessionUsageEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (suegb *SessionUsageEventGroupBy) Aggregate(fns ...AggregateFunc) *SessionUsageEventGroupBy {
	suegb.fns = append(suegb.fns, fns...)
	return suegb
}

// Scan applies the selector query and scans the result into the given value.
func (suegb *SessionUsageEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, suegb.build.ctx, ent.OpQueryGroupBy)
	if err := suegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SessionUsageEventQuery, *SessionUsageEventGroupBy](ctx, suegb.build, suegb, suegb.build.inters, v)
}

func (suegb *SessionUsageEventGroupBy) sqlScan(ctx context.Context, root *SessionUsageEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(suegb.fns))
	for _, fn := range suegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*suegb.flds)+len(suegb.fns))
		for _, f := range *suegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*suegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := suegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SessionUsageEventSelect is the builder for selecting fields of SessionUsageEvent entities.
type SessionUsageEventSelect struct {
	*SessionUsageEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sues *SessionUsageEventSelect) Aggregate(fns ...AggregateFunc) *SessionUsageEventSelect {
	sues.fns = append(sues.fns, fns...)
	return sues
}

// Scan applies the selector query and scans the result into the given value.
func (sues *SessionUsageEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sues.ctx, ent.OpQuerySelect)
	if err := sues.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SessionUsageEventQuery, *SessionUsageEventSelect](ctx, sues.SessionUsageEventQuery, sues, sues.inters, v)
}

func (sues *SessionUsageEventSelect) sqlScan(ctx context.Context, root *SessionUsageEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sues.fns))
	for _, fn := range sues.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sues.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sues.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
