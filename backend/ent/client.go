// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"github.com/ai-efficiency/backend/ent/migrate"
	"github.com/google/uuid"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/ai-efficiency/backend/ent/agentmetadataevent"
	"github.com/ai-efficiency/backend/ent/aiscanresult"
	"github.com/ai-efficiency/backend/ent/commitcheckpoint"
	"github.com/ai-efficiency/backend/ent/commitrewrite"
	"github.com/ai-efficiency/backend/ent/credential"
	"github.com/ai-efficiency/backend/ent/efficiencymetric"
	"github.com/ai-efficiency/backend/ent/prattributionrun"
	"github.com/ai-efficiency/backend/ent/prrecord"
	"github.com/ai-efficiency/backend/ent/relayprovider"
	"github.com/ai-efficiency/backend/ent/repoconfig"
	"github.com/ai-efficiency/backend/ent/scmprovider"
	"github.com/ai-efficiency/backend/ent/session"
	"github.com/ai-efficiency/backend/ent/sessionevent"
	"github.com/ai-efficiency/backend/ent/sessionusageevent"
	"github.com/ai-efficiency/backend/ent/sessionworkspace"
	"github.com/ai-efficiency/backend/ent/systemsetting"
	"github.com/ai-efficiency/backend/ent/user"
	"github.com/ai-efficiency/backend/ent/webhookdeadletter"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// AgentMetadataEvent is the client for interacting with the AgentMetadataEvent builders.
	AgentMetadataEvent *AgentMetadataEventClient
	// AiScanResult is the client for interacting with the AiScanResult builders.
	AiScanResult *AiScanResultClient
	// CommitCheckpoint is the client for interacting with the CommitCheckpoint builders.
	CommitCheckpoint *CommitCheckpointClient
	// CommitRewrite is the client for interacting with the CommitRewrite builders.
	CommitRewrite *CommitRewriteClient
	// Credential is the client for interacting with the Credential builders.
	Credential *CredentialClient
	// EfficiencyMetric is the client for interacting with the EfficiencyMetric builders.
	EfficiencyMetric *EfficiencyMetricClient
	// PrAttributionRun is the client for interacting with the PrAttributionRun builders.
	PrAttributionRun *PrAttributionRunClient
	// PrRecord is the client for interacting with the PrRecord builders.
	PrRecord *PrRecordClient
	// RelayProvider is the client for interacting with the RelayProvider builders.
	RelayProvider *RelayProviderClient
	// RepoConfig is the client for interacting with the RepoConfig builders.
	RepoConfig *RepoConfigClient
	// ScmProvider is the client for interacting with the ScmProvider builders.
	ScmProvider *ScmProviderClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// SessionEvent is the client for interacting with the SessionEvent builders.
	SessionEvent *SessionEventClient
	// SessionUsageEvent is the client for interacting with the SessionUsageEvent builders.
	SessionUsageEvent *SessionUsageEventClient
	// SessionWorkspace is the client for interacting with the SessionWorkspace builders.
	SessionWorkspace *SessionWorkspaceClient
	// SystemSetting is the client for interacting with the SystemSetting builders.
	SystemSetting *SystemSettingClient
	// User is the client for interacting with the User builders.
	User *UserClient
	// WebhookDeadLetter is the client for interacting with the WebhookDeadLetter builders.
	WebhookDeadLetter *WebhookDeadLetterClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.AgentMetadataEvent = NewAgentMetadataEventClient(c.config)
	c.AiScanResult = NewAiScanResultClient(c.config)
	c.CommitCheckpoint = NewCommitCheckpointClient(c.config)
	c.CommitRewrite = NewCommitRewriteClient(c.config)
	c.Credential = NewCredentialClient(c.config)
	c.EfficiencyMetric = NewEfficiencyMetricClient(c.config)
	c.PrAttributionRun = NewPrAttributionRunClient(c.config)
	c.PrRecord = NewPrRecordClient(c.config)
	c.RelayProvider = NewRelayProviderClient(c.config)
	c.RepoConfig = NewRepoConfigClient(c.config)
	c.ScmProvider = NewScmProviderClient(c.config)
	c.Session = NewSessionClient(c.config)
	c.SessionEvent = NewSessionEventClient(c.config)
	c.SessionUsageEvent = NewSessionUsageEventClient(c.config)
	c.SessionWorkspace = NewSessionWorkspaceClient(c.config)
	c.SystemSetting = NewSystemSettingClient(c.config)
	c.User = NewUserClient(c.config)
	c.WebhookDeadLetter = NewWebhookDeadLetterClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                ctx,
		config:             cfg,
		AgentMetadataEvent: NewAgentMetadataEventClient(cfg),
		AiScanResult:       NewAiScanResultClient(cfg),
		CommitCheckpoint:   NewCommitCheckpointClient(cfg),
		CommitRewrite:      NewCommitRewriteClient(cfg),
		Credential:         NewCredentialClient(cfg),
		EfficiencyMetric:   NewEfficiencyMetricClient(cfg),
		PrAttributionRun:   NewPrAttributionRunClient(cfg),
		PrRecord:           NewPrRecordClient(cfg),
		RelayProvider:      NewRelayProviderClient(cfg),
		RepoConfig:         NewRepoConfigClient(cfg),
		ScmProvider:        NewScmProviderClient(cfg),
		Session:            NewSessionClient(cfg),
		SessionEvent:       NewSessionEventClient(cfg),
		SessionUsageEvent:  NewSessionUsageEventClient(cfg),
		SessionWorkspace:   NewSessionWorkspaceClient(cfg),
		SystemSetting:      NewSystemSettingClient(cfg),
		User:               NewUserClient(cfg),
		WebhookDeadLetter:  NewWebhookDeadLetterClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                ctx,
		config:             cfg,
		AgentMetadataEvent: NewAgentMetadataEventClient(cfg),
		AiScanResult:       NewAiScanResultClient(cfg),
		CommitCheckpoint:   NewCommitCheckpointClient(cfg),
		CommitRewrite:      NewCommitRewriteClient(cfg),
		Credential:         NewCredentialClient(cfg),
		EfficiencyMetric:   NewEfficiencyMetricClient(cfg),
		PrAttributionRun:   NewPrAttributionRunClient(cfg),
		PrRecord:           NewPrRecordClient(cfg),
		RelayProvider:      NewRelayProviderClient(cfg),
		RepoConfig:         NewRepoConfigClient(cfg),
		ScmProvider:        NewScmProviderClient(cfg),
		Session:            NewSessionClient(cfg),
		SessionEvent:       NewSessionEventClient(cfg),
		SessionUsageEvent:  NewSessionUsageEventClient(cfg),
		SessionWorkspace:   NewSessionWorkspaceClient(cfg),
		SystemSetting:      NewSystemSettingClient(cfg),
		User:               NewUserClient(cfg),
		WebhookDeadLetter:  NewWebhookDeadLetterClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		AgentMetadataEvent.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	for _, n := range []interface{ Use(...Hook) }{
		c.AgentMetadataEvent, c.AiScanResult, c.CommitCheckpoint, c.CommitRewrite,
		c.Credential, c.EfficiencyMetric, c.PrAttributionRun, c.PrRecord,
		c.RelayProvider, c.RepoConfig, c.ScmProvider, c.Session, c.SessionEvent,
		c.SessionUsageEvent, c.SessionWorkspace, c.SystemSetting, c.User,
		c.WebhookDeadLetter,
	} {
		n.Use(hooks...)
	}
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	for _, n := range []interface{ Intercept(...Interceptor) }{
		c.AgentMetadataEvent, c.AiScanResult, c.CommitCheckpoint, c.CommitRewrite,
		c.Credential, c.EfficiencyMetric, c.PrAttributionRun, c.PrRecord,
		c.RelayProvider, c.RepoConfig, c.ScmProvider, c.Session, c.SessionEvent,
		c.SessionUsageEvent, c.SessionWorkspace, c.SystemSetting, c.User,
		c.WebhookDeadLetter,
	} {
		n.Intercept(interceptors...)
	}
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *AgentMetadataEventMutation:
		return c.AgentMetadataEvent.mutate(ctx, m)
	case *AiScanResultMutation:
		return c.AiScanResult.mutate(ctx, m)
	case *CommitCheckpointMutation:
		return c.CommitCheckpoint.mutate(ctx, m)
	case *CommitRewriteMutation:
		return c.CommitRewrite.mutate(ctx, m)
	case *CredentialMutation:
		return c.Credential.mutate(ctx, m)
	case *EfficiencyMetricMutation:
		return c.EfficiencyMetric.mutate(ctx, m)
	case *PrAttributionRunMutation:
		return c.PrAttributionRun.mutate(ctx, m)
	case *PrRecordMutation:
		return c.PrRecord.mutate(ctx, m)
	case *RelayProviderMutation:
		return c.RelayProvider.mutate(ctx, m)
	case *RepoConfigMutation:
		return c.RepoConfig.mutate(ctx, m)
	case *ScmProviderMutation:
		return c.ScmProvider.mutate(ctx, m)
	case *SessionMutation:
		return c.Session.mutate(ctx, m)
	case *SessionEventMutation:
		return c.SessionEvent.mutate(ctx, m)
	case *SessionUsageEventMutation:
		return c.SessionUsageEvent.mutate(ctx, m)
	case *SessionWorkspaceMutation:
		return c.SessionWorkspace.mutate(ctx, m)
	case *SystemSettingMutation:
		return c.SystemSetting.mutate(ctx, m)
	case *UserMutation:
		return c.User.mutate(ctx, m)
	case *WebhookDeadLetterMutation:
		return c.WebhookDeadLetter.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// AgentMetadataEventClient is a client for the AgentMetadataEvent schema.
type AgentMetadataEventClient struct {
	config
}

// NewAgentMetadataEventClient returns a client for the AgentMetadataEvent from the given config.
func NewAgentMetadataEventClient(c config) *AgentMetadataEventClient {
	return &AgentMetadataEventClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `agentmetadataevent.Hooks(f(g(h())))`.
func (c *AgentMetadataEventClient) Use(hooks ...Hook) {
	c.hooks.AgentMetadataEvent = append(c.hooks.AgentMetadataEvent, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `agentmetadataevent.Intercept(f(g(h())))`.
func (c *AgentMetadataEventClient) Intercept(interceptors ...Interceptor) {
	c.inters.AgentMetadataEvent = append(c.inters.AgentMetadataEvent, interceptors...)
}

// Create returns a builder for creating a AgentMetadataEvent entity.
func (c *AgentMetadataEventClient) Create() *AgentMetadataEventCreate {
	mutation := newAgentMetadataEventMutation(c.config, OpCreate)
	return &AgentMetadataEventCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AgentMetadataEvent entities.
func (c *AgentMetadataEventClient) CreateBulk(builders ...*AgentMetadataEventCreate) *AgentMetadataEventCreateBulk {
	return &AgentMetadataEventCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AgentMetadataEventClient) MapCreateBulk(slice any, setFunc func(*AgentMetadataEventCreate, int)) *AgentMetadataEventCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AgentMetadataEventCreateBulk{err: fmt.Errorf("calling to AgentMetadataEventClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AgentMetadataEventCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AgentMetadataEventCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AgentMetadataEvent.
func (c *AgentMetadataEventClient) Update() *AgentMetadataEventUpdate {
	mutation := newAgentMetadataEventMutation(c.config, OpUpdate)
	return &AgentMetadataEventUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AgentMetadataEventClient) UpdateOne(ame *AgentMetadataEvent) *AgentMetadataEventUpdateOne {
	mutation := newAgentMetadataEventMutation(c.config, OpUpdateOne, withAgentMetadataEvent(ame))
	return &AgentMetadataEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AgentMetadataEventClient) UpdateOneID(id int) *AgentMetadataEventUpdateOne {
	mutation := newAgentMetadataEventMutation(c.config, OpUpdateOne, withAgentMetadataEventID(id))
	return &AgentMetadataEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AgentMetadataEvent.
func (c *AgentMetadataEventClient) Delete() *AgentMetadataEventDelete {
	mutation := newAgentMetadataEventMutation(c.config, OpDelete)
	return &AgentMetadataEventDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AgentMetadataEventClient) DeleteOne(ame *AgentMetadataEvent) *AgentMetadataEventDeleteOne {
	return c.DeleteOneID(ame.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AgentMetadataEventClient) DeleteOneID(id int) *AgentMetadataEventDeleteOne {
	builder := c.Delete().Where(agentmetadataevent.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AgentMetadataEventDeleteOne{builder}
}

// Query returns a query builder for AgentMetadataEvent.
func (c *AgentMetadataEventClient) Query() *AgentMetadataEventQuery {
	return &AgentMetadataEventQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAgentMetadataEvent},
		inters: c.Interceptors(),
	}
}

// Get returns a AgentMetadataEvent entity by its id.
func (c *AgentMetadataEventClient) Get(ctx context.Context, id int) (*AgentMetadataEvent, error) {
	return c.Query().Where(agentmetadataevent.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AgentMetadataEventClient) GetX(ctx context.Context, id int) *AgentMetadataEvent {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a AgentMetadataEvent.
func (c *AgentMetadataEventClient) QuerySession(ame *AgentMetadataEvent) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ame.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(agentmetadataevent.Table, agentmetadataevent.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, agentmetadataevent.SessionTable, agentmetadataevent.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(ame.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *AgentMetadataEventClient) Hooks() []Hook {
	return c.hooks.AgentMetadataEvent
}

// Interceptors returns the client interceptors.
func (c *AgentMetadataEventClient) Interceptors() []Interceptor {
	return c.inters.AgentMetadataEvent
}

func (c *AgentMetadataEventClient) mutate(ctx context.Context, m *AgentMetadataEventMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AgentMetadataEventCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AgentMetadataEventUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AgentMetadataEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AgentMetadataEventDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AgentMetadataEvent mutation op: %q", m.Op())
	}
}

// AiScanResultClient is a client for the AiScanResult schema.
type AiScanResultClient struct {
	config
}

// NewAiScanResultClient returns a client for the AiScanResult from the given config.
func NewAiScanResultClient(c config) *AiScanResultClient {
	return &AiScanResultClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `aiscanresult.Hooks(f(g(h())))`.
func (c *AiScanResultClient) Use(hooks ...Hook) {
	c.hooks.AiScanResult = append(c.hooks.AiScanResult, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `aiscanresult.Intercept(f(g(h())))`.
func (c *AiScanResultClient) Intercept(interceptors ...Interceptor) {
	c.inters.AiScanResult = append(c.inters.AiScanResult, interceptors...)
}

// Create returns a builder for creating a AiScanResult entity.
func (c *AiScanResultClient) Create() *AiScanResultCreate {
	mutation := newAiScanResultMutation(c.config, OpCreate)
	return &AiScanResultCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AiScanResult entities.
func (c *AiScanResultClient) CreateBulk(builders ...*AiScanResultCreate) *AiScanResultCreateBulk {
	return &AiScanResultCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AiScanResultClient) MapCreateBulk(slice any, setFunc func(*AiScanResultCreate, int)) *AiScanResultCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AiScanResultCreateBulk{err: fmt.Errorf("calling to AiScanResultClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AiScanResultCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AiScanResultCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AiScanResult.
func (c *AiScanResultClient) Update() *AiScanResultUpdate {
	mutation := newAiScanResultMutation(c.config, OpUpdate)
	return &AiScanResultUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AiScanResultClient) UpdateOne(asr *AiScanResult) *AiScanResultUpdateOne {
	mutation := newAiScanResultMutation(c.config, OpUpdateOne, withAiScanResult(asr))
	return &AiScanResultUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AiScanResultClient) UpdateOneID(id int) *AiScanResultUpdateOne {
	mutation := newAiScanResultMutation(c.config, OpUpdateOne, withAiScanResultID(id))
	return &AiScanResultUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AiScanResult.
func (c *AiScanResultClient) Delete() *AiScanResultDelete {
	mutation := newAiScanResultMutation(c.config, OpDelete)
	return &AiScanResultDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AiScanResultClient) DeleteOne(asr *AiScanResult) *AiScanResultDeleteOne {
	return c.DeleteOneID(asr.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AiScanResultClient) DeleteOneID(id int) *AiScanResultDeleteOne {
	builder := c.Delete().Where(aiscanresult.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AiScanResultDeleteOne{builder}
}

// Query returns a query builder for AiScanResult.
func (c *AiScanResultClient) Query() *AiScanResultQuery {
	return &AiScanResultQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAiScanResult},
		inters: c.Interceptors(),
	}
}

// Get returns a AiScanResult entity by its id.
func (c *AiScanResultClient) Get(ctx context.Context, id int) (*AiScanResult, error) {
	return c.Query().Where(aiscanresult.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AiScanResultClient) GetX(ctx context.Context, id int) *AiScanResult {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryRepoConfig queries the repo_config edge of a AiScanResult.
func (c *AiScanResultClient) QueryRepoConfig(asr *AiScanResult) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := asr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(aiscanresult.Table, aiscanresult.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, aiscanresult.RepoConfigTable, aiscanresult.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(asr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *AiScanResultClient) Hooks() []Hook {
	return c.hooks.AiScanResult
}

// Interceptors returns the client interceptors.
func (c *AiScanResultClient) Interceptors() []Interceptor {
	return c.inters.AiScanResult
}

func (c *AiScanResultClient) mutate(ctx context.Context, m *AiScanResultMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AiScanResultCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AiScanResultUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AiScanResultUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AiScanResultDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AiScanResult mutation op: %q", m.Op())
	}
}

// CommitCheckpointClient is a client for the CommitCheckpoint schema.
type CommitCheckpointClient struct {
	config
}

// NewCommitCheckpointClient returns a client for the CommitCheckpoint from the given config.
func NewCommitCheckpointClient(c config) *CommitCheckpointClient {
	return &CommitCheckpointClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `commitcheckpoint.Hooks(f(g(h())))`.
func (c *CommitCheckpointClient) Use(hooks ...Hook) {
	c.hooks.CommitCheckpoint = append(c.hooks.CommitCheckpoint, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `commitcheckpoint.Intercept(f(g(h())))`.
func (c *CommitCheckpointClient) Intercept(interceptors ...Interceptor) {
	c.inters.CommitCheckpoint = append(c.inters.CommitCheckpoint, interceptors...)
}

// Create returns a builder for creating a CommitCheckpoint entity.
func (c *CommitCheckpointClient) Create() *CommitCheckpointCreate {
	mutation := newCommitCheckpointMutation(c.config, OpCreate)
	return &CommitCheckpointCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CommitCheckpoint entities.
func (c *CommitCheckpointClient) CreateBulk(builders ...*CommitCheckpointCreate) *CommitCheckpointCreateBulk {
	return &CommitCheckpointCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CommitCheckpointClient) MapCreateBulk(slice any, setFunc func(*CommitCheckpointCreate, int)) *CommitCheckpointCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CommitCheckpointCreateBulk{err: fmt.Errorf("calling to CommitCheckpointClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CommitCheckpointCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CommitCheckpointCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CommitCheckpoint.
func (c *CommitCheckpointClient) Update() *CommitCheckpointUpdate {
	mutation := newCommitCheckpointMutation(c.config, OpUpdate)
	return &CommitCheckpointUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CommitCheckpointClient) UpdateOne(cc *CommitCheckpoint) *CommitCheckpointUpdateOne {
	mutation := newCommitCheckpointMutation(c.config, OpUpdateOne, withCommitCheckpoint(cc))
	return &CommitCheckpointUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CommitCheckpointClient) UpdateOneID(id int) *CommitCheckpointUpdateOne {
	mutation := newCommitCheckpointMutation(c.config, OpUpdateOne, withCommitCheckpointID(id))
	return &CommitCheckpointUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CommitCheckpoint.
func (c *CommitCheckpointClient) Delete() *CommitCheckpointDelete {
	mutation := newCommitCheckpointMutation(c.config, OpDelete)
	return &CommitCheckpointDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CommitCheckpointClient) DeleteOne(cc *CommitCheckpoint) *CommitCheckpointDeleteOne {
	return c.DeleteOneID(cc.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CommitCheckpointClient) DeleteOneID(id int) *CommitCheckpointDeleteOne {
	builder := c.Delete().Where(commitcheckpoint.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CommitCheckpointDeleteOne{builder}
}

// Query returns a query builder for CommitCheckpoint.
func (c *CommitCheckpointClient) Query() *CommitCheckpointQuery {
	return &CommitCheckpointQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCommitCheckpoint},
		inters: c.Interceptors(),
	}
}

// Get returns a CommitCheckpoint entity by its id.
func (c *CommitCheckpointClient) Get(ctx context.Context, id int) (*CommitCheckpoint, error) {
	return c.Query().Where(commitcheckpoint.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CommitCheckpointClient) GetX(ctx context.Context, id int) *CommitCheckpoint {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a CommitCheckpoint.
func (c *CommitCheckpointClient) QuerySession(cc *CommitCheckpoint) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(commitcheckpoint.Table, commitcheckpoint.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitcheckpoint.SessionTable, commitcheckpoint.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(cc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryRepoConfig queries the repo_config edge of a CommitCheckpoint.
func (c *CommitCheckpointClient) QueryRepoConfig(cc *CommitCheckpoint) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(commitcheckpoint.Table, commitcheckpoint.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitcheckpoint.RepoConfigTable, commitcheckpoint.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(cc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CommitCheckpointClient) Hooks() []Hook {
	return c.hooks.CommitCheckpoint
}

// Interceptors returns the client interceptors.
func (c *CommitCheckpointClient) Interceptors() []Interceptor {
	return c.inters.CommitCheckpoint
}

func (c *CommitCheckpointClient) mutate(ctx context.Context, m *CommitCheckpointMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CommitCheckpointCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CommitCheckpointUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CommitCheckpointUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CommitCheckpointDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CommitCheckpoint mutation op: %q", m.Op())
	}
}

// CommitRewriteClient is a client for the CommitRewrite schema.
type CommitRewriteClient struct {
	config
}

// NewCommitRewriteClient returns a client for the CommitRewrite from the given config.
func NewCommitRewriteClient(c config) *CommitRewriteClient {
	return &CommitRewriteClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `commitrewrite.Hooks(f(g(h())))`.
func (c *CommitRewriteClient) Use(hooks ...Hook) {
	c.hooks.CommitRewrite = append(c.hooks.CommitRewrite, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `commitrewrite.Intercept(f(g(h())))`.
func (c *CommitRewriteClient) Intercept(interceptors ...Interceptor) {
	c.inters.CommitRewrite = append(c.inters.CommitRewrite, interceptors...)
}

// Create returns a builder for creating a CommitRewrite entity.
func (c *CommitRewriteClient) Create() *CommitRewriteCreate {
	mutation := newCommitRewriteMutation(c.config, OpCreate)
	return &CommitRewriteCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CommitRewrite entities.
func (c *CommitRewriteClient) CreateBulk(builders ...*CommitRewriteCreate) *CommitRewriteCreateBulk {
	return &CommitRewriteCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CommitRewriteClient) MapCreateBulk(slice any, setFunc func(*CommitRewriteCreate, int)) *CommitRewriteCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CommitRewriteCreateBulk{err: fmt.Errorf("calling to CommitRewriteClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CommitRewriteCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CommitRewriteCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CommitRewrite.
func (c *CommitRewriteClient) Update() *CommitRewriteUpdate {
	mutation := newCommitRewriteMutation(c.config, OpUpdate)
	return &CommitRewriteUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CommitRewriteClient) UpdateOne(cr *CommitRewrite) *CommitRewriteUpdateOne {
	mutation := newCommitRewriteMutation(c.config, OpUpdateOne, withCommitRewrite(cr))
	return &CommitRewriteUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CommitRewriteClient) UpdateOneID(id int) *CommitRewriteUpdateOne {
	mutation := newCommitRewriteMutation(c.config, OpUpdateOne, withCommitRewriteID(id))
	return &CommitRewriteUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CommitRewrite.
func (c *CommitRewriteClient) Delete() *CommitRewriteDelete {
	mutation := newCommitRewriteMutation(c.config, OpDelete)
	return &CommitRewriteDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CommitRewriteClient) DeleteOne(cr *CommitRewrite) *CommitRewriteDeleteOne {
	return c.DeleteOneID(cr.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CommitRewriteClient) DeleteOneID(id int) *CommitRewriteDeleteOne {
	builder := c.Delete().Where(commitrewrite.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CommitRewriteDeleteOne{builder}
}

// Query returns a query builder for CommitRewrite.
func (c *CommitRewriteClient) Query() *CommitRewriteQuery {
	return &CommitRewriteQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCommitRewrite},
		inters: c.Interceptors(),
	}
}

// Get returns a CommitRewrite entity by its id.
func (c *CommitRewriteClient) Get(ctx context.Context, id int) (*CommitRewrite, error) {
	return c.Query().Where(commitrewrite.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CommitRewriteClient) GetX(ctx context.Context, id int) *CommitRewrite {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a CommitRewrite.
func (c *CommitRewriteClient) QuerySession(cr *CommitRewrite) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(commitrewrite.Table, commitrewrite.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitrewrite.SessionTable, commitrewrite.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(cr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryRepoConfig queries the repo_config edge of a CommitRewrite.
func (c *CommitRewriteClient) QueryRepoConfig(cr *CommitRewrite) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(commitrewrite.Table, commitrewrite.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitrewrite.RepoConfigTable, commitrewrite.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(cr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CommitRewriteClient) Hooks() []Hook {
	return c.hooks.CommitRewrite
}

// Interceptors returns the client interceptors.
func (c *CommitRewriteClient) Interceptors() []Interceptor {
	return c.inters.CommitRewrite
}

func (c *CommitRewriteClient) mutate(ctx context.Context, m *CommitRewriteMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CommitRewriteCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CommitRewriteUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CommitRewriteUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CommitRewriteDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CommitRewrite mutation op: %q", m.Op())
	}
}

// CredentialClient is a client for the Credential schema.
type CredentialClient struct {
	config
}

// NewCredentialClient returns a client for the Credential from the given config.
func NewCredentialClient(c config) *CredentialClient {
	return &CredentialClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `credential.Hooks(f(g(h())))`.
func (c *CredentialClient) Use(hooks ...Hook) {
	c.hooks.Credential = append(c.hooks.Credential, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `credential.Intercept(f(g(h())))`.
func (c *CredentialClient) Intercept(interceptors ...Interceptor) {
	c.inters.Credential = append(c.inters.Credential, interceptors...)
}

// Create returns a builder for creating a Credential entity.
func (c *CredentialClient) Create() *CredentialCreate {
	mutation := newCredentialMutation(c.config, OpCreate)
	return &CredentialCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Credential entities.
func (c *CredentialClient) CreateBulk(builders ...*CredentialCreate) *CredentialCreateBulk {
	return &CredentialCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CredentialClient) MapCreateBulk(slice any, setFunc func(*CredentialCreate, int)) *CredentialCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CredentialCreateBulk{err: fmt.Errorf("calling to CredentialClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CredentialCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CredentialCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Credential.
func (c *CredentialClient) Update() *CredentialUpdate {
	mutation := newCredentialMutation(c.config, OpUpdate)
	return &CredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CredentialClient) UpdateOne(cr *Credential) *CredentialUpdateOne {
	mutation := newCredentialMutation(c.config, OpUpdateOne, withCredential(cr))
	return &CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CredentialClient) UpdateOneID(id int) *CredentialUpdateOne {
	mutation := newCredentialMutation(c.config, OpUpdateOne, withCredentialID(id))
	return &CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Credential.
func (c *CredentialClient) Delete() *CredentialDelete {
	mutation := newCredentialMutation(c.config, OpDelete)
	return &CredentialDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CredentialClient) DeleteOne(cr *Credential) *CredentialDeleteOne {
	return c.DeleteOneID(cr.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CredentialClient) DeleteOneID(id int) *CredentialDeleteOne {
	builder := c.Delete().Where(credential.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CredentialDeleteOne{builder}
}

// Query returns a query builder for Credential.
func (c *CredentialClient) Query() *CredentialQuery {
	return &CredentialQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCredential},
		inters: c.Interceptors(),
	}
}

// Get returns a Credential entity by its id.
func (c *CredentialClient) Get(ctx context.Context, id int) (*Credential, error) {
	return c.Query().Where(credential.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CredentialClient) GetX(ctx context.Context, id int) *Credential {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryAPIScmProviders queries the api_scm_providers edge of a Credential.
func (c *CredentialClient) QueryAPIScmProviders(cr *Credential) *ScmProviderQuery {
	query := (&ScmProviderClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(credential.Table, credential.FieldID, id),
			sqlgraph.To(scmprovider.Table, scmprovider.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, credential.APIScmProvidersTable, credential.APIScmProvidersColumn),
		)
		fromV = sqlgraph.Neighbors(cr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCloneScmProviders queries the clone_scm_providers edge of a Credential.
func (c *CredentialClient) QueryCloneScmProviders(cr *Credential) *ScmProviderQuery {
	query := (&ScmProviderClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(credential.Table, credential.FieldID, id),
			sqlgraph.To(scmprovider.Table, scmprovider.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, credential.CloneScmProvidersTable, credential.CloneScmProvidersColumn),
		)
		fromV = sqlgraph.Neighbors(cr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CredentialClient) Hooks() []Hook {
	return c.hooks.Credential
}

// Interceptors returns the client interceptors.
func (c *CredentialClient) Interceptors() []Interceptor {
	return c.inters.Credential
}

func (c *CredentialClient) mutate(ctx context.Context, m *CredentialMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CredentialCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CredentialUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CredentialUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CredentialDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Credential mutation op: %q", m.Op())
	}
}

// EfficiencyMetricClient is a client for the EfficiencyMetric schema.
type EfficiencyMetricClient struct {
	config
}

// NewEfficiencyMetricClient returns a client for the EfficiencyMetric from the given config.
func NewEfficiencyMetricClient(c config) *EfficiencyMetricClient {
	return &EfficiencyMetricClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `efficiencymetric.Hooks(f(g(h())))`.
func (c *EfficiencyMetricClient) Use(hooks ...Hook) {
	c.hooks.EfficiencyMetric = append(c.hooks.EfficiencyMetric, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `efficiencymetric.Intercept(f(g(h())))`.
func (c *EfficiencyMetricClient) Intercept(interceptors ...Interceptor) {
	c.inters.EfficiencyMetric = append(c.inters.EfficiencyMetric, interceptors...)
}

// Create returns a builder for creating a EfficiencyMetric entity.
func (c *EfficiencyMetricClient) Create() *EfficiencyMetricCreate {
	mutation := newEfficiencyMetricMutation(c.config, OpCreate)
	return &EfficiencyMetricCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of EfficiencyMetric entities.
func (c *EfficiencyMetricClient) CreateBulk(builders ...*EfficiencyMetricCreate) *EfficiencyMetricCreateBulk {
	return &EfficiencyMetricCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *EfficiencyMetricClient) MapCreateBulk(slice any, setFunc func(*EfficiencyMetricCreate, int)) *EfficiencyMetricCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &EfficiencyMetricCreateBulk{err: fmt.Errorf("calling to EfficiencyMetricClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*EfficiencyMetricCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &EfficiencyMetricCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for EfficiencyMetric.
func (c *EfficiencyMetricClient) Update() *EfficiencyMetricUpdate {
	mutation := newEfficiencyMetricMutation(c.config, OpUpdate)
	return &EfficiencyMetricUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *EfficiencyMetricClient) UpdateOne(em *EfficiencyMetric) *EfficiencyMetricUpdateOne {
	mutation := newEfficiencyMetricMutation(c.config, OpUpdateOne, withEfficiencyMetric(em))
	return &EfficiencyMetricUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *EfficiencyMetricClient) UpdateOneID(id int) *EfficiencyMetricUpdateOne {
	mutation := newEfficiencyMetricMutation(c.config, OpUpdateOne, withEfficiencyMetricID(id))
	return &EfficiencyMetricUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for EfficiencyMetric.
func (c *EfficiencyMetricClient) Delete() *EfficiencyMetricDelete {
	mutation := newEfficiencyMetricMutation(c.config, OpDelete)
	return &EfficiencyMetricDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *EfficiencyMetricClient) DeleteOne(em *EfficiencyMetric) *EfficiencyMetricDeleteOne {
	return c.DeleteOneID(em.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *EfficiencyMetricClient) DeleteOneID(id int) *EfficiencyMetricDeleteOne {
	builder := c.Delete().Where(efficiencymetric.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &EfficiencyMetricDeleteOne{builder}
}

// Query returns a query builder for EfficiencyMetric.
func (c *EfficiencyMetricClient) Query() *EfficiencyMetricQuery {
	return &EfficiencyMetricQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeEfficiencyMetric},
		inters: c.Interceptors(),
	}
}

// Get returns a EfficiencyMetric entity by its id.
func (c *EfficiencyMetricClient) Get(ctx context.Context, id int) (*EfficiencyMetric, error) {
	return c.Query().Where(efficiencymetric.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *EfficiencyMetricClient) GetX(ctx context.Context, id int) *EfficiencyMetric {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryRepoConfig queries the repo_config edge of a EfficiencyMetric.
func (c *EfficiencyMetricClient) QueryRepoConfig(em *EfficiencyMetric) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := em.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(efficiencymetric.Table, efficiencymetric.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, efficiencymetric.RepoConfigTable, efficiencymetric.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(em.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *EfficiencyMetricClient) Hooks() []Hook {
	return c.hooks.EfficiencyMetric
}

// Interceptors returns the client interceptors.
func (c *EfficiencyMetricClient) Interceptors() []Interceptor {
	return c.inters.EfficiencyMetric
}

func (c *EfficiencyMetricClient) mutate(ctx context.Context, m *EfficiencyMetricMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&EfficiencyMetricCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&EfficiencyMetricUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&EfficiencyMetricUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&EfficiencyMetricDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown EfficiencyMetric mutation op: %q", m.Op())
	}
}

// PrAttributionRunClient is a client for the PrAttributionRun schema.
type PrAttributionRunClient struct {
	config
}

// NewPrAttributionRunClient returns a client for the PrAttributionRun from the given config.
func NewPrAttributionRunClient(c config) *PrAttributionRunClient {
	return &PrAttributionRunClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `prattributionrun.Hooks(f(g(h())))`.
func (c *PrAttributionRunClient) Use(hooks ...Hook) {
	c.hooks.PrAttributionRun = append(c.hooks.PrAttributionRun, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `prattributionrun.Intercept(f(g(h())))`.
func (c *PrAttributionRunClient) Intercept(interceptors ...Interceptor) {
	c.inters.PrAttributionRun = append(c.inters.PrAttributionRun, interceptors...)
}

// Create returns a builder for creating a PrAttributionRun entity.
func (c *PrAttributionRunClient) Create() *PrAttributionRunCreate {
	mutation := newPrAttributionRunMutation(c.config, OpCreate)
	return &PrAttributionRunCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PrAttributionRun entities.
func (c *PrAttributionRunClient) CreateBulk(builders ...*PrAttributionRunCreate) *PrAttributionRunCreateBulk {
	return &PrAttributionRunCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PrAttributionRunClient) MapCreateBulk(slice any, setFunc func(*PrAttributionRunCreate, int)) *PrAttributionRunCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PrAttributionRunCreateBulk{err: fmt.Errorf("calling to PrAttributionRunClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PrAttributionRunCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PrAttributionRunCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PrAttributionRun.
func (c *PrAttributionRunClient) Update() *PrAttributionRunUpdate {
	mutation := newPrAttributionRunMutation(c.config, OpUpdate)
	return &PrAttributionRunUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PrAttributionRunClient) UpdateOne(par *PrAttributionRun) *PrAttributionRunUpdateOne {
	mutation := newPrAttributionRunMutation(c.config, OpUpdateOne, withPrAttributionRun(par))
	return &PrAttributionRunUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PrAttributionRunClient) UpdateOneID(id int) *PrAttributionRunUpdateOne {
	mutation := newPrAttributionRunMutation(c.config, OpUpdateOne, withPrAttributionRunID(id))
	return &PrAttributionRunUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PrAttributionRun.
func (c *PrAttributionRunClient) Delete() *PrAttributionRunDelete {
	mutation := newPrAttributionRunMutation(c.config, OpDelete)
	return &PrAttributionRunDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PrAttributionRunClient) DeleteOne(par *PrAttributionRun) *PrAttributionRunDeleteOne {
	return c.DeleteOneID(par.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PrAttributionRunClient) DeleteOneID(id int) *PrAttributionRunDeleteOne {
	builder := c.Delete().Where(prattributionrun.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PrAttributionRunDeleteOne{builder}
}

// Query returns a query builder for PrAttributionRun.
func (c *PrAttributionRunClient) Query() *PrAttributionRunQuery {
	return &PrAttributionRunQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePrAttributionRun},
		inters: c.Interceptors(),
	}
}

// Get returns a PrAttributionRun entity by its id.
func (c *PrAttributionRunClient) Get(ctx context.Context, id int) (*PrAttributionRun, error) {
	return c.Query().Where(prattributionrun.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PrAttributionRunClient) GetX(ctx context.Context, id int) *PrAttributionRun {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryPrRecord queries the pr_record edge of a PrAttributionRun.
func (c *PrAttributionRunClient) QueryPrRecord(par *PrAttributionRun) *PrRecordQuery {
	query := (&PrRecordClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := par.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(prattributionrun.Table, prattributionrun.FieldID, id),
			sqlgraph.To(prrecord.Table, prrecord.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, prattributionrun.PrRecordTable, prattributionrun.PrRecordColumn),
		)
		fromV = sqlgraph.Neighbors(par.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *PrAttributionRunClient) Hooks() []Hook {
	hooks := c.hooks.PrAttributionRun
	return append(hooks[:len(hooks):len(hooks)], prattributionrun.Hooks[:]...)
}

// Interceptors returns the client interceptors.
func (c *PrAttributionRunClient) Interceptors() []Interceptor {
	return c.inters.PrAttributionRun
}

func (c *PrAttributionRunClient) mutate(ctx context.Context, m *PrAttributionRunMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PrAttributionRunCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PrAttributionRunUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PrAttributionRunUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PrAttributionRunDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PrAttributionRun mutation op: %q", m.Op())
	}
}

// PrRecordClient is a client for the PrRecord schema.
type PrRecordClient struct {
	config
}

// NewPrRecordClient returns a client for the PrRecord from the given config.
func NewPrRecordClient(c config) *PrRecordClient {
	return &PrRecordClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `prrecord.Hooks(f(g(h())))`.
func (c *PrRecordClient) Use(hooks ...Hook) {
	c.hooks.PrRecord = append(c.hooks.PrRecord, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `prrecord.Intercept(f(g(h())))`.
func (c *PrRecordClient) Intercept(interceptors ...Interceptor) {
	c.inters.PrRecord = append(c.inters.PrRecord, interceptors...)
}

// Create returns a builder for creating a PrRecord entity.
func (c *PrRecordClient) Create() *PrRecordCreate {
	mutation := newPrRecordMutation(c.config, OpCreate)
	return &PrRecordCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PrRecord entities.
func (c *PrRecordClient) CreateBulk(builders ...*PrRecordCreate) *PrRecordCreateBulk {
	return &PrRecordCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PrRecordClient) MapCreateBulk(slice any, setFunc func(*PrRecordCreate, int)) *PrRecordCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PrRecordCreateBulk{err: fmt.Errorf("calling to PrRecordClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PrRecordCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PrRecordCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PrRecord.
func (c *PrRecordClient) Update() *PrRecordUpdate {
	mutation := newPrRecordMutation(c.config, OpUpdate)
	return &PrRecordUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PrRecordClient) UpdateOne(pr *PrRecord) *PrRecordUpdateOne {
	mutation := newPrRecordMutation(c.config, OpUpdateOne, withPrRecord(pr))
	return &PrRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PrRecordClient) UpdateOneID(id int) *PrRecordUpdateOne {
	mutation := newPrRecordMutation(c.config, OpUpdateOne, withPrRecordID(id))
	return &PrRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PrRecord.
func (c *PrRecordClient) Delete() *PrRecordDelete {
	mutation := newPrRecordMutation(c.config, OpDelete)
	return &PrRecordDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PrRecordClient) DeleteOne(pr *PrRecord) *PrRecordDeleteOne {
	return c.DeleteOneID(pr.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PrRecordClient) DeleteOneID(id int) *PrRecordDeleteOne {
	builder := c.Delete().Where(prrecord.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PrRecordDeleteOne{builder}
}

// Query returns a query builder for PrRecord.
func (c *PrRecordClient) Query() *PrRecordQuery {
	return &PrRecordQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePrRecord},
		inters: c.Interceptors(),
	}
}

// Get returns a PrRecord entity by its id.
func (c *PrRecordClient) Get(ctx context.Context, id int) (*PrRecord, error) {
	return c.Query().Where(prrecord.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PrRecordClient) GetX(ctx context.Context, id int) *PrRecord {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryRepoConfig queries the repo_config edge of a PrRecord.
func (c *PrRecordClient) QueryRepoConfig(pr *PrRecord) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(prrecord.Table, prrecord.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, prrecord.RepoConfigTable, prrecord.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(pr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryAttributionRuns queries the attribution_runs edge of a PrRecord.
func (c *PrRecordClient) QueryAttributionRuns(pr *PrRecord) *PrAttributionRunQuery {
	query := (&PrAttributionRunClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(prrecord.Table, prrecord.FieldID, id),
			sqlgraph.To(prattributionrun.Table, prattributionrun.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, prrecord.AttributionRunsTable, prrecord.AttributionRunsColumn),
		)
		fromV = sqlgraph.Neighbors(pr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryLastAttributionRun queries the last_attribution_run edge of a PrRecord.
func (c *PrRecordClient) QueryLastAttributionRun(pr *PrRecord) *PrAttributionRunQuery {
	query := (&PrAttributionRunClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pr.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(prrecord.Table, prrecord.FieldID, id),
			sqlgraph.To(prattributionrun.Table, prattributionrun.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, prrecord.LastAttributionRunTable, prrecord.LastAttributionRunColumn),
		)
		fromV = sqlgraph.Neighbors(pr.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *PrRecordClient) Hooks() []Hook {
	hooks := c.hooks.PrRecord
	return append(hooks[:len(hooks):len(hooks)], prrecord.Hooks[:]...)
}

// Interceptors returns the client interceptors.
func (c *PrRecordClient) Interceptors() []Interceptor {
	return c.inters.PrRecord
}

func (c *PrRecordClient) mutate(ctx context.Context, m *PrRecordMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PrRecordCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PrRecordUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PrRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PrRecordDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PrRecord mutation op: %q", m.Op())
	}
}

// RelayProviderClient is a client for the RelayProvider schema.
type RelayProviderClient struct {
	config
}

// NewRelayProviderClient returns a client for the RelayProvider from the given config.
func NewRelayProviderClient(c config) *RelayProviderClient {
	return &RelayProviderClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `relayprovider.Hooks(f(g(h())))`.
func (c *RelayProviderClient) Use(hooks ...Hook) {
	c.hooks.RelayProvider = append(c.hooks.RelayProvider, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `relayprovider.Intercept(f(g(h())))`.
func (c *RelayProviderClient) Intercept(interceptors ...Interceptor) {
	c.inters.RelayProvider = append(c.inters.RelayProvider, interceptors...)
}

// Create returns a builder for creating a RelayProvider entity.
func (c *RelayProviderClient) Create() *RelayProviderCreate {
	mutation := newRelayProviderMutation(c.config, OpCreate)
	return &RelayProviderCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of RelayProvider entities.
func (c *RelayProviderClient) CreateBulk(builders ...*RelayProviderCreate) *RelayProviderCreateBulk {
	return &RelayProviderCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *RelayProviderClient) MapCreateBulk(slice any, setFunc func(*RelayProviderCreate, int)) *RelayProviderCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &RelayProviderCreateBulk{err: fmt.Errorf("calling to RelayProviderClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*RelayProviderCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &RelayProviderCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for RelayProvider.
func (c *RelayProviderClient) Update() *RelayProviderUpdate {
	mutation := newRelayProviderMutation(c.config, OpUpdate)
	return &RelayProviderUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *RelayProviderClient) UpdateOne(rp *RelayProvider) *RelayProviderUpdateOne {
	mutation := newRelayProviderMutation(c.config, OpUpdateOne, withRelayProvider(rp))
	return &RelayProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *RelayProviderClient) UpdateOneID(id int) *RelayProviderUpdateOne {
	mutation := newRelayProviderMutation(c.config, OpUpdateOne, withRelayProviderID(id))
	return &RelayProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for RelayProvider.
func (c *RelayProviderClient) Delete() *RelayProviderDelete {
	mutation := newRelayProviderMutation(c.config, OpDelete)
	return &RelayProviderDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *RelayProviderClient) DeleteOne(rp *RelayProvider) *RelayProviderDeleteOne {
	return c.DeleteOneID(rp.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *RelayProviderClient) DeleteOneID(id int) *RelayProviderDeleteOne {
	builder := c.Delete().Where(relayprovider.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &RelayProviderDeleteOne{builder}
}

// Query returns a query builder for RelayProvider.
func (c *RelayProviderClient) Query() *RelayProviderQuery {
	return &RelayProviderQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeRelayProvider},
		inters: c.Interceptors(),
	}
}

// Get returns a RelayProvider entity by its id.
func (c *RelayProviderClient) Get(ctx context.Context, id int) (*RelayProvider, error) {
	return c.Query().Where(relayprovider.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *RelayProviderClient) GetX(ctx context.Context, id int) *RelayProvider {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *RelayProviderClient) Hooks() []Hook {
	return c.hooks.RelayProvider
}

// Interceptors returns the client interceptors.
func (c *RelayProviderClient) Interceptors() []Interceptor {
	return c.inters.RelayProvider
}

func (c *RelayProviderClient) mutate(ctx context.Context, m *RelayProviderMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&RelayProviderCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&RelayProviderUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&RelayProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&RelayProviderDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown RelayProvider mutation op: %q", m.Op())
	}
}

// RepoConfigClient is a client for the RepoConfig schema.
type RepoConfigClient struct {
	config
}

// NewRepoConfigClient returns a client for the RepoConfig from the given config.
func NewRepoConfigClient(c config) *RepoConfigClient {
	return &RepoConfigClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `repoconfig.Hooks(f(g(h())))`.
func (c *RepoConfigClient) Use(hooks ...Hook) {
	c.hooks.RepoConfig = append(c.hooks.RepoConfig, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `repoconfig.Intercept(f(g(h())))`.
func (c *RepoConfigClient) Intercept(interceptors ...Interceptor) {
	c.inters.RepoConfig = append(c.inters.RepoConfig, interceptors...)
}

// Create returns a builder for creating a RepoConfig entity.
func (c *RepoConfigClient) Create() *RepoConfigCreate {
	mutation := newRepoConfigMutation(c.config, OpCreate)
	return &RepoConfigCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of RepoConfig entities.
func (c *RepoConfigClient) CreateBulk(builders ...*RepoConfigCreate) *RepoConfigCreateBulk {
	return &RepoConfigCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *RepoConfigClient) MapCreateBulk(slice any, setFunc func(*RepoConfigCreate, int)) *RepoConfigCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &RepoConfigCreateBulk{err: fmt.Errorf("calling to RepoConfigClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*RepoConfigCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &RepoConfigCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for RepoConfig.
func (c *RepoConfigClient) Update() *RepoConfigUpdate {
	mutation := newRepoConfigMutation(c.config, OpUpdate)
	return &RepoConfigUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *RepoConfigClient) UpdateOne(rc *RepoConfig) *RepoConfigUpdateOne {
	mutation := newRepoConfigMutation(c.config, OpUpdateOne, withRepoConfig(rc))
	return &RepoConfigUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *RepoConfigClient) UpdateOneID(id int) *RepoConfigUpdateOne {
	mutation := newRepoConfigMutation(c.config, OpUpdateOne, withRepoConfigID(id))
	return &RepoConfigUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for RepoConfig.
func (c *RepoConfigClient) Delete() *RepoConfigDelete {
	mutation := newRepoConfigMutation(c.config, OpDelete)
	return &RepoConfigDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *RepoConfigClient) DeleteOne(rc *RepoConfig) *RepoConfigDeleteOne {
	return c.DeleteOneID(rc.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *RepoConfigClient) DeleteOneID(id int) *RepoConfigDeleteOne {
	builder := c.Delete().Where(repoconfig.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &RepoConfigDeleteOne{builder}
}

// Query returns a query builder for RepoConfig.
func (c *RepoConfigClient) Query() *RepoConfigQuery {
	return &RepoConfigQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeRepoConfig},
		inters: c.Interceptors(),
	}
}

// Get returns a RepoConfig entity by its id.
func (c *RepoConfigClient) Get(ctx context.Context, id int) (*RepoConfig, error) {
	return c.Query().Where(repoconfig.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *RepoConfigClient) GetX(ctx context.Context, id int) *RepoConfig {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryScmProvider queries the scm_provider edge of a RepoConfig.
func (c *RepoConfigClient) QueryScmProvider(rc *RepoConfig) *ScmProviderQuery {
	query := (&ScmProviderClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(scmprovider.Table, scmprovider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, repoconfig.ScmProviderTable, repoconfig.ScmProviderColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QuerySessions queries the sessions edge of a RepoConfig.
func (c *RepoConfigClient) QuerySessions(rc *RepoConfig) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.SessionsTable, repoconfig.SessionsColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCommitCheckpoints queries the commit_checkpoints edge of a RepoConfig.
func (c *RepoConfigClient) QueryCommitCheckpoints(rc *RepoConfig) *CommitCheckpointQuery {
	query := (&CommitCheckpointClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(commitcheckpoint.Table, commitcheckpoint.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.CommitCheckpointsTable, repoconfig.CommitCheckpointsColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCommitRewrites queries the commit_rewrites edge of a RepoConfig.
func (c *RepoConfigClient) QueryCommitRewrites(rc *RepoConfig) *CommitRewriteQuery {
	query := (&CommitRewriteClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(commitrewrite.Table, commitrewrite.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.CommitRewritesTable, repoconfig.CommitRewritesColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryWebhookDeadLetters queries the webhook_dead_letters edge of a RepoConfig.
func (c *RepoConfigClient) QueryWebhookDeadLetters(rc *RepoConfig) *WebhookDeadLetterQuery {
	query := (&WebhookDeadLetterClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(webhookdeadletter.Table, webhookdeadletter.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.WebhookDeadLettersTable, repoconfig.WebhookDeadLettersColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryAiScanResults queries the ai_scan_results edge of a RepoConfig.
func (c *RepoConfigClient) QueryAiScanResults(rc *RepoConfig) *AiScanResultQuery {
	query := (&AiScanResultClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(aiscanresult.Table, aiscanresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.AiScanResultsTable, repoconfig.AiScanResultsColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPrRecords queries the pr_records edge of a RepoConfig.
func (c *RepoConfigClient) QueryPrRecords(rc *RepoConfig) *PrRecordQuery {
	query := (&PrRecordClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(prrecord.Table, prrecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.PrRecordsTable, repoconfig.PrRecordsColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryEfficiencyMetrics queries the efficiency_metrics edge of a RepoConfig.
func (c *RepoConfigClient) QueryEfficiencyMetrics(rc *RepoConfig) *EfficiencyMetricQuery {
	query := (&EfficiencyMetricClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := rc.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(repoconfig.Table, repoconfig.FieldID, id),
			sqlgraph.To(efficiencymetric.Table, efficiencymetric.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, repoconfig.EfficiencyMetricsTable, repoconfig.EfficiencyMetricsColumn),
		)
		fromV = sqlgraph.Neighbors(rc.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *RepoConfigClient) Hooks() []Hook {
	return c.hooks.RepoConfig
}

// Interceptors returns the client interceptors.
func (c *RepoConfigClient) Interceptors() []Interceptor {
	return c.inters.RepoConfig
}

func (c *RepoConfigClient) mutate(ctx context.Context, m *RepoConfigMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&RepoConfigCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&RepoConfigUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&RepoConfigUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&RepoConfigDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown RepoConfig mutation op: %q", m.Op())
	}
}

// ScmProviderClient is a client for the ScmProvider schema.
type ScmProviderClient struct {
	config
}

// NewScmProviderClient returns a client for the ScmProvider from the given config.
func NewScmProviderClient(c config) *ScmProviderClient {
	return &ScmProviderClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `scmprovider.Hooks(f(g(h())))`.
func (c *ScmProviderClient) Use(hooks ...Hook) {
	c.hooks.ScmProvider = append(c.hooks.ScmProvider, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `scmprovider.Intercept(f(g(h())))`.
func (c *ScmProviderClient) Intercept(interceptors ...Interceptor) {
	c.inters.ScmProvider = append(c.inters.ScmProvider, interceptors...)
}

// Create returns a builder for creating a ScmProvider entity.
func (c *ScmProviderClient) Create() *ScmProviderCreate {
	mutation := newScmProviderMutation(c.config, OpCreate)
	return &ScmProviderCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ScmProvider entities.
func (c *ScmProviderClient) CreateBulk(builders ...*ScmProviderCreate) *ScmProviderCreateBulk {
	return &ScmProviderCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ScmProviderClient) MapCreateBulk(slice any, setFunc func(*ScmProviderCreate, int)) *ScmProviderCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ScmProviderCreateBulk{err: fmt.Errorf("calling to ScmProviderClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ScmProviderCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ScmProviderCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ScmProvider.
func (c *ScmProviderClient) Update() *ScmProviderUpdate {
	mutation := newScmProviderMutation(c.config, OpUpdate)
	return &ScmProviderUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ScmProviderClient) UpdateOne(sp *ScmProvider) *ScmProviderUpdateOne {
	mutation := newScmProviderMutation(c.config, OpUpdateOne, withScmProvider(sp))
	return &ScmProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ScmProviderClient) UpdateOneID(id int) *ScmProviderUpdateOne {
	mutation := newScmProviderMutation(c.config, OpUpdateOne, withScmProviderID(id))
	return &ScmProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ScmProvider.
func (c *ScmProviderClient) Delete() *ScmProviderDelete {
	mutation := newScmProviderMutation(c.config, OpDelete)
	return &ScmProviderDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ScmProviderClient) DeleteOne(sp *ScmProvider) *ScmProviderDeleteOne {
	return c.DeleteOneID(sp.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ScmProviderClient) DeleteOneID(id int) *ScmProviderDeleteOne {
	builder := c.Delete().Where(scmprovider.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ScmProviderDeleteOne{builder}
}

// Query returns a query builder for ScmProvider.
func (c *ScmProviderClient) Query() *ScmProviderQuery {
	return &ScmProviderQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeScmProvider},
		inters: c.Interceptors(),
	}
}

// Get returns a ScmProvider entity by its id.
func (c *ScmProviderClient) Get(ctx context.Context, id int) (*ScmProvider, error) {
	return c.Query().Where(scmprovider.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ScmProviderClient) GetX(ctx context.Context, id int) *ScmProvider {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryAPICredential queries the api_credential edge of a ScmProvider.
func (c *ScmProviderClient) QueryAPICredential(sp *ScmProvider) *CredentialQuery {
	query := (&CredentialClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := sp.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(scmprovider.Table, scmprovider.FieldID, id),
			sqlgraph.To(credential.Table, credential.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, scmprovider.APICredentialTable, scmprovider.APICredentialColumn),
		)
		fromV = sqlgraph.Neighbors(sp.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCloneCredential queries the clone_credential edge of a ScmProvider.
func (c *ScmProviderClient) QueryCloneCredential(sp *ScmProvider) *CredentialQuery {
	query := (&CredentialClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := sp.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(scmprovider.Table, scmprovider.FieldID, id),
			sqlgraph.To(credential.Table, credential.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, scmprovider.CloneCredentialTable, scmprovider.CloneCredentialColumn),
		)
		fromV = sqlgraph.Neighbors(sp.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryRepoConfigs queries the repo_configs edge of a ScmProvider.
func (c *ScmProviderClient) QueryRepoConfigs(sp *ScmProvider) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := sp.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(scmprovider.Table, scmprovider.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, scmprovider.RepoConfigsTable, scmprovider.RepoConfigsColumn),
		)
		fromV = sqlgraph.Neighbors(sp.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *ScmProviderClient) Hooks() []Hook {
	return c.hooks.ScmProvider
}

// Interceptors returns the client interceptors.
func (c *ScmProviderClient) Interceptors() []Interceptor {
	return c.inters.ScmProvider
}

func (c *ScmProviderClient) mutate(ctx context.Context, m *ScmProviderMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ScmProviderCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ScmProviderUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ScmProviderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ScmProviderDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown ScmProvider mutation op: %q", m.Op())
	}
}

// SessionClient is a client for the Session schema.
type SessionClient struct {
	config
}

// NewSessionClient returns a client for the Session from the given config.
func NewSessionClient(c config) *SessionClient {
	return &SessionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `session.Hooks(f(g(h())))`.
func (c *SessionClient) Use(hooks ...Hook) {
	c.hooks.Session = append(c.hooks.Session, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `session.Intercept(f(g(h())))`.
func (c *SessionClient) Intercept(interceptors ...Interceptor) {
	c.inters.Session = append(c.inters.Session, interceptors...)
}

// Create returns a builder for creating a Session entity.
func (c *SessionClient) Create() *SessionCreate {
	mutation := newSessionMutation(c.config, OpCreate)
	return &SessionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Session entities.
func (c *SessionClient) CreateBulk(builders ...*SessionCreate) *SessionCreateBulk {
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionClient) MapCreateBulk(slice any, setFunc func(*SessionCreate, int)) *SessionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionCreateBulk{err: fmt.Errorf("calling to SessionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Session.
func (c *SessionClient) Update() *SessionUpdate {
	mutation := newSessionMutation(c.config, OpUpdate)
	return &SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionClient) UpdateOne(s *Session) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSession(s))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionClient) UpdateOneID(id uuid.UUID) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSessionID(id))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Session.
func (c *SessionClient) Delete() *SessionDelete {
	mutation := newSessionMutation(c.config, OpDelete)
	return &SessionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionClient) DeleteOne(s *Session) *SessionDeleteOne {
	return c.DeleteOneID(s.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionClient) DeleteOneID(id uuid.UUID) *SessionDeleteOne {
	builder := c.Delete().Where(session.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionDeleteOne{builder}
}

// Query returns a query builder for Session.
func (c *SessionClient) Query() *SessionQuery {
	return &SessionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSession},
		inters: c.Interceptors(),
	}
}

// Get returns a Session entity by its id.
func (c *SessionClient) Get(ctx context.Context, id uuid.UUID) (*Session, error) {
	return c.Query().Where(session.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionClient) GetX(ctx context.Context, id uuid.UUID) *Session {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryRepoConfig queries the repo_config edge of a Session.
func (c *SessionClient) QueryRepoConfig(s *Session) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, session.RepoConfigTable, session.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryUser queries the user edge of a Session.
func (c *SessionClient) QueryUser(s *Session) *UserQuery {
	query := (&UserClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, session.UserTable, session.UserColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QuerySessionWorkspaces queries the session_workspaces edge of a Session.
func (c *SessionClient) QuerySessionWorkspaces(s *Session) *SessionWorkspaceQuery {
	query := (&SessionWorkspaceClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(sessionworkspace.Table, sessionworkspace.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.SessionWorkspacesTable, session.SessionWorkspacesColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryAgentMetadataEvents queries the agent_metadata_events edge of a Session.
func (c *SessionClient) QueryAgentMetadataEvents(s *Session) *AgentMetadataEventQuery {
	query := (&AgentMetadataEventClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(agentmetadataevent.Table, agentmetadataevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.AgentMetadataEventsTable, session.AgentMetadataEventsColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QuerySessionUsageEvents queries the session_usage_events edge of a Session.
func (c *SessionClient) QuerySessionUsageEvents(s *Session) *SessionUsageEventQuery {
	query := (&SessionUsageEventClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(sessionusageevent.Table, sessionusageevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.SessionUsageEventsTable, session.SessionUsageEventsColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QuerySessionEvents queries the session_events edge of a Session.
func (c *SessionClient) QuerySessionEvents(s *Session) *SessionEventQuery {
	query := (&SessionEventClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(sessionevent.Table, sessionevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.SessionEventsTable, session.SessionEventsColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCommitCheckpoints queries the commit_checkpoints edge of a Session.
func (c *SessionClient) QueryCommitCheckpoints(s *Session) *CommitCheckpointQuery {
	query := (&CommitCheckpointClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(commitcheckpoint.Table, commitcheckpoint.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.CommitCheckpointsTable, session.CommitCheckpointsColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCommitRewrites queries the commit_rewrites edge of a Session.
func (c *SessionClient) QueryCommitRewrites(s *Session) *CommitRewriteQuery {
	query := (&CommitRewriteClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := s.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(session.Table, session.FieldID, id),
			sqlgraph.To(commitrewrite.Table, commitrewrite.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, session.CommitRewritesTable, session.CommitRewritesColumn),
		)
		fromV = sqlgraph.Neighbors(s.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionClient) Hooks() []Hook {
	return c.hooks.Session
}

// Interceptors returns the client interceptors.
func (c *SessionClient) Interceptors() []Interceptor {
	return c.inters.Session
}

func (c *SessionClient) mutate(ctx context.Context, m *SessionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Session mutation op: %q", m.Op())
	}
}

// SessionEventClient is a client for the SessionEvent schema.
type SessionEventClient struct {
	config
}

// NewSessionEventClient returns a client for the SessionEvent from the given config.
func NewSessionEventClient(c config) *SessionEventClient {
	return &SessionEventClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `sessionevent.Hooks(f(g(h())))`.
func (c *SessionEventClient) Use(hooks ...Hook) {
	c.hooks.SessionEvent = append(c.hooks.SessionEvent, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `sessionevent.Intercept(f(g(h())))`.
func (c *SessionEventClient) Intercept(interceptors ...Interceptor) {
	c.inters.SessionEvent = append(c.inters.SessionEvent, interceptors...)
}

// Create returns a builder for creating a SessionEvent entity.
func (c *SessionEventClient) Create() *SessionEventCreate {
	mutation := newSessionEventMutation(c.config, OpCreate)
	return &SessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SessionEvent entities.
func (c *SessionEventClient) CreateBulk(builders ...*SessionEventCreate) *SessionEventCreateBulk {
	return &SessionEventCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionEventClient) MapCreateBulk(slice any, setFunc func(*SessionEventCreate, int)) *SessionEventCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionEventCreateBulk{err: fmt.Errorf("calling to SessionEventClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionEventCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionEventCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SessionEvent.
func (c *SessionEventClient) Update() *SessionEventUpdate {
	mutation := newSessionEventMutation(c.config, OpUpdate)
	return &SessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionEventClient) UpdateOne(se *SessionEvent) *SessionEventUpdateOne {
	mutation := newSessionEventMutation(c.config, OpUpdateOne, withSessionEvent(se))
	return &SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionEventClient) UpdateOneID(id int) *SessionEventUpdateOne {
	mutation := newSessionEventMutation(c.config, OpUpdateOne, withSessionEventID(id))
	return &SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SessionEvent.
func (c *SessionEventClient) Delete() *SessionEventDelete {
	mutation := newSessionEventMutation(c.config, OpDelete)
	return &SessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionEventClient) DeleteOne(se *SessionEvent) *SessionEventDeleteOne {
	return c.DeleteOneID(se.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionEventClient) DeleteOneID(id int) *SessionEventDeleteOne {
	builder := c.Delete().Where(sessionevent.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionEventDeleteOne{builder}
}

// Query returns a query builder for SessionEvent.
func (c *SessionEventClient) Query() *SessionEventQuery {
	return &SessionEventQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSessionEvent},
		inters: c.Interceptors(),
	}
}

// Get returns a SessionEvent entity by its id.
func (c *SessionEventClient) Get(ctx context.Context, id int) (*SessionEvent, error) {
	return c.Query().Where(sessionevent.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionEventClient) GetX(ctx context.Context, id int) *SessionEvent {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a SessionEvent.
func (c *SessionEventClient) QuerySession(se *SessionEvent) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := se.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionevent.Table, sessionevent.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionevent.SessionTable, sessionevent.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(se.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionEventClient) Hooks() []Hook {
	return c.hooks.SessionEvent
}

// Interceptors returns the client interceptors.
func (c *SessionEventClient) Interceptors() []Interceptor {
	return c.inters.SessionEvent
}

func (c *SessionEventClient) mutate(ctx context.Context, m *SessionEventMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionEventCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionEventUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionEventDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SessionEvent mutation op: %q", m.Op())
	}
}

// SessionUsageEventClient is a client for the SessionUsageEvent schema.
type SessionUsageEventClient struct {
	config
}

// NewSessionUsageEventClient returns a client for the SessionUsageEvent from the given config.
func NewSessionUsageEventClient(c config) *SessionUsageEventClient {
	return &SessionUsageEventClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `sessionusageevent.Hooks(f(g(h())))`.
func (c *SessionUsageEventClient) Use(hooks ...Hook) {
	c.hooks.SessionUsageEvent = append(c.hooks.SessionUsageEvent, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `sessionusageevent.Intercept(f(g(h())))`.
func (c *SessionUsageEventClient) Intercept(interceptors ...Interceptor) {
	c.inters.SessionUsageEvent = append(c.inters.SessionUsageEvent, interceptors...)
}

// Create returns a builder for creating a SessionUsageEvent entity.
func (c *SessionUsageEventClient) Create() *SessionUsageEventCreate {
	mutation := newSessionUsageEventMutation(c.config, OpCreate)
	return &SessionUsageEventCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SessionUsageEvent entities.
func (c *SessionUsageEventClient) CreateBulk(builders ...*SessionUsageEventCreate) *SessionUsageEventCreateBulk {
	return &SessionUsageEventCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionUsageEventClient) MapCreateBulk(slice any, setFunc func(*SessionUsageEventCreate, int)) *SessionUsageEventCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionUsageEventCreateBulk{err: fmt.Errorf("calling to SessionUsageEventClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionUsageEventCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionUsageEventCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SessionUsageEvent.
func (c *SessionUsageEventClient) Update() *SessionUsageEventUpdate {
	mutation := newSessionUsageEventMutation(c.config, OpUpdate)
	return &SessionUsageEventUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionUsageEventClient) UpdateOne(sue *SessionUsageEvent) *SessionUsageEventUpdateOne {
	mutation := newSessionUsageEventMutation(c.config, OpUpdateOne, withSessionUsageEvent(sue))
	return &SessionUsageEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionUsageEventClient) UpdateOneID(id int) *SessionUsageEventUpdateOne {
	mutation := newSessionUsageEventMutation(c.config, OpUpdateOne, withSessionUsageEventID(id))
	return &SessionUsageEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SessionUsageEvent.
func (c *SessionUsageEventClient) Delete() *SessionUsageEventDelete {
	mutation := newSessionUsageEventMutation(c.config, OpDelete)
	return &SessionUsageEventDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionUsageEventClient) DeleteOne(sue *SessionUsageEvent) *SessionUsageEventDeleteOne {
	return c.DeleteOneID(sue.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionUsageEventClient) DeleteOneID(id int) *SessionUsageEventDeleteOne {
	builder := c.Delete().Where(sessionusageevent.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionUsageEventDeleteOne{builder}
}

// Query returns a query builder for SessionUsageEvent.
func (c *SessionUsageEventClient) Query() *SessionUsageEventQuery {
	return &SessionUsageEventQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSessionUsageEvent},
		inters: c.Interceptors(),
	}
}

// Get returns a SessionUsageEvent entity by its id.
func (c *SessionUsageEventClient) Get(ctx context.Context, id int) (*SessionUsageEvent, error) {
	return c.Query().Where(sessionusageevent.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionUsageEventClient) GetX(ctx context.Context, id int) *SessionUsageEvent {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a SessionUsageEvent.
func (c *SessionUsageEventClient) QuerySession(sue *SessionUsageEvent) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := sue.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionusageevent.Table, sessionusageevent.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionusageevent.SessionTable, sessionusageevent.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(sue.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionUsageEventClient) Hooks() []Hook {
	return c.hooks.SessionUsageEvent
}

// Interceptors returns the client interceptors.
func (c *SessionUsageEventClient) Interceptors() []Interceptor {
	return c.inters.SessionUsageEvent
}

func (c *SessionUsageEventClient) mutate(ctx context.Context, m *SessionUsageEventMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionUsageEventCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionUsageEventUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionUsageEventUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionUsageEventDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SessionUsageEvent mutation op: %q", m.Op())
	}
}

// SessionWorkspaceClient is a client for the SessionWorkspace schema.
type SessionWorkspaceClient struct {
	config
}

// NewSessionWorkspaceClient returns a client for the SessionWorkspace from the given config.
func NewSessionWorkspaceClient(c config) *SessionWorkspaceClient {
	return &SessionWorkspaceClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `sessionworkspace.Hooks(f(g(h())))`.
func (c *SessionWorkspaceClient) Use(hooks ...Hook) {
	c.hooks.SessionWorkspace = append(c.hooks.SessionWorkspace, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `sessionworkspace.Intercept(f(g(h())))`.
func (c *SessionWorkspaceClient) Intercept(interceptors ...Interceptor) {
	c.inters.SessionWorkspace = append(c.inters.SessionWorkspace, interceptors...)
}

// Create returns a builder for creating a SessionWorkspace entity.
func (c *SessionWorkspaceClient) Create() *SessionWorkspaceCreate {
	mutation := newSessionWorkspaceMutation(c.config, OpCreate)
	return &SessionWorkspaceCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SessionWorkspace entities.
func (c *SessionWorkspaceClient) CreateBulk(builders ...*SessionWorkspaceCreate) *SessionWorkspaceCreateBulk {
	return &SessionWorkspaceCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionWorkspaceClient) MapCreateBulk(slice any, setFunc func(*SessionWorkspaceCreate, int)) *SessionWorkspaceCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionWorkspaceCreateBulk{err: fmt.Errorf("calling to SessionWorkspaceClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionWorkspaceCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionWorkspaceCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SessionWorkspace.
func (c *SessionWorkspaceClient) Update() *SessionWorkspaceUpdate {
	mutation := newSessionWorkspaceMutation(c.config, OpUpdate)
	return &SessionWorkspaceUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionWorkspaceClient) UpdateOne(sw *SessionWorkspace) *SessionWorkspaceUpdateOne {
	mutation := newSessionWorkspaceMutation(c.config, OpUpdateOne, withSessionWorkspace(sw))
	return &SessionWorkspaceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionWorkspaceClient) UpdateOneID(id int) *SessionWorkspaceUpdateOne {
	mutation := newSessionWorkspaceMutation(c.config, OpUpdateOne, withSessionWorkspaceID(id))
	return &SessionWorkspaceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SessionWorkspace.
func (c *SessionWorkspaceClient) Delete() *SessionWorkspaceDelete {
	mutation := newSessionWorkspaceMutation(c.config, OpDelete)
	return &SessionWorkspaceDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionWorkspaceClient) DeleteOne(sw *SessionWorkspace) *SessionWorkspaceDeleteOne {
	return c.DeleteOneID(sw.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionWorkspaceClient) DeleteOneID(id int) *SessionWorkspaceDeleteOne {
	builder := c.Delete().Where(sessionworkspace.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionWorkspaceDeleteOne{builder}
}

// Query returns a query builder for SessionWorkspace.
func (c *SessionWorkspaceClient) Query() *SessionWorkspaceQuery {
	return &SessionWorkspaceQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSessionWorkspace},
		inters: c.Interceptors(),
	}
}

// Get returns a SessionWorkspace entity by its id.
func (c *SessionWorkspaceClient) Get(ctx context.Context, id int) (*SessionWorkspace, error) {
	return c.Query().Where(sessionworkspace.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionWorkspaceClient) GetX(ctx context.Context, id int) *SessionWorkspace {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySession queries the session edge of a SessionWorkspace.
func (c *SessionWorkspaceClient) QuerySession(sw *SessionWorkspace) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := sw.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionworkspace.Table, sessionworkspace.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionworkspace.SessionTable, sessionworkspace.SessionColumn),
		)
		fromV = sqlgraph.Neighbors(sw.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *SessionWorkspaceClient) Hooks() []Hook {
	return c.hooks.SessionWorkspace
}

// Interceptors returns the client interceptors.
func (c *SessionWorkspaceClient) Interceptors() []Interceptor {
	return c.inters.SessionWorkspace
}

func (c *SessionWorkspaceClient) mutate(ctx context.Context, m *SessionWorkspaceMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionWorkspaceCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionWorkspaceUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionWorkspaceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionWorkspaceDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SessionWorkspace mutation op: %q", m.Op())
	}
}

// SystemSettingClient is a client for the SystemSetting schema.
type SystemSettingClient struct {
	config
}

// NewSystemSettingClient returns a client for the SystemSetting from the given config.
func NewSystemSettingClient(c config) *SystemSettingClient {
	return &SystemSettingClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `systemsetting.Hooks(f(g(h())))`.
func (c *SystemSettingClient) Use(hooks ...Hook) {
	c.hooks.SystemSetting = append(c.hooks.SystemSetting, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `systemsetting.Intercept(f(g(h())))`.
func (c *SystemSettingClient) Intercept(interceptors ...Interceptor) {
	c.inters.SystemSetting = append(c.inters.SystemSetting, interceptors...)
}

// Create returns a builder for creating a SystemSetting entity.
func (c *SystemSettingClient) Create() *SystemSettingCreate {
	mutation := newSystemSettingMutation(c.config, OpCreate)
	return &SystemSettingCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SystemSetting entities.
func (c *SystemSettingClient) CreateBulk(builders ...*SystemSettingCreate) *SystemSettingCreateBulk {
	return &SystemSettingCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SystemSettingClient) MapCreateBulk(slice any, setFunc func(*SystemSettingCreate, int)) *SystemSettingCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SystemSettingCreateBulk{err: fmt.Errorf("calling to SystemSettingClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SystemSettingCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SystemSettingCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SystemSetting.
func (c *SystemSettingClient) Update() *SystemSettingUpdate {
	mutation := newSystemSettingMutation(c.config, OpUpdate)
	return &SystemSettingUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SystemSettingClient) UpdateOne(ss *SystemSetting) *SystemSettingUpdateOne {
	mutation := newSystemSettingMutation(c.config, OpUpdateOne, withSystemSetting(ss))
	return &SystemSettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SystemSettingClient) UpdateOneID(id int) *SystemSettingUpdateOne {
	mutation := newSystemSettingMutation(c.config, OpUpdateOne, withSystemSettingID(id))
	return &SystemSettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SystemSetting.
func (c *SystemSettingClient) Delete() *SystemSettingDelete {
	mutation := newSystemSettingMutation(c.config, OpDelete)
	return &SystemSettingDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SystemSettingClient) DeleteOne(ss *SystemSetting) *SystemSettingDeleteOne {
	return c.DeleteOneID(ss.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SystemSettingClient) DeleteOneID(id int) *SystemSettingDeleteOne {
	builder := c.Delete().Where(systemsetting.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SystemSettingDeleteOne{builder}
}

// Query returns a query builder for SystemSetting.
func (c *SystemSettingClient) Query() *SystemSettingQuery {
	return &SystemSettingQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSystemSetting},
		inters: c.Interceptors(),
	}
}

// Get returns a SystemSetting entity by its id.
func (c *SystemSettingClient) Get(ctx context.Context, id int) (*SystemSetting, error) {
	return c.Query().Where(systemsetting.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SystemSettingClient) GetX(ctx context.Context, id int) *SystemSetting {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SystemSettingClient) Hooks() []Hook {
	return c.hooks.SystemSetting
}

// Interceptors returns the client interceptors.
func (c *SystemSettingClient) Interceptors() []Interceptor {
	return c.inters.SystemSetting
}

func (c *SystemSettingClient) mutate(ctx context.Context, m *SystemSettingMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SystemSettingCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SystemSettingUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SystemSettingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SystemSettingDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SystemSetting mutation op: %q", m.Op())
	}
}

// UserClient is a client for the User schema.
type UserClient struct {
	config
}

// NewUserClient returns a client for the User from the given config.
func NewUserClient(c config) *UserClient {
	return &UserClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `user.Hooks(f(g(h())))`.
func (c *UserClient) Use(hooks ...Hook) {
	c.hooks.User = append(c.hooks.User, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `user.Intercept(f(g(h())))`.
func (c *UserClient) Intercept(interceptors ...Interceptor) {
	c.inters.User = append(c.inters.User, interceptors...)
}

// Create returns a builder for creating a User entity.
func (c *UserClient) Create() *UserCreate {
	mutation := newUserMutation(c.config, OpCreate)
	return &UserCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of User entities.
func (c *UserClient) CreateBulk(builders ...*UserCreate) *UserCreateBulk {
	return &UserCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *UserClient) MapCreateBulk(slice any, setFunc func(*UserCreate, int)) *UserCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &UserCreateBulk{err: fmt.Errorf("calling to UserClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*UserCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &UserCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for User.
func (c *UserClient) Update() *UserUpdate {
	mutation := newUserMutation(c.config, OpUpdate)
	return &UserUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserClient) UpdateOne(u *User) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUser(u))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserClient) UpdateOneID(id int) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUserID(id))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for User.
func (c *UserClient) Delete() *UserDelete {
	mutation := newUserMutation(c.config, OpDelete)
	return &UserDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UserClient) DeleteOne(u *User) *UserDeleteOne {
	return c.DeleteOneID(u.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UserClient) DeleteOneID(id int) *UserDeleteOne {
	builder := c.Delete().Where(user.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserDeleteOne{builder}
}

// Query returns a query builder for User.
func (c *UserClient) Query() *UserQuery {
	return &UserQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUser},
		inters: c.Interceptors(),
	}
}

// Get returns a User entity by its id.
func (c *UserClient) Get(ctx context.Context, id int) (*User, error) {
	return c.Query().Where(user.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserClient) GetX(ctx context.Context, id int) *User {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QuerySessions queries the sessions edge of a User.
func (c *UserClient) QuerySessions(u *User) *SessionQuery {
	query := (&SessionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := u.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(user.Table, user.FieldID, id),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, user.SessionsTable, user.SessionsColumn),
		)
		fromV = sqlgraph.Neighbors(u.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *UserClient) Hooks() []Hook {
	return c.hooks.User
}

// Interceptors returns the client interceptors.
func (c *UserClient) Interceptors() []Interceptor {
	return c.inters.User
}

func (c *UserClient) mutate(ctx context.Context, m *UserMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UserCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UserUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UserDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown User mutation op: %q", m.Op())
	}
}

// WebhookDeadLetterClient is a client for the WebhookDeadLetter schema.
type WebhookDeadLetterClient struct {
	config
}

// NewWebhookDeadLetterClient returns a client for the WebhookDeadLetter from the given config.
func NewWebhookDeadLetterClient(c config) *WebhookDeadLetterClient {
	return &WebhookDeadLetterClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `webhookdeadletter.Hooks(f(g(h())))`.
func (c *WebhookDeadLetterClient) Use(hooks ...Hook) {
	c.hooks.WebhookDeadLetter = append(c.hooks.WebhookDeadLetter, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `webhookdeadletter.Intercept(f(g(h())))`.
func (c *WebhookDeadLetterClient) Intercept(interceptors ...Interceptor) {
	c.inters.WebhookDeadLetter = append(c.inters.WebhookDeadLetter, interceptors...)
}

// Create returns a builder for creating a WebhookDeadLetter entity.
func (c *WebhookDeadLetterClient) Create() *WebhookDeadLetterCreate {
	mutation := newWebhookDeadLetterMutation(c.config, OpCreate)
	return &WebhookDeadLetterCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of WebhookDeadLetter entities.
func (c *WebhookDeadLetterClient) CreateBulk(builders ...*WebhookDeadLetterCreate) *WebhookDeadLetterCreateBulk {
	return &WebhookDeadLetterCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *WebhookDeadLetterClient) MapCreateBulk(slice any, setFunc func(*WebhookDeadLetterCreate, int)) *WebhookDeadLetterCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &WebhookDeadLetterCreateBulk{err: fmt.Errorf("calling to WebhookDeadLetterClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*WebhookDeadLetterCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &WebhookDeadLetterCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for WebhookDeadLetter.
func (c *WebhookDeadLetterClient) Update() *WebhookDeadLetterUpdate {
	mutation := newWebhookDeadLetterMutation(c.config, OpUpdate)
	return &WebhookDeadLetterUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *WebhookDeadLetterClient) UpdateOne(wdl *WebhookDeadLetter) *WebhookDeadLetterUpdateOne {
	mutation := newWebhookDeadLetterMutation(c.config, OpUpdateOne, withWebhookDeadLetter(wdl))
	return &WebhookDeadLetterUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *WebhookDeadLetterClient) UpdateOneID(id int) *WebhookDeadLetterUpdateOne {
	mutation := newWebhookDeadLetterMutation(c.config, OpUpdateOne, withWebhookDeadLetterID(id))
	return &WebhookDeadLetterUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for WebhookDeadLetter.
func (c *WebhookDeadLetterClient) Delete() *WebhookDeadLetterDelete {
	mutation := newWebhookDeadLetterMutation(c.config, OpDelete)
	return &WebhookDeadLetterDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *WebhookDeadLetterClient) DeleteOne(wdl *WebhookDeadLetter) *WebhookDeadLetterDeleteOne {
	return c.DeleteOneID(wdl.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *WebhookDeadLetterClient) DeleteOneID(id int) *WebhookDeadLetterDeleteOne {
	builder := c.Delete().Where(webhookdeadletter.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &WebhookDeadLetterDeleteOne{builder}
}

// Query returns a query builder for WebhookDeadLetter.
func (c *WebhookDeadLetterClient) Query() *WebhookDeadLetterQuery {
	return &WebhookDeadLetterQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeWebhookDeadLetter},
		inters: c.Interceptors(),
	}
}

// Get returns a WebhookDeadLetter entity by its id.
func (c *WebhookDeadLetterClient) Get(ctx context.Context, id int) (*WebhookDeadLetter, error) {
	return c.Query().Where(webhookdeadletter.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *WebhookDeadLetterClient) GetX(ctx context.Context, id int) *WebhookDeadLetter {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryRepoConfig queries the repo_config edge of a WebhookDeadLetter.
func (c *WebhookDeadLetterClient) QueryRepoConfig(wdl *WebhookDeadLetter) *RepoConfigQuery {
	query := (&RepoConfigClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := wdl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(webhookdeadletter.Table, webhookdeadletter.FieldID, id),
			sqlgraph.To(repoconfig.Table, repoconfig.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, webhookdeadletter.RepoConfigTable, webhookdeadletter.RepoConfigColumn),
		)
		fromV = sqlgraph.Neighbors(wdl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *WebhookDeadLetterClient) Hooks() []Hook {
	return c.hooks.WebhookDeadLetter
}

// Interceptors returns the client interceptors.
func (c *WebhookDeadLetterClient) Interceptors() []Interceptor {
	return c.inters.WebhookDeadLetter
}

func (c *WebhookDeadLetterClient) mutate(ctx context.Context, m *WebhookDeadLetterMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&WebhookDeadLetterCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&WebhookDeadLetterUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&WebhookDeadLetterUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&WebhookDeadLetterDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown WebhookDeadLetter mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		AgentMetadataEvent, AiScanResult, CommitCheckpoint, CommitRewrite, Credential,
		EfficiencyMetric, PrAttributionRun, PrRecord, RelayProvider, RepoConfig,
		ScmProvider, Session, SessionEvent, SessionUsageEvent, SessionWorkspace,
		SystemSetting, User, WebhookDeadLetter []ent.Hook
	}
	inters struct {
		AgentMetadataEvent, AiScanResult, CommitCheckpoint, CommitRewrite, Credential,
		EfficiencyMetric, PrAttributionRun, PrRecord, RelayProvider, RepoConfig,
		ScmProvider, Session, SessionEvent, SessionUsageEvent, SessionWorkspace,
		SystemSetting, User, WebhookDeadLetter []ent.Interceptor
	}
)
