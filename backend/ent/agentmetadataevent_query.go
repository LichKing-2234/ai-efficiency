// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/agentmetadataevent"
	"github.com/ai-efficiency/backend/ent/predicate"
	"github.com/ai-efficiency/backend/ent/session"
	"github.com/google/uuid"
)

// AgentMetadataEventQuery is the builder for querying AgentMetadataEvent entities.
type AgentMetadataEventQuery struct {
	config
	ctx         *QueryContext
	order       []agentmetadataevent.OrderOption
	inters      []Interceptor
	predicates  []predicate.AgentMetadataEvent
	withSession *SessionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AgentMetadataEventQuery builder.
func (ameq *AgentMetadataEventQuery) Where(ps ...predicate.AgentMetadataEvent) *AgentMetadataEventQuery {
	ameq.predicates = append(ameq.predicates, ps...)
	return ameq
}

// Limit the number of records to be returned by this query.
func (ameq *AgentMetadataEventQuery) Limit(limit int) *AgentMetadataEventQuery {
	ameq.ctx.Limit = &limit
	return ameq
}

// Offset to start from.
func (ameq *AgentMetadataEventQuery) Offset(offset int) *AgentMetadataEventQuery {
	ameq.ctx.Offset = &offset
	return ameq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ameq *AgentMetadataEventQuery) Unique(unique bool) *AgentMetadataEventQuery {
	ameq.ctx.Unique = &unique
	return ameq
}

// Order specifies how the records should be ordered.
func (ameq *AgentMetadataEventQuery) Order(o ...agentmetadataevent.OrderOption) *AgentMetadataEventQuery {
	ameq.order = append(ameq.order, o...)
	return ameq
}

// QuerySession chains the current query on the "session" edge.
func (ameq *AgentMetadataEventQuery) QuerySession() *SessionQuery {
	query := (&SessionClient{config: ameq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ameq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ameq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentmetadataevent.Table, agentmetadataevent.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, agentmetadataevent.SessionTable, agentmetadataevent.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(ameq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AgentMetadataEvent entity from the query.
// Returns a *NotFoundError when no AgentMetadataEvent was found.
func (ameq *AgentMetadataEventQuery) First(ctx context.Context) (*AgentMetadataEvent, error) {
	nodes, err := ameq.Limit(1).All(setContextOp(ctx, ameq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{agentmetadataevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) FirstX(ctx context.Context) *AgentMetadataEvent {
	node, err := ameq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AgentMetadataEvent ID from the query.
// Returns a *NotFoundError when no AgentMetadataEvent ID was found.
func (ameq *AgentMetadataEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ameq.Limit(1).IDs(setContextOp(ctx, ameq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{agentmetadataevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) FirstIDX(ctx context.Context) int {
	id, err := ameq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AgentMetadataEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AgentMetadataEvent entity is found.
// Returns a *NotFoundError when no AgentMetadataEvent entities are found.
func (ameq *AgentMetadataEventQuery) Only(ctx context.Context) (*AgentMetadataEvent, error) {
	nodes, err := ameq.Limit(2).All(setContextOp(ctx, ameq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{agentmetadataevent.Label}
	default:
		return nil, &NotSingularError{agentmetadataevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) OnlyX(ctx context.Context) *AgentMetadataEvent {
	node, err := ameq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AgentMetadataEvent ID in the query.
// Returns a *NotSingularError when more than one AgentMetadataEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (ameq *AgentMetadataEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ameq.Limit(2).IDs(setContextOp(ctx, ameq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{agentmetadataevent.Label}
	default:
		err = &NotSingularError{agentmetadataevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := ameq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AgentMetadataEvents.
func (ameq *AgentMetadataEventQuery) All(ctx context.Context) ([]*AgentMetadataEvent, error) {
	ctx = setContextOp(ctx, ameq.ctx, ent.OpQueryAll)
	if err := ameq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AgentMetadataEvent, *AgentMetadataEventQuery]()
	return withInterceptors[[]*AgentMetadataEvent](ctx, ameq, qr, ameq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) AllX(ctx context.Context) []*AgentMetadataEvent {
	nodes, err := ameq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AgentMetadataEvent IDs.
func (ameq *AgentMetadataEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ameq.ctx.Unique == nil && ameq.path != nil {
		ameq.Unique(true)
	}
	ctx = setContextOp(ctx, ameq.ctx, ent.OpQueryIDs)
	if err = ameq.Select(agentmetadataevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) IDsX(ctx context.Context) []int {
	ids, err := ameq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ameq *AgentMetadataEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ameq.ctx, ent.OpQueryCount)
	if err := ameq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ameq, querierCount[*AgentMetadataEventQuery](), ameq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) CountX(ctx context.Context) int {
	count, err := ameq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ameq *AgentMetadataEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ameq.ctx, ent.OpQueryExist)
	switch _, err := ameq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ameq *AgentMetadataEventQuery) ExistX(ctx context.Context) bool {
	exist, err := ameq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AgentMetadataEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ameq *AgentMetadataEventQuery) Clone() *AgentMetadataEventQuery {
	if ameq == nil {
		return nil
	}
	return &AgentMetadataEventQuery{
		config:      ameq.config,
		ctx:         ameq.ctx.Clone(),
		order:       append([]agentmetadataevent.OrderOption{}, ameq.order...),
		inters:      append([]Interceptor{}, ameq.inters...),
		predicates:  append([]predicate.AgentMetadataEvent{}, ameq.predicates...),
		withSession: ameq.withSession.Clone(),
		// clone intermediate query.
		sql:  ameq.sql.Clone(),
		path: ameq.path,
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (ameq *AgentMetadataEventQuery) WithSession(opts ...func(*SessionQuery)) *AgentMetadataEventQuery {
	query := (&SessionClient{config: ameq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ameq.withSession = query
	return ameq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SessionID uuid.UUID `json:"session_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AgentMetadataEvent.Query().
//		GroupBy(agentmetadataevent.FieldSessionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ameq *AgentMetadataEventQuery) GroupBy(field string, fields ...string) *AgentMetadataEventGroupBy {
	ameq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AgentMetadataEventGroupBy{build: ameq}
	grbuild.flds = &ameq.ctx.Fields
	grbuild.label = agentmetadataevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SessionID uuid.UUID `json:"session_id,omitempty"`
//	}
//
//	client.AgentMetadataEvent.Query().
//		Select(agentmetadataevent.FieldSessionID).
//		Scan(ctx, &v)
func (ameq *AgentMetadataEventQuery) Select(fields ...string) *AgentMetadataEventSelect {
	ameq.ctx.Fields = append(ameq.ctx.Fields, fields...)
	sbuild := &AgentMetadataEventSelect{AgentMetadataEventQuery: ameq}
	sbuild.label = agentmetadataevent.Label
	sbuild.flds, sbuild.scan = &ameq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AgentMetadataEventSelect configured with the given aggregations.
func (ameq *AgentMetadataEventQuery) Aggregate(fns ...AggregateFunc) *AgentMetadataEventSelect {
	return ameq.Select().Aggregate(fns...)
}

func (ameq *AgentMetadataEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ameq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ameq); err != nil {
				return err
			}
		}
	}
	for _, f := range ameq.ctx.Fields {
		if !agentmetadataevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ameq.path != nil {
		prev, err := ameq.path(ctx)
		if err != nil {
			return err
		}
		ameq.sql = prev
	}
	return nil
}

func (ameq *AgentMetadataEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AgentMetadataEvent, error) {
	var (
		nodes       = []*AgentMetadataEvent{}
		_spec       = ameq.querySpec()
		loadedTypes = [1]bool{
			ameq.withSession != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AgentMetadataEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AgentMetadataEvent{config: ameq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ameq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ameq.withSession; query != nil {
		if err := ameq.loadSession(ctx, query, nodes, nil,
			func(n *AgentMetadataEvent, e *Session) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ameq *AgentMetadataEventQuery) loadSession(ctx context.Context, query *SessionQuery, nodes []*AgentMetadataEvent, init func(*AgentMetadataEvent), assign func(*AgentMetadataEvent, *Session)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AgentMetadataEvent)
	for i := range nodes {
		fk := nodes[i].SessionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(session.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ameq *AgentMetadataEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ameq.querySpec()
	_spec.Node.Columns = ameq.ctx.Fields
	if len(ameq.ctx.Fields) > 0 {
		_spec.Unique = ameq.ctx.Unique != nil && *ameq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ameq.driver, _spec)
}

func (ameq *AgentMetadataEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(agentmetadataevent.Table, agentmetadataevent.Columns, sqlgraph.NewFieldSpec(agentmetadataevent.FieldID, field.TypeInt))
	_spec.From = ameq.sql
	if unique := ameq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ameq.path != nil {
		_spec.Unique = true
	}
	if fields := ameq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, agentmetadataevent.FieldID)
		for i := range fields {
			if fields[i] != agentmetadataevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ameq.withSession != nil {
			_spec.Node.AddColumnOnce(agentmetadataevent.FieldSessionID)
		}
	}
	if ps := ameq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ameq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ameq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ameq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ameq *AgentMetadataEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ameq.driver.Dialect())
	t1 := builder.Table(agentmetadataevent.Table)
	columns := ameq.ctx.Fields
	if len(columns) == 0 {
		columns = agentmetadataevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ameq.sql != nil {
		selector = ameq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ameq.ctx.Unique != nil && *ameq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ameq.predicates {
		p(selector)
	}
	for _, p := range ameq.order {
		p(selector)
	}
	if offset := ameq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ameq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AgentMetadataEventGroupBy is the group-by builder for AgentMetadataEvent entities.
type AgentMetadataEventGroupBy struct {
	selector
	build *AgentMetadataEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (amegb *AgentMetadataEventGroupBy) Aggregate(fns ...AggregateFunc) *AgentMetadataEventGroupBy {
	amegb.fns = append(amegb.fns, fns...)
	return amegb
}

// Scan applies the selector query and scans the result into the given value.
func (amegb *AgentMetadataEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, amegb.build.ctx, ent.OpQueryGroupBy)
	if err := amegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgentMetadataEventQuery, *AgentMetadataEventGroupBy](ctx, amegb.build, amegb, amegb.build.inters, v)
}

func (amegb *AgentMetadataEventGroupBy) sqlScan(ctx context.Context, root *AgentMetadataEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(amegb.fns))
	for _, fn := range amegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*amegb.flds)+len(amegb.fns))
		for _, f := range *amegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*amegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := amegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AgentMetadataEventSelect is the builder for selecting fields of AgentMetadataEvent entities.
type AgentMetadataEventSelect struct {
	*AgentMetadataEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ames *AgentMetadataEventSelect) Aggregate(fns ...AggregateFunc) *AgentMetadataEventSelect {
	ames.fns = append(ames.fns, fns...)
	return ames
}

// Scan applies the selector query and scans the result into the given value.
func (ames *AgentMetadataEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ames.ctx, ent.OpQuerySelect)
	if err := ames.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgentMetadataEventQuery, *AgentMetadataEventSelect](ctx, ames.AgentMetadataEventQuery, ames, ames.inters, v)
}

func (ames *AgentMetadataEventSelect) sqlScan(ctx context.Context, root *AgentMetadataEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ames.fns))
	for _, fn := range ames.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ames.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ames.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
