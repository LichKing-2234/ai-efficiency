import type * as React from 'react'
import { FieldDescription } from '@/components/ui/field'

export function HelperText(props: React.ComponentProps<typeof FieldDescription>) {
  return <FieldDescription {...props} />
}
