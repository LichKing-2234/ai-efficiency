// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ai-efficiency/backend/ent/directorymember"
)

// DirectoryMember is the model entity for the DirectoryMember schema.
type DirectoryMember struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SourceID holds the value of the "source_id" field.
	SourceID int `json:"source_id,omitempty"`
	// ExternalID holds the value of the "external_id" field.
	ExternalID string `json:"external_id,omitempty"`
	// EmailNormalized holds the value of the "email_normalized" field.
	EmailNormalized string `json:"email_normalized,omitempty"`
	// DisplayName holds the value of the "display_name" field.
	DisplayName string `json:"display_name,omitempty"`
	// DepartmentExternalID holds the value of the "department_external_id" field.
	DepartmentExternalID string `json:"department_external_id,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// MatchedUserID holds the value of the "matched_user_id" field.
	MatchedUserID *int `json:"matched_user_id,omitempty"`
	// LastSeenRunID holds the value of the "last_seen_run_id" field.
	LastSeenRunID int `json:"last_seen_run_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DirectoryMember) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case directorymember.FieldMetadata:
			values[i] = new([]byte)
		case directorymember.FieldID, directorymember.FieldSourceID, directorymember.FieldMatchedUserID, directorymember.FieldLastSeenRunID:
			values[i] = new(sql.NullInt64)
		case directorymember.FieldExternalID, directorymember.FieldEmailNormalized, directorymember.FieldDisplayName, directorymember.FieldDepartmentExternalID, directorymember.FieldStatus:
			values[i] = new(sql.NullString)
		case directorymember.FieldCreatedAt, directorymember.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DirectoryMember fields.
func (dm *DirectoryMember) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case directorymember.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dm.ID = int(value.Int64)
		case directorymember.FieldSourceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field source_id", values[i])
			} else if value.Valid {
				dm.SourceID = int(value.Int64)
			}
		case directorymember.FieldExternalID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field external_id", values[i])
			} else if value.Valid {
				dm.ExternalID = value.String
			}
		case directorymember.FieldEmailNormalized:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email_normalized", values[i])
			} else if value.Valid {
				dm.EmailNormalized = value.String
			}
		case directorymember.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				dm.DisplayName = value.String
			}
		case directorymember.FieldDepartmentExternalID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field department_external_id", values[i])
			} else if value.Valid {
				dm.DepartmentExternalID = value.String
			}
		case directorymember.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				dm.Status = value.String
			}
		case directorymember.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &dm.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case directorymember.FieldMatchedUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field matched_user_id", values[i])
			} else if value.Valid {
				dm.MatchedUserID = new(int)
				*dm.MatchedUserID = int(value.Int64)
			}
		case directorymember.FieldLastSeenRunID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_run_id", values[i])
			} else if value.Valid {
				dm.LastSeenRunID = int(value.Int64)
			}
		case directorymember.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dm.CreatedAt = value.Time
			}
		case directorymember.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dm.UpdatedAt = value.Time
			}
		default:
			dm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DirectoryMember.
// This includes values selected through modifiers, order, etc.
func (dm *DirectoryMember) Value(name string) (ent.Value, error) {
	return dm.selectValues.Get(name)
}

// Update returns a builder for updating this DirectoryMember.
// Note that you need to call DirectoryMember.Unwrap() before calling this method if this DirectoryMember
// was returned from a transaction, and the transaction was committed or rolled back.
func (dm *DirectoryMember) Update() *DirectoryMemberUpdateOne {
	return NewDirectoryMemberClient(dm.config).UpdateOne(dm)
}

// Unwrap unwraps the DirectoryMember entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dm *DirectoryMember) Unwrap() *DirectoryMember {
	_tx, ok := dm.config.driver.(*txDriver)
	if !ok {
		panic("ent: DirectoryMember is not a transactional entity")
	}
	dm.config.driver = _tx.drv
	return dm
}

// String implements the fmt.Stringer.
func (dm *DirectoryMember) String() string {
	var builder strings.Builder
	builder.WriteString("DirectoryMember(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dm.ID))
	builder.WriteString("source_id=")
	builder.WriteString(fmt.Sprintf("%v", dm.SourceID))
	builder.WriteString(", ")
	builder.WriteString("external_id=")
	builder.WriteString(dm.ExternalID)
	builder.WriteString(", ")
	builder.WriteString("email_normalized=")
	builder.WriteString(dm.EmailNormalized)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(dm.DisplayName)
	builder.WriteString(", ")
	builder.WriteString("department_external_id=")
	builder.WriteString(dm.DepartmentExternalID)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(dm.Status)
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", dm.Metadata))
	builder.WriteString(", ")
	if v := dm.MatchedUserID; v != nil {
		builder.WriteString("matched_user_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("last_seen_run_id=")
	builder.WriteString(fmt.Sprintf("%v", dm.LastSeenRunID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dm.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DirectoryMembers is a parsable slice of DirectoryMember.
type DirectoryMembers []*DirectoryMember
