// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ai-efficiency/backend/ent/directorysyncrun"
	"github.com/ai-efficiency/backend/ent/predicate"
)

// DirectorySyncRunQuery is the builder for querying DirectorySyncRun entities.
type DirectorySyncRunQuery struct {
	config
	ctx        *QueryContext
	order      []directorysyncrun.OrderOption
	inters     []Interceptor
	predicates []predicate.DirectorySyncRun
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DirectorySyncRunQuery builder.
func (dsrq *DirectorySyncRunQuery) Where(ps ...predicate.DirectorySyncRun) *DirectorySyncRunQuery {
	dsrq.predicates = append(dsrq.predicates, ps...)
	return dsrq
}

// Limit the number of records to be returned by this query.
func (dsrq *DirectorySyncRunQuery) Limit(limit int) *DirectorySyncRunQuery {
	dsrq.ctx.Limit = &limit
	return dsrq
}

// Offset to start from.
func (dsrq *DirectorySyncRunQuery) Offset(offset int) *DirectorySyncRunQuery {
	dsrq.ctx.Offset = &offset
	return dsrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dsrq *DirectorySyncRunQuery) Unique(unique bool) *DirectorySyncRunQuery {
	dsrq.ctx.Unique = &unique
	return dsrq
}

// Order specifies how the records should be ordered.
func (dsrq *DirectorySyncRunQuery) Order(o ...directorysyncrun.OrderOption) *DirectorySyncRunQuery {
	dsrq.order = append(dsrq.order, o...)
	return dsrq
}

// First returns the first DirectorySyncRun entity from the query.
// Returns a *NotFoundError when no DirectorySyncRun was found.
func (dsrq *DirectorySyncRunQuery) First(ctx context.Context) (*DirectorySyncRun, error) {
	nodes, err := dsrq.Limit(1).All(setContextOp(ctx, dsrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{directorysyncrun.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) FirstX(ctx context.Context) *DirectorySyncRun {
	node, err := dsrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DirectorySyncRun ID from the query.
// Returns a *NotFoundError when no DirectorySyncRun ID was found.
func (dsrq *DirectorySyncRunQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dsrq.Limit(1).IDs(setContextOp(ctx, dsrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{directorysyncrun.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) FirstIDX(ctx context.Context) int {
	id, err := dsrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DirectorySyncRun entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DirectorySyncRun entity is found.
// Returns a *NotFoundError when no DirectorySyncRun entities are found.
func (dsrq *DirectorySyncRunQuery) Only(ctx context.Context) (*DirectorySyncRun, error) {
	nodes, err := dsrq.Limit(2).All(setContextOp(ctx, dsrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{directorysyncrun.Label}
	default:
		return nil, &NotSingularError{directorysyncrun.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) OnlyX(ctx context.Context) *DirectorySyncRun {
	node, err := dsrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DirectorySyncRun ID in the query.
// Returns a *NotSingularError when more than one DirectorySyncRun ID is found.
// Returns a *NotFoundError when no entities are found.
func (dsrq *DirectorySyncRunQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dsrq.Limit(2).IDs(setContextOp(ctx, dsrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{directorysyncrun.Label}
	default:
		err = &NotSingularError{directorysyncrun.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) OnlyIDX(ctx context.Context) int {
	id, err := dsrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DirectorySyncRuns.
func (dsrq *DirectorySyncRunQuery) All(ctx context.Context) ([]*DirectorySyncRun, error) {
	ctx = setContextOp(ctx, dsrq.ctx, ent.OpQueryAll)
	if err := dsrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DirectorySyncRun, *DirectorySyncRunQuery]()
	return withInterceptors[[]*DirectorySyncRun](ctx, dsrq, qr, dsrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) AllX(ctx context.Context) []*DirectorySyncRun {
	nodes, err := dsrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DirectorySyncRun IDs.
func (dsrq *DirectorySyncRunQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dsrq.ctx.Unique == nil && dsrq.path != nil {
		dsrq.Unique(true)
	}
	ctx = setContextOp(ctx, dsrq.ctx, ent.OpQueryIDs)
	if err = dsrq.Select(directorysyncrun.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) IDsX(ctx context.Context) []int {
	ids, err := dsrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dsrq *DirectorySyncRunQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dsrq.ctx, ent.OpQueryCount)
	if err := dsrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dsrq, querierCount[*DirectorySyncRunQuery](), dsrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) CountX(ctx context.Context) int {
	count, err := dsrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dsrq *DirectorySyncRunQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dsrq.ctx, ent.OpQueryExist)
	switch _, err := dsrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dsrq *DirectorySyncRunQuery) ExistX(ctx context.Context) bool {
	exist, err := dsrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DirectorySyncRunQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dsrq *DirectorySyncRunQuery) Clone() *DirectorySyncRunQuery {
	if dsrq == nil {
		return nil
	}
	return &DirectorySyncRunQuery{
		config:     dsrq.config,
		ctx:        dsrq.ctx.Clone(),
		order:      append([]directorysyncrun.OrderOption{}, dsrq.order...),
		inters:     append([]Interceptor{}, dsrq.inters...),
		predicates: append([]predicate.DirectorySyncRun{}, dsrq.predicates...),
		// clone intermediate query.
		sql:  dsrq.sql.Clone(),
		path: dsrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DirectorySyncRun.Query().
//		GroupBy(directorysyncrun.FieldSourceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dsrq *DirectorySyncRunQuery) GroupBy(field string, fields ...string) *DirectorySyncRunGroupBy {
	dsrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DirectorySyncRunGroupBy{build: dsrq}
	grbuild.flds = &dsrq.ctx.Fields
	grbuild.label = directorysyncrun.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SourceID int `json:"source_id,omitempty"`
//	}
//
//	client.DirectorySyncRun.Query().
//		Select(directorysyncrun.FieldSourceID).
//		Scan(ctx, &v)
func (dsrq *DirectorySyncRunQuery) Select(fields ...string) *DirectorySyncRunSelect {
	dsrq.ctx.Fields = append(dsrq.ctx.Fields, fields...)
	sbuild := &DirectorySyncRunSelect{DirectorySyncRunQuery: dsrq}
	sbuild.label = directorysyncrun.Label
	sbuild.flds, sbuild.scan = &dsrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DirectorySyncRunSelect configured with the given aggregations.
func (dsrq *DirectorySyncRunQuery) Aggregate(fns ...AggregateFunc) *DirectorySyncRunSelect {
	return dsrq.Select().Aggregate(fns...)
}

func (dsrq *DirectorySyncRunQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dsrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dsrq); err != nil {
				return err
			}
		}
	}
	for _, f := range dsrq.ctx.Fields {
		if !directorysyncrun.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dsrq.path != nil {
		prev, err := dsrq.path(ctx)
		if err != nil {
			return err
		}
		dsrq.sql = prev
	}
	return nil
}

func (dsrq *DirectorySyncRunQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DirectorySyncRun, error) {
	var (
		nodes = []*DirectorySyncRun{}
		_spec = dsrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DirectorySyncRun).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DirectorySyncRun{config: dsrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dsrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dsrq *DirectorySyncRunQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dsrq.querySpec()
	_spec.Node.Columns = dsrq.ctx.Fields
	if len(dsrq.ctx.Fields) > 0 {
		_spec.Unique = dsrq.ctx.Unique != nil && *dsrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dsrq.driver, _spec)
}

func (dsrq *DirectorySyncRunQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(directorysyncrun.Table, directorysyncrun.Columns, sqlgraph.NewFieldSpec(directorysyncrun.FieldID, field.TypeInt))
	_spec.From = dsrq.sql
	if unique := dsrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dsrq.path != nil {
		_spec.Unique = true
	}
	if fields := dsrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, directorysyncrun.FieldID)
		for i := range fields {
			if fields[i] != directorysyncrun.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dsrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dsrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dsrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dsrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dsrq *DirectorySyncRunQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dsrq.driver.Dialect())
	t1 := builder.Table(directorysyncrun.Table)
	columns := dsrq.ctx.Fields
	if len(columns) == 0 {
		columns = directorysyncrun.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dsrq.sql != nil {
		selector = dsrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dsrq.ctx.Unique != nil && *dsrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dsrq.predicates {
		p(selector)
	}
	for _, p := range dsrq.order {
		p(selector)
	}
	if offset := dsrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dsrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DirectorySyncRunGroupBy is the group-by builder for DirectorySyncRun entities.
type DirectorySyncRunGroupBy struct {
	selector
	build *DirectorySyncRunQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dsrgb *DirectorySyncRunGroupBy) Aggregate(fns ...AggregateFunc) *DirectorySyncRunGroupBy {
	dsrgb.fns = append(dsrgb.fns, fns...)
	return dsrgb
}

// Scan applies the selector query and scans the result into the given value.
func (dsrgb *DirectorySyncRunGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dsrgb.build.ctx, ent.OpQueryGroupBy)
	if err := dsrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectorySyncRunQuery, *DirectorySyncRunGroupBy](ctx, dsrgb.build, dsrgb, dsrgb.build.inters, v)
}

func (dsrgb *DirectorySyncRunGroupBy) sqlScan(ctx context.Context, root *DirectorySyncRunQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dsrgb.fns))
	for _, fn := range dsrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dsrgb.flds)+len(dsrgb.fns))
		for _, f := range *dsrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dsrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dsrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DirectorySyncRunSelect is the builder for selecting fields of DirectorySyncRun entities.
type DirectorySyncRunSelect struct {
	*DirectorySyncRunQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dsrs *DirectorySyncRunSelect) Aggregate(fns ...AggregateFunc) *DirectorySyncRunSelect {
	dsrs.fns = append(dsrs.fns, fns...)
	return dsrs
}

// Scan applies the selector query and scans the result into the given value.
func (dsrs *DirectorySyncRunSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dsrs.ctx, ent.OpQuerySelect)
	if err := dsrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DirectorySyncRunQuery, *DirectorySyncRunSelect](ctx, dsrs.DirectorySyncRunQuery, dsrs, dsrs.inters, v)
}

func (dsrs *DirectorySyncRunSelect) sqlScan(ctx context.Context, root *DirectorySyncRunQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dsrs.fns))
	for _, fn := range dsrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dsrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dsrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
